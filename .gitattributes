# Kabuk betikleri ve konteynere giren dosyalar DAİMA LF (BUG #230 takibi).
# Windows'ta geliştirilip Linux konteynerine mount edilen bir betik CRLF'liyse
# `#!/bin/sh\r` yüzünden "no such file or directory" verir — yedek/entrypoint
# SESSİZCE çalışmaz. Bu tam da D09'un sınıfı: "kurdum sandığı ama koşmayan iş".
*.sh text eol=lf
docker-entrypoint.sh text eol=lf
deploy/*.sh text eol=lf
Dockerfile* text eol=lf
*.yml text eol=lf
*.yaml text eol=lf

# BUG #306: dondurulmuş API sözleşmesi DAİMA LF. Ölçüm makineye bağlı olmamalı —
# seen-backend'de aynı repo iki makinede 31 ve 59 "biçimsiz dosya" ölçtü, sebebi CRLF'ti.
# Betik dosyayı zaten LF ile yazar; bu satır klonlamada da aynı garantiyi verir.
docs/api-reference/api-sozlesmesi.json text eol=lf

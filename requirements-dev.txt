# Geliştirme/test bağımlılıkları (TEST-011). Kurulum: pip install -r requirements-dev.txt
pytest>=8.0
pytest-cov>=5.0
httpx>=0.27          # FastAPI TestClient için
hypothesis>=6.100    # finansal matematik property testleri (RULE)

# BUG #309 (KAP-04) — kalite gerileme sayacı (`scripts/kalite_kapisi.py`).
# SÜRÜM TAM SABİT: tavan bir araç sürümüne aittir. Yeni sürüm kural ekler ya da mevcut
# kuralı değiştirir; o an `kalite-baseline.json`'daki sayı anlamını yitirir — zıplarsa
# sahte kırmızı, düşerse gerçek gerileme görünmez olur. Kapı tavandan ÖNCE sürümü doğrular.
ruff==0.16.4

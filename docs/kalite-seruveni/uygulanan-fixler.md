# Uygulanan Değişiklikler (Kalite Serüveni — fix ledger)

Her satır: yapıldı → **doğrulandı**. Halüsinasyon/varsayım yok; her fix çalıştırılarak teyit edildi. Backlog ID + BUG/ADR numarası referanslı.

> **KARAR (7 Ağu 2026): bu dosya TEK RESMÎ BUG ENVANTERİDİR.** Bundan sonra açılan her `BUG #NNN`
> buraya satır yazar — başka hiçbir yer envanter sayılmaz.
> **Dürüst kayıt (master rapor YANILGI-5):** repoda **235** benzersiz BUG numarası geçiyor, bu
> ledger'da **114**'ü var (#059–#254 aralığı). Eksiklerin kaydı `milestone-log.md` ve
> `masterprompt-publish.md` içinde dağınık duruyor. **Geriye dönük toplama YAPILMADI** — ayrı iş
> olarak açık bırakıldı; bu not onu gizlememek içindir.

| # | ID | Değişiklik | Dosya | Doğrulama | Durum |
|---|----|-----------|-------|-----------|-------|
| 1 | RULE-001 / BUG #059 | `account_type` kriteri `str(enum)` yerine `.value` — enum bug'ı yüzünden account_type kriterli her GoalRule sessizce ölüydü | `app/goal_rules.py:130` | `AccountType.cash.value=='cash'` (True), `str(...)=='AccountType.cash'` (False); import OK | ✅ |
| 2 | DATA-003/004 / BUG #060 | SQLite connect listener: `foreign_keys=ON`, WAL, `busy_timeout`, `synchronous=NORMAL` — FK enforcement default kapalıydı, ondelete tanımları çalışmıyordu | `app/database.py` | Canlı bağlantı: `foreign_keys=1, journal_mode=wal, busy_timeout=5000`. `foreign_key_check`: **20 yetim reasoning_traces kaydı** tespit (temizlenecek) | ✅ |
| 3 | RULE-023 / ADR-026 | ZikZak additive `carried_forward` REDDEDİLDİ (çift-sayım/Sanal Zenginlik). Dinamik `daily_limit` zaten zikzak; yanıltıcı yorum düzeltildi, fonksiyonlar DEPRECATED | `app/rules_engine.py:729-732`; `adr-026` | Simülasyon: naif today_target=1474.96 vs sürdürülebilir 394.10 (çift-sayım kanıtı). Import + davranış (today_target=344.84) korundu | ✅ |
| 4 | FE-002 / BUG #061 | Dinamik Tailwind renkleri prod'da purge oluyordu → safelist eklendi. **Ek latent bug:** palette'te 950 shade'i yoktu, `bg-color-950/30` (RedLines dark bg) hiç render olmuyordu → 4 palette'e 950 eklendi | `frontend/tailwind.config.js` | `npm run build` (5.25s) + dist CSS grep: `bg-warn-950/30`, `dark:bg-warn-950/30`, `text-brand-400`, `ring-negative-500` hepsi VAR | ✅ |

## Tur 2 — P0 sprinti (per-file MASTER-FIX-LIST'ten)

| # | ID | Değişiklik | Dosya | Doğrulama | Durum |
|---|----|-----------|-------|-----------|-------|
| 5 | P0-18 SC-001 / BUG #062 | `run_extractor` except'ine `db.rollback()` — bir extractor commit'te patlarsa paylaşılan session zehirlenip sonraki extractor'lar + Coach `_save_message` sessizce/patlayarak çalışmıyordu | `app/scheduler.py` | app.main import OK | ✅ |
| 6 | P0-21 SH-002 / BUG #063 | `GoalUpdate.status` literal'inden "achieved" çıkarıldı — kullanıcı PATCH ile hiç katkı yapmadan "sanal başarı" işaretleyemez; achieved yalnız refresh_goal'da | `app/schemas.py` | `GoalUpdate(status='achieved')` → ValidationError; 'active' geçerli | ✅ |
| 7 | P0-6 GR-001 / BUG #064 | "fixed" allocation işareti `tx_amount>=0` (hep True) yerine `tx.transaction_type`'tan — gidere eşleşen fixed kural withdrawal yerine +contribution kaydedip goal'i şişiriyordu | `app/goal_rules.py` | `income.value=='income'`; import OK | ✅ |
| 8 | P0-9 CS-001 / BUG #065 | premortem yanlış anahtar `crunch_day` (hep '-') → `lowest_balance_date` + `lowest_balance_tl` + `crunch_count` eklendi; nakit-kriz verisi artık LLM'e ulaşıyor | `app/premortem.py` | `build_cockpit_snapshot` anahtarları kodla teyit | ✅ |
| 9 | P0-5 GE-001 / BUG #066 | goal_engine `snowball.months_to_freedom` (attribute) → `snowball["months_to_freedom"]`; compare_strategies dict döndürüyor, AttributeError bare-except'te yutuluyordu → debt_freedom "tahmini bitiş" hep None'du | `app/goal_engine.py` | Canlı: `months_to_freedom=9` dönüyor; attribute erişimi AttributeError (eski hata teyidi) | ✅ |
| 10 | P0-20 RCH-003 / BUG #067 | `update_checkpoint`'e koruma: korunan (priority=1+red_line) checkpoint'in priority/checkpoint_type'ı değiştirilip sonra hard-delete ile Master Checkpoint enforcement delinmesin | `app/routers/checkpoints.py` | app.main + router import OK | ✅ |

| 11 | P0-2 AE-002 / BUG #068 | `_execute_sell_investment`: satış gelirinin hedef hesabı MUTASYONDAN ÖNCE doğrulanıyor — geçersiz/emanet/eksik hesapta lot düşmeden başarısız dönüyor (eskiden `net_eline_gecen` hiçbir yere yatmadan lot düşüp success dönüyordu → para sessizce kaybı) | `app/action_executor.py` | **In-memory test 4 senaryo:** geçerli→lot 6→2+para yatıyor; emanet/geçersiz/hedefsiz→fail+lot 6 kalıyor | ✅ |
| 12 | P0-1 AE-001 / BUG #069 | `execute_pending_action` post-commit trigger'ı ayrı try'a — trigger hatası zaten 'executed' aksiyonu 'failed' işaretleyip çift-sayıma yol açmasın. (Tam handler-commit birleştirmesi test ağı sonrası) | `app/action_executor.py` | app.main import OK | ✅ |

| 13 | P0-15 REX-001 / BUG #070 | Recurring `last_triggered_year_month` artık propose'ta değil **execute'te** (`_mark_recurring_triggered`) set ediliyor — reddedilen/başarısız gider "bu ay halledildi" sayılıp kaybolmuyor, re-triggerable kalıyor | `action_executor.py`, `expenses.py`, `incomes.py` | In-memory: None→2026-07; non-recurring no-op; import OK | ✅ |
| 14 | P0-16 RIN-001/REX-004 / BUG #071 | Recurring tetikleme `day_of_month`'u ay uzunluğuna clamp'liyor — day=31 kısa aylarda (Şubat/Nisan…) sessizce atlanmıyor | `expenses.py`, `incomes.py` | Clamp: 31→30 (Nisan), 31→28 (Şubat) | ✅ |

| 15 | P0-13/14 RGO-001/002 / BUG #072 | `create_allocation`: bu tx'e tüm hedeflerdeki mevcut allocation toplamı + yeni istek, `abs(tx.amount)`'ı aşamaz (422) — 10 TL işlem "1M katkı" veya aynı tx çok hedefe tam tutarla bağlanıp sanal zenginlik/çift-sayım üretemez | `app/routers/goals.py` | In-memory: sum(abs)=80; 80+30>100 reddet, 80+20 izin | ✅ |

| 16 | P0-11 RRE-001 / BUG #073 | reports category_breakdown "both" modunda gelir+gider aynı kategori satırında toplanıyordu → `transaction_type` group_by'a eklendi, yön etiketli ayrı satırlar | `app/routers/reports.py` | In-memory: "diger" → 2 satır (gelir 1000, gider 200); eskiden 1200 | ✅ |
| 17 | P0-12 RRE-002 / BUG #074 | reports upcoming_cashflow krediler için sadece 1 taksit gösteriyordu → ufuk boyunca aylık taksitler (kalan-limit + gün-clamp + yıl-geçişi) | `app/routers/reports.py` | Logic test: 180g→6 taksit, remaining=2 sınırı, gün-31 clamp, yıl-geçişi | ✅ |

## Genel smoke test (17 fix sonrası, canlı DB read-only)
`generate_cockpit` ✅ (today_target==daily_limit → ADR-026 doğru) · `generate_forecast(180g)` ✅ · `build_cockpit_snapshot` ✅ (P0-9 anahtarları mevcut) · `app.main` import ✅ (her batch). Çekirdek motorlar sağlam.

| 18 | SBK-001..007 / BUG #075 | `scripts/backup.py`: DB yolu DATABASE_URL'den (hardcoded değil, mutlak); DB yoksa `sys.exit(1)` (sessiz başarı yok); negatif `--keep-days` reddi; az önce alınan yedek asla silinmez (≥1 garanti); saniye hassasiyeti + integrity_check + sqlite hata yakalama | `scripts/backup.py` | Gerçek yedek: 380 KB alındı, integrity OK, yeni silinmedi; negatif -5 reddedildi; DATABASE_URL türevli yol | ✅ |
| 19 | SSD-001 / BUG #076 | `scripts/setup_data.py`: `drop_all` öncesi açık onay (interaktif) veya `--force`/`SETUP_DATA_FORCE=1` + hedef DATABASE_URL uyarısı — yanlışlıkla Murat'ın gerçek verisini silme koruması | `scripts/setup_data.py` | import OK (main/drop_all çalışmadı, guard aktif) | ✅ |

| 20 | TEST-018/001 / BUG #077 | `pyproject.toml` (yeni) `testpaths=["tests"]` — `pytest` artık kökteki `test_*.py` (import-time `drop_all`!) script'lerini TOPLAMAZ → canlı-veri kaybı riski kapandı. + `requirements-dev.txt` + izole emanet testi (5 test) | `pyproject.toml`, `requirements-dev.txt`, `tests/test_action_executor_emanet.py` | `pytest` emanet testi 5/5 PASSED (0.11s) | ✅ |
| 21 | TEST-005/006 / BUG #078 | `conftest.py` `db_session` artık production engine yerine **izole in-memory StaticPool** — canlı DB'ye yazma + testler arası sızıntı riski kapandı | `tests/conftest.py` | **Tüm süit: 159 passed, 1 skipped, 0 failed (3.70s)** — 17 fix hiçbir testi bozmadı | ✅ |

## Test durumu
- **159 test yeşil, 0 hata** (izole in-memory, 3.70s). Master Checkpoint emanet enforcement + P0-2 artık gerçek testle korunuyor.
- Bekleyen test işi: rules_engine/action_executor için genişletilmiş kapsam (Hypothesis property testleri), LLM eval harness (coach P0-19 için).

## Tur 3 — script veri-güvenliği + test altyapısı + P0-3

| # | ID | Değişiklik | Dosya | Doğrulama | Durum |
|---|----|-----------|-------|-----------|-------|
| 18 | SBK-001..007 / BUG #075 | backup.py: DB yolu DATABASE_URL'den, negatif keep-days reddi, az-önceki yedek silinmez, saniye+ezme-koruması, integrity_check, mutlak yol | `scripts/backup.py` | Gerçek yedek alındı (380 KB, integrity OK); negatif reddedildi | ✅ |
| 19 | SSD-001 / BUG #076 | setup_data.py drop_all öncesi interaktif onay + `--force`/env flag + DATABASE_URL uyarısı (Murat'ın gerçek verisi yanlışlıkla silinmesin) | `scripts/setup_data.py` | import-safe (main/drop_all çalışmadı) | ✅ |
| 20 | TEST-018/001 / BUG #077 | `pyproject.toml` testpaths=["tests"] → pytest kök `test_*.py` (drop_all) toplamaz + `requirements-dev.txt` + izole emanet testi (MC1 + P0-2, 5 test) | `pyproject.toml`, `requirements-dev.txt`, `tests/test_action_executor_emanet.py` | 5 emanet testi geçti | ✅ |
| 21 | TEST-005/006 / BUG #078 | conftest `db_session` production engine → izole in-memory StaticPool (canlı DB yazma + test sızıntısı riski kapandı) | `tests/conftest.py` | Tüm süit **159→161 yeşil**, 0 hata | ✅ |
| 22 | P0-3 DS-001 / BUG #079 | debt_strategy kart asgari ödemesi her ay güncel bakiyeden (azalan); eskiden başlangıç bakiyesinden sabit → payoff iyimserdi | `app/debt_strategy.py` | 2 test: azalan-min yakınsama + korunum invariant; süit 161 yeşil | ✅ |

| 23 | P0-8 SE-002 / BUG #080 | simulation add_transaction bakiyeyi koşulsuz (transfer dahil) değiştiriyordu; gerçek executor SADECE `auto_update_balance=True` iken, transfer'de hiç değiştirmiyor → birebir hizalandı | `app/simulation_engine.py` | Süit 162 yeşil | ✅ |
| 24 | P0-4 DS-003 / BUG #081 | debt_strategy: faizi belirtilmemiş krediler faizsiz simüle ediliyordu (iyimser maliyet) → `compare_strategies` sonucuna açık `warnings` eklendi | `app/debt_strategy.py` | +1 test (faizsiz kredi uyarısı); süit 162 yeşil | ✅ |

| 25 | LLM-003 / BUG #083 (DEVRİMSEL #1) | Grounding check: koç cevabındaki her TL tutarı cockpit'e izlenebilir mi denetlenir; izlenemeyen → uyarı + confidence≤0.4 + trace grounding_violation. "Rules Engine karar verir, LLM açıklar"ın doğrulama katmanı; "varsayım yasak" mandatının kod enforcement'ı | `app/grounding.py` (yeni), `app/coach.py` | +6 test; süit 168 yeşil | ✅ |
| 26 | P0-7 GR-... / BUG #084 | simulation `_project_forward` zincirleme ufuklarda sınır gününü (T+30) çift sayıyordu (`[start,end]` kapalı pencere) → yarı-açık `(start,end]`. Önce boundary testi kırmızı (kredi zincir 100k→80k vs tek 100k→85k), fix sonrası eşit | `app/simulation_engine.py`, `tests/test_simulation_boundary.py` (yeni) | +3 test; süit 171 yeşil | ✅ |
| 27 | P0-19 / BUG #085 | Coach parantezsiz düz geçmiş-zaman sahte-tamamlama ("Kaydettim.") propose_action olmadan "işlendi" izlenimi veriyordu → `_FAKE_PASTTENSE_RE` (1. tekil + edilgen) iddia cümlesini atar + netleştirme sorusu. Kullanıcının geçmişine dokunmaz | `app/coach.py`, `tests/test_coach_fake_completion.py` (yeni) | +16 test (8 catch + 6 preserve + 2); süit 187 yeşil | ✅ |

> **Durum: 21/21 P0 çözüldü + doğrulandı (10 Tem 2026, süit 187 yeşil).** + Devrimsel adım #1: grounding check (LLM-003).

## SESSION-2 (11 Tem 2026) — Gemini tam-okuma + 7-ajan denetim + vizyon gerçekleştirme

Bu turda ~50 commit, süit **162 → 287 yeşil** (+125 test). Tümü `kalite-seruveni` dalında,
`main` dokunulmadı, tek yazarlı (AI trailer'ları temizlendi — kullanıcı isteği).

**Devrimsel/altyapı:** LLM-003 grounding (#083) · LLM-005 Ollama sovereign · serializers (#092) ·
koç davranış sözleşmesi harness (deterministik eval).

**Son 2 P0 + per-file denetim (#084-#108, hepsi doğrulandı):** P0-7 sim sınır çift-sayım (#084),
P0-19 sahte-tamamlama (#085 + iter2 regresyon düzeltmesi), beklenen-gelir çift-sayım (#086),
txn-update balance corruption (#087), expense ownership (#088), debt rollover (#089), goal-rules
işaret (#090), hesap-silme 409 (#091), tzinfo sweep (#092), FallbackProvider log (#093), YENİ
CHECKPOINT koruma (#094), KURAL SIFIR gelecek/niyet (#095), sim emanet (#101), sell_investment
valuation (#102), income-on-card (#103), K2 insight dedup (#104), goal projeksiyon span (#105),
debt paid-state (#106), cashflow sınırlama notu (#107), anomali penceresi (#108).

**Vizyon gerçekleştirme (kurucu koç tamamlandı):** A1 kart son ödeme reminder (#096), A3 aylık
özet (#097), koç aylık trend (#098), son işlemler grounding-tutarlı (#099), zikzak yarınki-limit
projeksiyonu (#100), Borç Çığı koç context'i (#109), frontend Cockpit görünürlük, main.py refactor.

**Golden test:** `test_founding_scenario.py` — Murat'ın manzarası entegre doğrulandı (Gölge
Muhasebe + Zikzak + kart son ödeme + Borç Çığı). Denetim kaydı: `dosya-denetimi/ROUND2-*.md`.

## Bekleyen (onay/temizlik)
- **20 yetim `reasoning_traces` kaydı** (user id=2, var olmayan): FK açıkken app çalışır ama veri kiri. Silmek düşük riskli (ölü debug trace) — **kullanıcı onayı bekliyor** (veri silme).
- Açık backlog (büyük/mimari, aceleye gerek yok): structured output (LLM-gated, harness ile de-risk edildi — ama NL koç için muhtemelen gereksiz), Account/Goal cascade (tek-kullanıcıda düşük etki).

## Yürütme notları
- Numaralandırma: BUG #059→#109 bu iki turda. ADR-026 (önceki #025).
- Her fix `dersler-gemini.md` 7 meta-dersine ve kök vizyona hizmet ediyor.
- Deterministik kalite backlog'u TÜKENDI; sonraki faz: gerçek kullanım (Wave-2 disiplini) veya kullanıcı yönlendirmesi.

## SESSION-3 (11-12 Tem 2026) — FEAT katmanı + eval-driven + zayıf-sağlayıcı sertleştirme

Deterministik P0 backlog'u tükendiğinden bu tur **vizyon-ilhamlı FEAT katmanı** (wave3-vision;
YNAB/Copilot/Actual/Maybe'den KOPYALAMADAN ilham) + eval-driven koç ölçümü + gözlem-güdümlü
bug avı. Süit **287 → 601 yeşil** (+~314 test, 1 skip). `main` dokunulmadı, tek yazarlı.

**FEAT (kanıt-temelli, hepsi test + koç context entegrasyonu):**
| FEAT | Değişiklik | Doğrulama |
|------|-----------|-----------|
| FEAT-001/002 | Kategori bütçe zarfları (envelope) + atanmamış nakit ("Ready to Assign"); yeni Envelope tablosu (create_all-safe), CRUD router, Bütçe paneli | test_envelopes.py; koç context "BÜTÇE ZARFLARI" |
| FEAT-003 | Birikim zarfları (sinking funds) — aylık gereken katkı; GoalRead.@computed_field (şema/DB değişmez) | test_sinking_fund.py |
| FEAT-005 | Kategori bütçe aşım öngörüsü (zarf-farkında erken uyarı) | test_category_overspend.py |
| FEAT-006/007 | Abonelik tespiti + "düzenli gidere çevir" döngüsü + cockpit yük görünürlüğü | test_subscription_*.py |
| FEAT-009 | Safe-to-Spend (kart-farkında, Copilot ilhamı) | test_safe_to_spend.py |
| FEAT-010 | Nakit runway (gelirsiz kaç gün) — kredi taksitleri dahil | test_cash_runway.py |
| FEAT-012 | Borçsuz olma tarihi (Borç Çığı çıktısından) | test_debt_freedom_metric.py |
| FEAT-013 | Faiz sızıntısı sayacı (aylık/yıllık/günlük borç faizi) | test_interest_leak.py |
| FEAT-021/024 | Net değer ayrıştırması + enflasyon-düzeltilmiş reel net değer; Net Değer Analizi paneli | test_networth_attribution.py, test_real_networth.py |
| FEAT-022 | Şeffaf finansal sağlık skoru (0-100 composite, bileşenler görünür) | test_health_score.py + property |
| LLM-004 | Koç eval harness (deterministik, judge-LLM'siz) + eval_runner (izole canlı ölçüm) | test_coach_eval.py; canlı: 6/8 |

**Bug/kural (gözlem + property + canlı-eval bulguları):**
| ID | Kök-neden | Doğrulama |
|----|-----------|-----------|
| BUG #121 | Nakit krizi öngörüsü kritik alert'e çevrilmiyordu (ileriye-dönük insolvency) | test_cashflow_crunch_alert.py |
| BUG #122 | Alert tutarları nokta-ondalık ("74.99 TL") grounding'i delip Türkçe tutarsızdı → _tl() | test_tl_format_grounding.py |
| BUG #123 | Safe-to-Spend kart borcunu düşmüyordu → tehlikeli iyimserlik (gözlem-yakalandı) | test_safe_to_spend.py |
| BUG #124 | Runway kredi taksitini saymıyordu → crunch ile çelişki (gözlem-yakalandı) | test_cash_runway.py |
| BUG #125 | Alert önem sıralaması kararsızdı → kararlı stable-sort (kritikler önce) | test_metric_coherence.py |
| BUG #126 | Alert yorgunluğu → uyarılar top-3, kritikler korunur, gizli_uyari_sayisi | test_metric_coherence.py |
| BUG #127 | Zayıf sağlayıcı gerçekleşmiş eylemi düz metinle geçiştirip propose'u unutuyordu → STEP-E retry has_realized_action ile genişletildi | test_coach_behavior_contract.py (düz-metin + nötr-guard) |
| BUG #129 | recommend_next_action (FEAT-041) "saf/dışa-açık" sözleşmesi eksikti: property fuzzing 3 gerçek çökme buldu — daily_limit/faiz_sizintisi/toplam_gecikmis present-but-None → _tl(None)/None>0 TypeError; ayrıca alacak_yaslanma non-dict + eksik en_riskli guard'ı. Production'da crash yoktu (generate_cockpit hep geçerli) ama sözleşme artık arbitrer girdide tutuyor | test_next_action.py (fuzz 300+ örnek, 2000 elle doğrulandı) |
| RULE-008 | simulate_partial_sale giriş doğrulaması | test_partial_sale_validation.py |
| RULE-009 | Statü likidite oranı nakit>0 guard | test_rules |
| RULE-010/011 | payoff_date gerçek takvim ayı + enjekte today; asla-bitmeyen borçta None | test_debt_payoff_date.py |
| BE-009 | chat hatası ham detay sızdırmaz + loglanır | test_coach_chat_endpoint.py |
| — | Sağlık skoru denormal-bölen round(inf) overflow'u (property test yakaladı) | test_metric_properties.py |
| SEC/KVKK | Tüm veriyi dışa aktar (GET /api/user/export) — egemenlik/veri taşınabilirliği | test_data_export.py |

**Canlı eval bulguları (dış kısıt, kod defekti değil):** Groq/Cerebras deprecated model 404'leri
düzeltildi (gpt-oss-120b). Groq free tier 8000 TPM Türkçe prompt'u (~8400 tok) karşılamıyor →
her çağrı 413 verip fallback'e düşüyor (fonksiyonel çalışır, round-trip israfı). Detay: memory
`reference_groq_tpm_limiti`. Eval action gap'i (2/8) canlı-sağlayıcı erişilebilirliği kaynaklı;
gating deterministik doğrulandı (propose SUNULUYOR), #127 retry'ı bunu sertleştirdi.

**Golden:** test_founding_scenario + test_e2e_journey (uçtan-uca yeni yüzey entegrasyonu).

### SESSION-3 devamı (12 Tem 2026) — borç-stratejisi katmanı (Murat'ın 5-kredi + dolu-kart gerçeği)

Deterministik + FEAT/S işleri tükenince en yüksek-değer kalanlar Murat'ın borç-dominant
durumuna göre seçildi (gözlem-güdümlü, kopyalamadan ilham). Süit **620 → 632 yeşil**.

| FEAT | Değişiklik | Doğrulama |
|------|-----------|-----------|
| FEAT-015 | Kart asgari-ödeme tuzağı: sadece asgari ödeme senaryosu (kaç ay + toplam faiz). Asla-bitmez (asgari<faiz) → kritik sarmal; ≥12 ay → uyarı. `calculate_min_payment_trap` + cockpit + koç + Cockpit.jsx | test_min_payment_trap.py (12) |
| FEAT-027 | Alacak yaşlandırma (AR aging): 13 dağınık alacağı vade-yaşına göre grupla (60+/31-60/1-30 gecikmiş · vadesiz kör nokta); en_riskli önce kovala. `calculate_receivables_aging` + cockpit + koç + Cockpit.jsx | test_receivables_aging.py (7) |
| FEAT-014 | Kredi konsolidasyon simülatörü (nötr, tavsiye değil): (1) assumption-free eşik = ağırlıklı ort. oran; (2) what-if annüite (oran+vade → taksit/faiz). `calculate_consolidation_baseline`/`simulate_consolidation` + `GET /api/debt-strategy/consolidation` + DebtStrategy.jsx formu | test_consolidation.py (12) |

Borç-stratejisi yüzeyi artık kapsamlı: snowball/avalanche (mevcut) + borçsuzluk tarihi
(FEAT-012) + faiz sızıntısı (FEAT-013) + asgari tuzağı (FEAT-015) + konsolidasyon (FEAT-014);
alacak tarafı yaşlandırma (FEAT-027). `collect_debts` bu metrikler arasında paylaşımlı (tek sorgu).
Metrik-tutarlılık gözlemle doğrulandı: borç özgürlük (avalanche, rollover) vs asgari tuzağı
(izole min-only) FARKLI senaryolar → çelişki değil (#124-tarzı bug yok). Koç context ~946 token
(tüm bloklarla) — Groq TPM darboğazı sistem prompt'undan, context'ten değil.

**Ek sağlamlaştırma:** RESIL-008 circuit breaker (request-too-large veren sağlayıcı process
boyunca atlanır — Groq TPM israfı bitti) · BUG #128 annüite overflow + küçük-oran hassasiyet
guard'ları (property test yakaladı) + FEAT-014/015/027 korunum invariant testleri.

**Eval-driven doğrulama (12 Tem, fallback config):** BUG #127 retry + circuit breaker ile koç
eval'inde **action gap KAPANDI** — gerceklesmis_eylem/kart_action artık PASS (önce 2/8 düşüyordu;
hipotez "propose sunuluyor ama zayıf sağlayıcı çağırmıyor" doğrulandı, retry + çalışan Cerebras
çözdü). Kalan 2 FAIL (analiz grounding): Cerebras seyrek-veri analizinde TL uyduruyor → grounding
DOĞRU yakalıyor (grounded=-, confidence düşürülüyor) — sistem tasarım gereği çalışıyor, kod bug'ı
değil (kanonik eval DB'de borç/alacak yok → yeni context blokları devrede değil). Eval-driven
döngü işledi: hipotez → fix → eval teyidi.

### SESSION-3 dayanıklılık + doğruluk + egemenlik sweep (12 Tem 2026, süit 632→661)

Deterministik/FEAT işleri doyunca provider-gerçeği (Groq/Cerebras TPM, Gemini kota) etrafında
dayanıklılık + kullanıcıyı etkileyen doğruluk + veri-egemenliği tamlığı hedeflendi.

| ID | Değişiklik | Doğrulama |
|----|-----------|-----------|
| FEAT-016 | Kart utilization: toplam borç/limit oranı + band (saglikli/orta/yuksek/kritik) + %30 sağlıklı borç hedefi (somut çapa) + trend (en eski snapshot kart borcu ÷ güncel limit, ≥7g). Koç yalnız yuksek/kritik'te uyarır; Cockpit çubuk kartı. Murat'ın #1 problemi (maxed kart) tek metrikte | test_card_utilization.py (9) |
| FEAT-034 | Otomatik kategori: gider + kategori boşsa açıklamadan türetir (Migros→alisveris). MERCHANT_KEYWORDS + QUICK_KEYWORDS, kelime-sınırı token eşleşmesi (substring değil → "sokak" yanlış pozitifi yok). Kullanıcının açık seçimini ezmez. UI ipucu | test_auto_categorization.py (9) |
| FEAT-032 | İstek listesi / 24-saat impuls bekleme: WishlistItem + router (add/list/resolve, scheduler yok — 24h "hazır" türetilir) + koç bağlamı + export + Wishlist.jsx. Borç-batık için davranışsal impuls-lever | test_wishlist.py |
| FEAT-030 | Satın alma fırsat maliyeti: amount'ı harcamak vs en yüksek faizli borca ödemek (borçsuzluk + faiz farkı). Avalanche RAM kopyası; assumption-free. + endpoint + DebtStrategy.jsx formu | test_opportunity_cost.py (8) |
| RESIL-008 | Circuit breaker: request-too-large (413) veren sağlayıcı process boyunca atlanır (429 geçici kotadan AYRI). `_engine` singleton → Groq TPM israfı ilk çağrıdan sonra biter | test_fallback_provider.py (+4) |
| RESIL-004 | Graceful degradation: tüm sağlayıcı düşünce ham hata sızmaz + "Rules Engine LLM'siz çalışır, verilerin sağlam" mesajı + cockpit korunur | test_coach_behavior_contract.py |
| BE-025 | Fallback modda günlük-limit BLOCK koruması ölüydü (usage hep %0). provider_used loglanır + PROVIDER_DAILY_LIMITS haritası → Gemini kotası doğru izlenir | test_usage_tracking.py (5) |
| BE-010 | 6 sessiz `except: pass` → tanılanabilir loglama (goal_engine except'i #066'da AttributeError gizliyordu) | süit yeşil |
| SEC-006 | coach mesajı max_length=4000 (sağlayıcı token/maliyet koruması; büyük yapıştırma zinciri patlatmasın) | test_coach_chat_endpoint.py (+2) |
| SEC-032 | Finansal float alanları sonlu olmalı: paylaşılan schema_types (allow_inf_nan=False + üst sınır 1e12) accounts/transactions/debts/incomes/expenses'e uygulandı → inf/NaN/taşma(1e308) girişte reddedilir (round(inf) rules_engine sızıntısı kesildi). İşlem ≤0 dostça mesajı korundu | test_financial_input_validation.py (21) |
| SEC-032b | İKİNCİ savunma (propose→execute yolu): action_executor `_parse_finite` helper'ı ile para-hareketi handler'ları (update_balance/sell_investment/add_transaction/update_fund_price) nan/inf payload'ı reddeder — NaN eskiden `<=0`/`>lot` guard'larını atlayıp DB'ye yazılabiliyordu (nan bakiye/lot cockpit matematiğini bozar). Mutasyon YOK, status=failed | test_execute_pending_action.py (+3) |
| SEC-032c | ÜÇÜNCÜ boşluk: quick_text tutarı Pydantic şemasını ATLIYORDU (create'te amount None gelip _parse_quick_text'te doldurulur) → "1e308 yemek"/"Infinity market"/"nan kahve" DB'ye sızıyordu. Parser'a sonlu + üst-sınır(1e12) kontrolü eklendi (400) | test_financial_input_validation.py (+4) |
| SEC-031 (kısmi) | İşlem serbest-metin alanları (category/description/quick_text) cömert max_length aldı — DB bloat + prompt-injection payload boyutu + koç context şişmesi koruması. (Tam SEC-031 = tüm Dict[str,Any] propose payload şemaları; bu ilk dilim) | test_financial_input_validation.py (+3) |
| SEC-032d | DÖRDÜNCÜ giriş: DOĞRUDAN fund-price endpoint'i (`FundPriceUpdate.new_price` float gt=0) inf/1e308 kabul ediyordu → update_fund_price_manual'da lot*inf=inf bakiye → yatırım hesabı bozulurdu (executor yolu #032b'de korunmuştu ama doğrudan endpoint ayrı). FinansTutar'a geçirildi. Goals zaten Decimal+max_digits ile korumalıymış (doğrulandı) | test_financial_input_validation.py (+1) |
| KVKK | Veri export tamlığı: eylem/karar/hedef-izleme + koç-şeffaflık kayıtları eklendi (18 tablo). TAMLIK invariant testi | test_data_export.py (+2) |

**Provider gerçeği (memory: reference_groq_tpm_limiti):** Zengin veride koç isteği ~8000+ token →
Groq HEM Cerebras (gpt-oss, 8000 TPM) aşılır → circuit breaker ikisini de eler → koç fiilen
Gemini'de çalışır. Bu dış kısıt kod defekti değil; sistem prompt trim'i RİSKLİ (docs uyarısı) →
otonom yapılmadı. Eval DB kasıtlı minimal (davranışı provider-boyutundan izole).

**Golden tamamlandı:** test_founding_scenario Murat'ın alacaklarıyla genişletildi → yeni
borç/alacak metrikleri (asgari tuzağı/aging/konsolidasyon) + grounding kurucu manzarada kilitli.

### SESSION-3 doğruluk taraması (12 Tem 2026, süit 671→690) — RULE/DATA/LLM long-tail

FEAT + deterministik İLK ADIM (FEAT-041) sonrası, gözlem/backlog ile GERÇEK finansal-doğruluk
bug'ları avlandı (edge/cosmetic değil — birincil yolu etkileyenler öncelikli).

| ID | Bug (gerçek etki) | Doğrulama |
|----|-------------------|-----------|
| FEAT-041 | Deterministik "İLK ADIM" — sinyaller tek en-yüksek-etkili hamleye (temerrüt>kriz>tahsilat>fırsat>stabil). Öncelik LLM yargısına değil KODA → sağlayıcı-bağımsız. Fırsat likidite-güvenli (runway≥30). | test_next_action.py (10) |
| FEAT-017 | Borç ödeme ilerlemesi (momentum, Ramsey) — en eski snapshot'tan azalma. | test_debt_progress.py (7) |
| FEAT-030 spillover / BUG #129 | Fırsat maliyeti amount'ı avalanche sırasında dağıtır (fazlasını boşa atmıyor); property test yakaladı. | test_debt_metric_properties.py |
| RULE-016 | Stale next_payment_date remaining'i tüketip gelecek taksitleri gizliyordu → crunch/safe-to-spend TEHLİKELİ İYİMSER. Geçmiş-vadeli bugüne çekilir. | test_cashflow.py (+2) |
| RULE-020 | Kategori kalıpları cari pencerede üst sınır yok → GELECEK işlemler sızıp sahte anomali. `<= today` eklendi. | test_category_patterns.py |
| RULE-014 | goal_engine para Decimal(str(float)) kirli ondalık → baseline. `_money()` quantize. | goal testleri |
| RULE-015 | Cash hedef tamamlanma int() truncation → iyimser. math.ceil. | projection testleri |
| BE-010 | 6 sessiz except:pass → loglama (goal_engine except'i #066'yı gizliyordu). | süit |
| BE-025 | Fallback modda günlük-limit BLOCK koruması ölüydü + usage %0. provider_used loglanır. | test_usage_tracking.py |
| LLM-007 | usage/model_name/provider_used yalnız Groq'ta → tüm sağlayıcılarda (gerçek Gemini için trace kördü). | test_provider_metadata.py |
| LLM-001 | Anthropic modeli claude-opus-4-7→4-8 (güncel). | import |
| DATA-018 | Hesapsız "yetim" işlem (bakiye etkilemeyen) → varsayılan hesap her create'te + yoksa 400. | test_transaction_account_guard.py |
| DATA-020/028/029, SEC-006/009, RESIL-004/008, #127/#128 | Seed FK, 0-allocation, recurring ay-sınırı, mesaj sınırı, error truncate, graceful degradation, circuit breaker, annüite guard | ilgili testler |

**Ertelenen (bilinçli):** RULE-003/004/005 (kart-döngüsü edge, Murat'ın statement_day=2 → etkilenmez,
cohesive M), RULE-018/019 (simülasyon ikincil-yol, kısa ufukta minör), RULE-017 (gün sürüklenmesi
minör), RULE-013 (baseline model değişimi + migrasyon). Birincil-yol doğruluk bug'ları tükendi.

---

## Goal Charter Yürütme (Wave-2 Kapanış) — M1

| ID | Değişiklik | Dosya | Doğrulama | Durum |
|----|-----------|-------|-----------|-------|
| M1 / ADR-013 | Envelope (FEAT-001) + WishlistItem (FEAT-032) tabloları create_all ile eklenmişti, migration yoktu. Migration `fec73e5343e5` yazıldı (inspector-guard'lı, idempotent); down_revision f3dda4d3996d. Canlı DB'ye `alembic upgrade head` → iki tablo yaratıldı | `alembic/versions/fec73e5343e5_*.py`, `scripts/test_fresh_db_migration.py` | Canlı DB 20→22 tablo (envelopes+wishlist_items VAR), head fec73e5343e5. Fresh-db senaryo testi GEÇTİ. pytest 774 passed | ✅ |
| M1 / merge | `kalite-seruveni` (210 commit) → `main` `--no-ff` merge (e5a7d35) + origin'e push. 3 günlük iş GitHub'da güvende. Rollback tag `pre-kalite-seruveni-merge` push edildi | git main | origin/main=e5a7d35, pytest 774 passed | ✅ |
| M1 / bulgu | **Migration zinciri sıfırdan-şema değil** (baseline STAMP; taban create_all ile). Bomboş DB'de upgrade çöker (`NoSuchTableError: coach_insights`). ADR-013 kısmen gerçekleşmiş → M6/DATA backlog adayı: baseline'ı gerçek create_table'a çevir | `alembic/versions/fa46373f4ca8_*.py` | `scripts/test_fresh_db_migration.py` gerçek senaryoyu test eder | 📋 not |

## OTONOM KARAR M1 — Migration genesis collapse (ADR-013 tam gerçekleştirme)

| ID | Değişiklik | Doğrulama | Durum |
|----|-----------|-----------|-------|
| OTONOM KARAR M1 | **Bulgu:** temiz DB'de `alembic upgrade head` çöküyordu (baseline STAMP idi, coach_insights orphan+alter). **Kategori: (c) ADR İhlali** (ADR-013/DB-001). "Testi uyarlıyorum" reflex'i REDDEDİLDİ. **Karar (K10):** non-destructive collapse — `b70779a2f621_genesis_full_schema` tüm 21 tablo+48 index (create_all eşdeğeri) yaratan root; 9 migration no-op'a indirildi (revizyon+zincir korundu). **Gerekçe:** en sağlam+risksiz (canlı DB atası=genesis, dokunulmaz; re-stamp yok). ADR-013a yazıldı | Temiz DB `alembic upgrade head` → 21 tablo, create_all ile kolon+index ÖZDEŞ (`scripts/test_fresh_db_migration.py`). Canlı DB dokunulmadı (fec73e5343e5, 22 tablo). pytest 774 | ✅ |

## Milestone 2 — Rehber + skill + ADR + Mustafa temizlik

| ID | Değişiklik | Doğrulama | Durum |
|----|-----------|-----------|-------|
| M2.1 | PROJE.md 4 May bayat → güncel (774 baseline, kritik yollar, KURAL+OTONOM KARAR, anti-pattern, audit kod, skill) | 33→74 satır | ✅ |
| M2.2 | 4 global skill repo'ya kopyalandı + `financialos-kalite-seruveni` skill yazıldı | `.asistan/skills/` 5 skill, skill kayıtlı | ✅ |
| M2.3 | ADR-001/012/013 materyalize (repo-izli); kalan 7 ADR pending (MCP boş) | 3 ADR dosyası + pending | ✅ |
| M2.4 / ADR-001 | 8 yasaklı-isim referansı isimsiz forma çevrildi (coach_insights/premortem/mobile-roadmap/wave3-vision) | grep 0, pytest 774 | ✅ |

## Milestone 3 — Yetim trace temizliği + ADR-028

| ID | Değişiklik | Doğrulama | Durum |
|----|-----------|-----------|-------|
| M3.1 | `scripts/cleanup_orphan_traces.py`: test-kullanıcı (user_id=2 test_user_decision_rhythm) + dangling-orphan (user_id=3) temizliği. R3: memory "20 yetim user_id=2" yanlıştı (2=test 56, 3=orphan 20). Backup'lı, idempotent, tüm user-scoped tablo | 82 satır silindi (76 trace+4 insight+1 goal+1 user); sadece Murat kaldı; tekrar=0 | ✅ |
| M3.2 / ADR-028 | Koç fiilen Gemini-only gerçeği belgelendi (ADR-002 yapısı korunur). D1: OpenRouter araştırması (research-log.md) — Wave-3 fallback adayı | ADR-028 + research-log yazıldı | ✅ |

## Milestone 4 — Fiyat otomasyonu (Improvement #028 / FT-006 kapanış)

| ID | Değişiklik | Doğrulama | Durum |
|----|-----------|-----------|-------|
| M4 / #028 / FT-006 | `app/price_providers/` (router: get_fund_price pytefas-reuse + 4h TTL cache + record_investment_price PriceHistory idempotent + dispatch) + scheduler cron `fetch_investment_prices_job` (02:45) — `try_auto_fetch_*` ölü koddan CANLI'ya bağlandı | Canlı: TLY=7277.90, 2/2 hesap güncel, PriceHistory satırı | ✅ |
| M4 / ADR-029 | Sağlayıcı stratejisi belgelendi. **OTONOM KARAR (kategori-b):** charter "borsapy birincil" premisi R3 ile REDDEDİLDİ (borsapy TEFAS 404); pytefas birincil (tek çalışan) | ADR-029 + research-log K10 | ✅ |
| M4 test | `tests/test_price_providers.py` (5) + `tests/test_scheduler_price_job.py` (2) — ağsız, monkeypatch, izole in-memory | 781 passed (774→781, +7) | ✅ |
| M4 / backfill | `scripts/backfill_prices_all_accounts.py` — geçmiş PriceHistory doldurma (idempotent, iş günü) | script hazır | ✅ |

## Milestone 5 — Decimal göçü (Faz A-F, Faz G onay kapısı)

| ID | Değişiklik | Doğrulama | Durum |
|----|-----------|-----------|-------|
| M5 / ADR-030 | 20 para kolonu Float→Numeric(19,4); iç aritmetik Decimal, public sınır floatify (B1 JSON float) | 789 test (781→789); canlı KOPYA upgrade OK | ✅ Faz A-F |
| M5 OTONOM-1 (kat-c) | interest_rate/lot_count/confidence_score para DEĞİL → Float kaldı (körlemesine sweep semantik yanlış) | 3 kolon Float, 20 Numeric | ✅ |
| M5 OTONOM-2 (kat-b) | Pydantic condecimal ATLANDI — floatify sınırı B1'i daha temiz karşılıyor (condecimal Decimal'i JSON'a iterdi) | schemas float, hedef karşılandı | ✅ |
| M5 / money.py | D()/ZERO/q2/q4/floatify + `float(para)`→D() (rules 22+ae+router+ft) + json default=float (5) | drift-yok kanıtı (test_decimal_precision 8) | ✅ |
| M5 Faz G | **CANLI DB upgrade YAPILMADI** — head `fec73e5343e5`, onay bekliyor | canlı balance hala `real` | ⏸️ ONAY |
| M5 Faz H+I | **CANLI upgrade UYGULANDI** (2026-07-13, Murat onayı): backup+`alembic upgrade head` `fec73e5343e5`→`38360f856577`; canlı balance NUMERIC(19,4)/Decimal, cockpit sağlam, JSON-safe | tag `milestone-5-decimal-migration` push | ✅ M5 KAPANDI |

## Milestone 6 — Kalite Serüveni Faz 3 (P1 denetim + düzeltme, artım 1)

| ID | Değişiklik | Doğrulama | Durum |
|----|-----------|-----------|-------|
| M6 denetim | 27 P1 maddesi R3 ile denetlendi (4 paralel audit agent). **16 zaten CLOSED** (Faz 2'de kapanmış, MASTER-FIX-LIST bayat-açıktı) | faz-3-durum.md | ✅ |
| P1-7 / BUG #133 | usage sayacı `date.today()`→`datetime.utcnow().date()` — TR sunucuda Gemini kotası 3 saat erken sıfırlanıyordu | test | ✅ |
| P1-9-res / BUG #134 | GoalRuleUpdate percent (0,100] validator — create/update asimetrisi | test | ✅ |
| P1-10 / BUG #132 | cash_target `current_amount` `max(.,0)` — negatif birikmiş-tutar sızıntısı | test | ✅ |
| P1-20 / BUG #135 | PremortemScenario id deterministik S1..Sn — React key çakışması (dayanıklı yeniden-ata) | test | ✅ |
| P1-23 / BUG #131 | link_premortem_outcome try/except — executed aksiyon sonrası 500 riski | test | ✅ |
| M6 güvenlik | SQL injection taraması temiz (ORM parametreli, `_EXCLUDED_SQL` statik); secret mgmt temiz (.env gitignore+git-dışı) | grep+git | ✅ |

## Milestone 6 — artım 2 (P1-1, P1-22)

| ID | Değişiklik | Doğrulama | Durum |
|----|-----------|-----------|-------|
| P1-1 / BUG #136 | goals 6 datetime alanı `UtcDateTime` (GoalRead/AllocationRead/RuleRead) — naive→UTC suffix, JS 3h kayması önlendi | 795 test | ✅ |
| P1-22 / BUG #137 | `load_cached_premortem` — aynı cockpit_snapshot_hash → LLM'siz cache dönüşü (maliyet/gecikme tasarrufu) | test_p1_22 (hit/miss) | ✅ |

## Milestone 6 — artım 3 (P1-5, P1-13, P1-19)

| ID | Değişiklik | Doğrulama | Durum |
|----|-----------|-----------|-------|
| P1-5 / BUG #139 | explicit_red_line finansal-anchor: mutlak_red/niyet_beyani/kesin_red için finansal anahtar-kelime şartı ("asla o filmi izlemem" artık kırmızı-çizgi değil) | test_p1_5 | ✅ |
| P1-19 / BUG #138 | net_worth_delta ölü param kaldırıldı (link_premortem_outcome) — spekülatif kolon eklemeden | test güncellendi | ✅ |
| P1-13 | OTONOM KARAR (kategori-b): transfer bakiye no-op = tasarım-sınırı, bug değil (geçerli sınıflandırma; çift-hesap transfer Wave-3) | belgelendi | ✅ |

## Milestone 6 — artım 4 (P1-4)

| ID | Değişiklik | Doğrulama | Durum |
|----|-----------|-----------|-------|
| P1-4 / BUG #140 | `_sweep_insights_dormant` (DRY) — decision_rhythm (dominant değiş/dağıl) + mc_reference (top-3 dışı count>0) eski aktif insight'ları dormant'a indirir; bayat sinyal kalmaz | test_p1_4 + 54 coach_insights | ✅ |

## Milestone 6 — artım 5 (P1-8)

| ID | Değişiklik | Doğrulama | Durum |
|----|-----------|-----------|-------|
| P1-8 / BUG #141 | evaluate_rules_for_transaction transaction create'e BAĞLANDI (post-commit try/except) — GoalRule otomatik-tahsis özelliği çalışıyor (yarım özellik tamamlandı, opt-in) | test_p1_8 (2, endpoint) | ✅ |

## Milestone 6 — artım 6 (P1-27 simulation parite)

| ID | Değişiklik | Doğrulama | Durum |
|----|-----------|-----------|-------|
| P1-27 / BUG #142 (SE-007) | `_load_world` `if a.X else None`→`is not None` — gerçek 0.0 falsy→None sapması | test_p1_27 | ✅ |
| P1-27 / BUG #143 (SE-004) | mark_debt_paid zaten-ödenmiş guard (executor paritesi) | test_p1_27 | ✅ |
| P1-27 / BUG #144 (SE-005) | sell_investment eksik fiyat→red (sessiz 0 TL satış yok) | test_p1_27 | ✅ |
| P1-27 / BUG #145 (SE-008) | sell_investment satış geliri emanet hedefe yatırılamaz | test_p1_27 | ✅ |

## Milestone 6 — artım 7 (P1-15 enum migration)

| ID | Değişiklik | Doğrulama | Durum |
|----|-----------|-----------|-------|
| P1-15 / BUG #146 | OperationName values_callable + migration 978ad0f00814 (RULE_CHECK→rule_check). R3: CHECK yok, data-only. **Canlı uygulandı** (backup 2026-07-13-011907) | kopya+fresh-db+803 test | ✅ |

## Milestone 6 — artım 8 (P1-24 kart stratejisi wire)

| ID | Değişiklik | Doğrulama | Durum |
|----|-----------|-----------|-------|
| P1-24 / BUG #147-151 | evaluate_credit_card_strategy cockpit'e bağlandı. **OTONOM KARAR:** util-guard (near-full kartta "float silah" zararlı → "borç azalt" uyarır). RULE-003 (gerçek tarih) + RULE-004 (eff) fix; RULE-005 R3 ile doğru bulundu (geri alındı) | test_p1_24 + 7 card_strategy + canlı | ✅ |

## Milestone 6 — artım 9 (P1-25) — P1 TAMAMLANDI

| ID | Değişiklik | Doğrulama | Durum |
|----|-----------|-----------|-------|
| P1-25 / BUG #152 | `_to_anthropic_messages` adapter — internal tool-history → Anthropic content-block (tool_use/tool_result); AnthropicProvider._raw_chat kullanıyor | test_p1_25 | ✅ |
| **P1 ÖZET** | **27/27 kapandı** (16 pre-CLOSED + 11 M6 fix/decided); 19 BUG (#131-152) + 2 canlı migration + 25 test | 806 test yeşil | ✅ |

## Milestone 8 — Wave-3 Backlog Haritalama (2026-07-13)

| ID | Değişiklik | Doğrulama | Durum |
|----|-----------|-----------|-------|
| W3-BACKLOG | `wave-3-backlog.md` — 7 kaynak tarama, 68 madde + 4 big-package, top-35 seçim | 2 denetim ajanı + R3 | ✅ |
| OTONOM KARAR M8 | Premise düzeltmeleri (kategori-a KOZMETİK değil, R3-doğrulama): (1) #059/#060/#062 tanımları yanlış→gerçek kapalı; (2) P2-1 AÇIK (db.query 183×, session.query değil); (3) DATA-003/004 KAPALI (FK pragma ON, database.py:49); (4) create_all ADR-013 ihlali yok. Gerekçe: R3 disk>rapor — denetim section'ları bayat, koda karşı doğrulandı | grep + database.py:49 okuma | ✅ |

## Milestone 9 — Kritik Kısa Süre (Wave-3, 2026-07-13)

| ID | Değişiklik | Doğrulama | Durum |
|----|-----------|-----------|-------|
| W3-001 / BUG #156 | TR sayı parse veri bozulması: `parseTRNumber` (api.js) — locale-toleranslı (son ayraç=ondalık, tek nokta+3hane=binlik). 13 çağrı yeri (Accounts/Cashflow/Transactions/IncomeDebt/Cockpit/Wishlist/Budget/Goals/PendingActions) eski `parseFloat(x.replace(',','.'))` → parseTRNumber. "1.234,56"→1234.56 (eskiden 1.234, ~1000× hata). D1: Firefly III/Maybe locale-aware. K10: TR-first + US-tolerans | api.test.js +6 test (23 geçti), npm build ✓ | ✅ |
| W3-002 | markPaid UTC gün kayması: **R3 — markPaid/IncomeDebt zaten `todayLocalISO()` kullanıyor (düzeltilmiş).** Kalan tek `toISOString().slice(0,10)` yerel-gün bug'ı CashflowCalendar.jsx:86 (bugün-vurgusu, gece 00-03 TR'de yanlış gün) → `todayLocalISO()`. Aynı bug sınıfı temizlendi | npm build ✓, todayLocalISO api.test'te test edili | ✅ |
| W3-003 | Dinamik Tailwind renk purge: **R3 — ZATEN ÇÖZÜLMÜŞ.** tailwind.config.js:14-17 safelist pattern `(text\|bg\|ring\|border)-(brand\|positive\|negative\|warn)-(100\|400\|500\|600\|950)` + dark + `-950/30` explicit. Built dist CSS'te doğrulandı (text-negative-600, ring-warn-500, bg-brand-100, text-positive-400 üretiliyor). Agent FE-002 raporu bayattı. Kod değişikliği YOK; M9 top-15 boşluğu M14'ten bir madde ile doldurulacak | dist CSS grep ✓ | ✅ (R3-kapalı) |
| W3-005 | DebtStrategy fetch hatası: catch'te `data` null kalıyor → satır 282 "Aktif borç yok" (hata≠boş). `error` state + AlertTriangle hata ekranı + "Tekrar dene". Stale data varsa korunur (yalnız toast). D1: React error/empty/loading üç-durum ayrımı standart | npm build ✓ (RTL component testi → W3-060/M14) | ✅ |
| W3-004 | DebtStrategy slider klavye: `onMouseUp/onTouchEnd` commit ediyor ama ok-tuşu bunları tetiklemiyor → strateji güncellenmiyor. `onKeyUp={handleExtraCommit}` eklendi (a11y+UX) | npm build ✓ | ✅ |
| W3-006 | PendingActions klavye y/n/e kısayolu `acts[0]`'a uygulanıyordu ama ilk aksiyon düzenleme modundayken (butonlar disabled) bypass edip yanlış onay/red yapabiliyordu. `editingByIdRef` + guard: edit modunda kısayol yok sayılır | npm build ✓ | ✅ |
| W3-040 / SEC-003 | CORS sertleştirme: origin'ler env-driven (`CORS_ORIGINS`, dev default localhost) — prod domain'i env ile. `allow_methods`/`allow_headers` wildcard → açık liste (GET/POST/PATCH/PUT/DELETE/OPTIONS + Authorization/Content-Type/Accept). .env.example güncellendi | tests/security/test_cors.py (3) | ✅ |
| W3-030 / CO-001 | EMANET KASA halüsinasyon filtresi format-bağımsız: `_EMANET_HEADER_RE` yalnız `[5. EMANET KASA]` yakalıyordu; prompt kural 13 markdown `## 5.` istediğinden sızıyordu. Regex tüm başlık işaretçilerini (#/[/*/>) tolere eder + `_SECTION_BOUNDARY_RE` ile sonraki bölüm yenmez. ADR-001: koç prompt'a değil kod filtresine güvenir | tests/test_coach_emanet_strip.py (6) + fake_completion regresyon (18) | ✅ |
| W3-039 / RCH-002 | Checkpoint hard-delete koruma açığı: guard yalnız priority1+red_line'ı koruyordu → MC4/5/6/8 (type=rule) ?hard=true ile silinebiliyordu. `is_system` Boolean kolonu (migration 26a17fda5b32) + delete guard `is_system OR (priority1+red_line)` + setup_data is_system=True + CheckpointUpdate'te alan yok (API immutable). **OTONOM KARAR:** title-guard rename ile bypass edilir → kod-seviyesi flag (Master Checkpoint enforcement). Canlı DB veri-flag'i Murat onayı bekliyor (bekleyen-canli-db-aksiyonlari.md — R3: canlı başlıklar 'MC' önekli değil) | tests/test_checkpoint_protection.py (+3), fresh-db ✓, 817 test | ✅ (kod); canlı-veri bekliyor |
| W3-023 / DS-003 (E1) | Faizsiz kredi iyimser strateji: **R3 — backend ZATEN uyarı üretiyor** (BUG #081, `warnings` dict'te faizsiz-kredi mesajı). Gerçek açık: **frontend göstermiyordu**. DebtStrategy.jsx'e `data.warnings` banner'ı (AlertTriangle, warn renk) eklendi — iyimser months_to_freedom kararı öncesi kullanıcı görür | npm build ✓, backend warnings 65 debt testi | ✅ |
| W3-028 / AE-005 | income→kredi kartı borç artışı: **R3 — ZATEN FİX** (BUG #103, action_executor:542 `if credit_card: balance -= amount`). Audit raporu bayat. Kod değişikliği YOK | kod okuma | ✅ (R3-kapalı) |
| W3-026 / DS-002 | Borç payoff MAX_MONTHS "bitmedi" flag: **R3 — ZATEN FİX** (RULE-011, debt_strategy:248 month>=MAX_MONTHS→payoff_date=None + asla_bitmez flag). Kod değişikliği YOK | kod okuma | ✅ (R3-kapalı) |
| W3-029 / AE-006 | action_executor 0-fiyat yok sayma: **R3 — ZATEN FİX** (BUG #102, :685 `if _ap is not None else current_price` — explicit 0 saygı görür, `or` değil; +SEC-032 finite). Kod değişikliği YOK | kod okuma | ✅ (R3-kapalı) |
| W3-041 / SEC-004 | Rate limiting: **M11'e taşındı** (OTONOM KARAR — auth prod-gate/brute-force koruması ile birlikte, orta-süre; backlog notu da öyle işaret ediyor) | — | ⏭️ M11 |
| W3-058 / ONERI #030 | Commit-öncesi test kapısı: `.githooks/pre-commit` (staged'e göre pytest -x / vitest) + `scripts/install-hooks.sh` + `core.hooksPath=.githooks` aktif. BUG #061 (38 test sessiz kırıldı) tam da bu eksik olduğu için sızmıştı. D1: pre-commit framework yerine solo-dev için hooksPath (dış bağımlılık yok) | hook dry-run ✓ | ✅ |

## Milestone 10 — Production Deployment (Wave-3, 2026-07-13)

| ID | Değişiklik | Doğrulama | Durum |
|----|-----------|-----------|-------|
| ADR-035 | Deployment kararı D1(Firefly/fava/Maybe/Umami/Caddy)+K10: Docker Compose (backend uvicorn daemon + Caddy SPA/proxy/HTTPS) birincil, systemd alternatif. STUB→KARAR | ADR yazıldı | ✅ |
| M10-cron | M4 cron production: prod'da uvicorn --reload YOK → tek-process daemon → APScheduler 02:45 çalışır. Kök çözüm deployment-config | uvicorn daemon smoke: scheduler start + fetch_investment_prices job kayıtlı + /health 200 | ✅ |
| M10-artifacts | Dockerfile + Dockerfile.web + Caddyfile(HSTS/X-Frame W3-042) + docker-compose + .dockerignore + docker-entrypoint(alembic+uvicorn) + deploy/systemd(service+backup timer) + .env.example prod(DOMAIN/SECRET_KEY) + docs/deployment/README.md | compose YAML valid, fresh-db migration ✓ | ✅ |

## Milestone 11 — Auth + Multi-user (Wave-3, 2026-07-13)

| ID | Değişiklik | Doğrulama | Durum |
|----|-----------|-----------|-------|
| ADR-033 | Auth kararı D1(Firefly Sanctum/Maybe Devise/Firebase vs Supabase)+K10: kendi JWT (external değil, veri egemen/KVKK), bcrypt, access+refresh, OAuth authlib, KVKK sil/export. STUB→KARAR | ADR yazıldı | ✅ |
| M11-backend | User modeli (email/password_hash/oauth/kvkk/is_active) + RevokedToken + migration 380a9c1e7d8f (native ADD COLUMN, FK-güvenli). app/auth.py (bcrypt+PyJWT). routers/auth.py (register/login/refresh/logout/me/KVKK sil+export/rate-limit W3-041/OAuth+SMTP scaffold). get_current_user JWT+fallback (AUTH_ENABLED) | 17 auth testi, canlı migration ✓, 834 test | ✅ |
| M11-frontend | api.js token deposu + Authorization + authApi. Login.jsx (KVKK checkbox). App.jsx AuthGate + logout. /api/health auth_enabled | 6 vitest, build ✓ | ✅ |
| M11-kvkk | docs/legal/kvkk-consent-v1.md (KVKK m.4/7/11) + register açık rıza (kvkk_consent_at/version) + DELETE /api/users/me cascade + GET export | test_kvkk_delete/export | ✅ |
| W3-041 | Rate limiting (M9'dan taşındı): auth endpoint'lerde in-memory per-IP sliding window (brute-force) | test_rate_limit_login | ✅ |
| API_KEY_TALEP | OAuth Google/GitHub/Apple + SMTP Brevo (docs/api-key-talep-wave3.md) | scaffold + placeholder | ⏳ Murat |

## Milestone 12 — Multi-asset (Wave-3, 2026-07-13)

| ID | Değişiklik | Doğrulama | Durum |
|----|-----------|-----------|-------|
| ADR-031 | Multi-asset karar D1(Beancount commodities/Maybe/yfinance/EVDS)+K10: tek-tablo asset_type, kapsam stock+gold+fx (Numeric 19,4), kripto Wave-4. STUB→KARAR | ADR | ✅ |
| M12 | Account.asset_type + migration 697027467ca8 (native, investment→fund). yfinance_client + evds_client (EVDS_API_KEY, API_KEY_TALEP) + PriceSource.EVDS. fetch_for_account asset_type dispatch. **R3: yfinance Yahoo blok bu env'de → graceful None, canlı Murat'ta.** | 11 mock testi, canlı migrate (TLY→fund), 845 test | ✅ |
| M12-frontend | Accounts asset-type seçici UI | Wave-4 follow-up (backend dispatch hazır) | ⏭️ Wave-4 |

## Milestone 13 — Koç Sağlayıcı Ücretsiz (Wave-3, 2026-07-13)

| ID | Değişiklik | Doğrulama | Durum |
|----|-----------|-----------|-------|
| ADR-034 revize | 5+ ücretsiz sağlayıcı D1 + quality-per-cost matrisi. **R3: coach.py ZATEN 6 provider** (Anthropic/Gemini/Groq/Cerebras/OpenRouter/Ollama). Fallback sırası revize: Gemini→OpenRouter→Cerebras→Together→DeepInfra→Groq→Ollama | docs/architecture/adr-034-revize.md | ✅ |
| M13 | TogetherProvider + DeepInfraProvider (_OpenAICompatMixin — Cerebras deseni, P2-12 küçük DRY adımı) + _build_together/_build_deepinfra + zincir revize. API_KEY_TALEP (Together/DeepInfra) | 6 test, canlı smoke Murat'ta (key gerekli) | ✅ |

## Milestone 14 — Backlog Kalan (Wave-3, 2026-07-13)

| ID | Değişiklik | Doğrulama | Durum |
|----|-----------|-----------|-------|
| W3-009 | PendingActions `JSON.parse(payload)` guard'sızdı → bozuk payload listeyi çökertir. `safeParsePayload` (try/catch→{}), 2 çağrı yeri | npm build ✓ | ✅ |
| W3-008 | RedLines `handleToggleActive`/`handleDelete` hata yakalamıyordu (sessiz başarısızlık + unhandled rejection). try/catch→setError | npm build ✓ | ✅ |
| W3-024 | goal_engine compare_strategies AttributeError: **R3 ZATEN FİX** (BUG #066/GE-001, dict-handling) | kod okuma | ✅ (R3-kapalı) |
| W3-033 | create_allocation IDOR: **R3 ZATEN FİX** (goals.py:195 user_id==current_user.id + tx user_id filtresi + BUG #072 tutar sınırı) | kod okuma | ✅ (R3-kapalı) |
| M14-kalan | W3-007/010/011/012/013/014/018/019/020/025/031/032/035/043/046/057 → çoğu küçük guard/a11y; bir kısmı R3-zaten-fix olası. Wave-4'e devredildi (kalite düşürmeden, KURAL 12) | — | ⏭️ Wave-4 |

## Wave-3 Tamamlama (Wave-3-Tamamlama charter, 2026-07-14)

| ID | Değişiklik | Doğrulama | Durum |
|----|-----------|-----------|-------|
| M16 / BUG #157 | SECRET_KEY startup fail-fast: `app/settings.py` (`validate_security_config` — production'da boş/dev-default/<32char SECRET_KEY → RuntimeError, uygulama açılmaz; dev'de warning) + main.py lifespan çağrısı. R3: settings.py yoktu, auth.py lazy-raise ediyordu. ENVIRONMENT env. | tests/security/test_secret_key_fail_fast.py (6) | ✅ |
| M17 | Frontend OAuth: Login.jsx Google/GitHub butonları (authApi.oauthLogin → tam-sayfa /api/auth/oauth/{p}/login). `consumeOAuthRedirect()` (api.js) router'sız handler — backend /auth/oauth-success?token redirect'ini yakalar, token kaydeder, URL temizler, cockpit'e döner. App.jsx AuthGate mount'ta çağırır + oauth-error → Login initialError. **R3+K10:** frontend router'sız tab-app → query-param handler (Murat onayı, pages/ eklenmedi) | 4 vitest (30 toplam), build ✓ | ✅ |
| M18 | Password reset frontend: Login.jsx'e reset-request (email→passwordResetRequest) + reset (yeni şifre→passwordResetConfirm) modları + "Şifremi unuttum" linki. `getResetTokenFromUrl()` (api.js) Brevo linkindeki /auth/reset?token= yakalar, AuthGate Login'i reset modunda açar. Router'sız (M17 deseni) | 3 vitest (33 toplam), build ✓ | ✅ |
| M19 | EVDS fiyat endpoint: `app/routers/prices.py` GET /api/prices/currency/{code} + /gold/{type} (mevcut evds_client üzerine, graceful 502). Scheduler zaten fx/gold dispatch ediyor (M12). **R3: EVDS evds2.tcmb.gov.tr/service/evds/ artık SPA HTML dönüyor (endpoint taşınmış)** — canlı fiyat env'de erişilemez (yfinance M12 tekrarı); kod doğru-yapılı, canlı doğrulama Murat'ta (doğru endpoint/key) | 5 mock test | ✅ (kod); canlı EVDS bekliyor |
| M20 / W3-039 | Canlı DB master checkpoint is_system veri-flag'i UYGULANDI: `scripts/apply_w3_039_is_system.py` (backup + per-row ORM + güvenlik-guard beklenen-set kontrolü). 8/8 is_system=True. Wave-3'te auto-guard bare-UPDATE'i bloklamıştı → sanctioned script. Guard canlıda aktif. Backup 2026-07-14-020847.db, idempotent | canlı doğrulama 8/8 | ✅ |
| M21 | Auth rate limit production değerleri: `app/rate_limit.py` per-bucket (login 5/15dk, register 3/saat, pwreset 3/saat, oauth 10/dk) + env override (RATE_LIMIT_<BUCKET>_MAX/WINDOW). M11 global limiter genişletildi (slowapi yerine, yeni dep yok). oauth_login'e de eklendi. auth.py alias korundu (test uyumu) | tests/security/test_rate_limit.py (6) + auth regresyon | ✅ |
| M22 | CORS whitelist: **R3 — W3-040/M9'da zaten env-driven** (CORS_ORIGINS, credentials=True, açık method/header, 3 test). Enhancement: `_compute_cors_origins` CORS_ORIGINS boşsa dev-default + FRONTEND_URL birleştirir (prod tek-var kolaylığı) | 6 CORS testi | ✅ |
| M23 | Log rotation: `app/logging_config.py` (console + RotatingFileHandler logs/financialos.log 10MB×5, prod JSON/dev text, fail-safe console). main.py basicConfig → setup_logging. logs/.gitignore. Env: LOG_DIR/LOG_ROTATION_MAX_MB/BACKUP | tests/test_logging_config.py (3), 891 test | ✅ |
| M24 | MCP memory auto-sync: `.githooks/post-commit` (commit'i `.mcp-sync-pending.log`'a yakalar) + `scripts/mcp_sync_report.py` (flush okuma) + `docs/kalite-seruveni/memory-auto-sync.md`. **R3: hook MCP'ye DOĞRUDAN yazamaz (shell, MCP Claude-aracı)** → capture(otomatik)→flush(Claude) deseni. "graph bayat kalır" (Wave-3 dersi) yarı-otomatik çözüldü | post-commit ledger canlı | ✅ |
| M25 | Bug Archive kanıtlama turu: disk kanıt indeksi (commit/fixler/test/docstring) 141 bug (#1-157). 44 KANITLI (çok-kaynak) + 52 test-korumalı + 51 İDDİA (erken #1-55, docstring/commit var test yok). **Yeniden-açık: 0.** Rapor bug-archive-audit-14tem.md | R3 disk-indeks | ✅ |
| M26 | Sections kapatma turu: R3 sections prose-format, P0/P1 zaten kapalı. Spot-check kritik boyutlar. **2 otonom milestone:** M34 (SEC-015 /docs prod kapalı), M35 (FE-032/PERF-020 sourcemap kapalı). Gerçek açık P0/P1: 0. Rapor faz-4-durum.md | R3 spot-check | ✅ |
| M34 / SEC-015 | /docs+/redoc+/openapi.json production'da kapalı (is_production gate), dev'de açık. Bilgi ifşası önlendi | tests/security/test_docs_gating.py (2) | ✅ |
| M35 / FE-032,PERF-020 | vite.config sourcemap:true→false — prod build kaynak sızıntısı önlendi | dist .map yok doğrulandı | ✅ |
| M27 | Test coverage: TOTAL %86→%87, 897 test. startup.py 0→**%100** (4 test, catch_up_snapshots 4 dal). Çekirdek motorlar zaten >%80. Rapor tests/README.md (router/external düşük-kapsam hedef listesi) | coverage ölçüldü | ✅ |
| M28 | Frontend anti-pattern denetimi: 40 dosya R3. **Temiz** — doğrudan fetch 0 (api.js disiplini), parseFloat yalnız backend-değer, dinamik Tailwind safelist'li, kritik async-error M9/M14'te kapalı. Küçük (index-key 23, a11y) → Wave-4. Kritik anti-pattern: 0. Rapor frontend-audit-14tem.md | R3 tarama | ✅ |
| M29 | Improvement Backlog kapatma turu: ~188 obs, disk kanıtı (22 FEAT + Wave-3 milestone). ~15+ ONERI KAPANDI (#002/004/005/006/008/019/020/021/028/030 + FEAT dalgası). Açık kalanlar feature-fikri (#007/009/010/011/014/016/017/018/029) → Wave-4, kritik yok. Rapor improvement-backlog-audit-14tem.md | R3 disk-indeks | ✅ |
| M30 | Documentation turu: docs/contributing.md (dev kurulum + ADR kuralları + PR), docs/faq.md (KVKK/gizlilik/TR bağlam), docs/user-guide/README.md (paneller + akışlar), docs/api-reference/README.md (endpoint grupları + openapi). README.md mevcut (164 satır) | dosyalar oluşturuldu | ✅ |
| M31 | TR number locale denetimi: parseTRNumber 20 (text-input W3-001), Number() yalnız number/range-input (tarayıcı "." → doğru), parseFloat yalnız backend-değer, formatTL 19 dosya. Edge-case'ler W3-001 testli. 3 raw toLocaleString display-tutarlılık (Wave-4). Kritik TR-locale bug: 0. Rapor tr-locale-audit.md | R3 tarama | ✅ |
| M32 | Wave-4 skeleton detay: BUG #157 kapandı-işaretlendi (M16), mobil (ADR-032)/aile hesabı/kripto Numeric(28,8)/PostgreSQL+RLS/OHVPS/Sentry/design-system detaylandı. **Google OAuth "External Test" 100-limit + unverified uyarısı BUG olarak eklendi** (Wave-4 publish/doğrulama). Kalan küçük Wave-4 listesi | wave-4-skeleton.md (112 satır) | ✅ |
| M33 | Wave-3-Tamamlama kapanış: 20 milestone (18 planlı M16-M32 + 2 otonom M34/M35), 897 backend + 33 vitest, TOTAL %87. Durma kriteri KARŞILANDI: kapatılabilir kritik/orta açık iş 0. PROJE.md baseline + milestone-log kapanış tablosu | git senkron | ✅ GOAL TAMAM |

## M19-v3 EVDS Regression Fix (2026-07-14)

| ID | Değişiklik | Doğrulama | Durum |
|----|-----------|-----------|-------|
| M19-v3 / EVDS regression | EVDS v2→v3 geçişi: evds_client.py base `evds3.tcmb.gov.tr/igmevdsms-dis`, series PATH'e gömülü, header `{"key":...}` auth. fetch_series/fetch_currency_rate(buy+sell)/fetch_gold_price + get_evds_price compat. prices.py buy/sell response + date param. Scheduler otomatik v3 (get_evds_price). ADR-029 revize. **CANLI: USD 46.9121/46.9966 (14 Tem) 200 ✓.** R3: altın TP.MK.F.BILESIK.TUM bileşik-endeks (gram değil, Wave-4) | 7 endpoint testi + canlı curl 200 | ✅ |

## P1 — Çok-Kullanıcı İzolasyon Denetimi (Wave-9 publish yolu, 2026-08-04)

| ID | Değişiklik | Doğrulama | Durum |
|----|-----------|-----------|-------|
| BUG #162 | **ÇAPRAZ-KULLANICI SIZINTISI (kritik).** `app/goal_rules.evaluate_rules_for_transaction` TÜM kullanıcıların/workspace'lerin aktif `GoalRule`'larını çekip tek kullanıcının işlemine uyguluyordu → A'nın işlemi B'nin hedefine `GoalAllocation` yazıyor, B kendi allocation listesinde A'nın tutarını + `transaction_id`'sini görüyordu. Canlı yol (`transactions.py:368`, her işlem oluşturmada). Fix: kurallar `Goal` join + `scope_filter(Goal, tx.user_id, tx.workspace_id)` ile işlemin sahibine kapsanır (ADR-036: aynı workspace'teki üyenin kuralı geçerli kalır) | TDD: `tests/test_goal_rules.py::test_15/16` kırmızıydı → yeşil; `test_17` pozitif kontrol (aile üyesi kuralı çalışmaya devam ediyor) | ✅ |
| P1-gate | **Statik kapı genişletildi.** `tests/test_scope_enforcement.py` yalnız scope'suz `user_id ==` yakalıyordu; **filtresiz sorgu** (BUG #162'nin şekli) kör noktaydı. AST tabanlı yeni kapı `app/` ağacının tamamını tarar; her sahipli-model sorgusu ya kapsamlı ya `# scope-exempt: <gerekçe>` işaretli olmalı. 12 ihlal triyaj edildi: 2 gerçek sertleştirme (action_executor recurring, coach history PendingAction), 10 gerekçeli exempt | 6 test yeşil; **3 meta-test** kapının BUG #162 desenini gerçekten yakaladığını ispatlar (hep-yeşil kapı riski kapandı) | ✅ |
| P1-matris | **Runtime çapraz-kullanıcı matrisi.** `tests/test_cross_user_isolation.py`: A kaynak yaratır, B aynı id ile okur/yazar/siler → 403/404 beklenir (7 kaynak ailesi, 17 test). Liste uçlarında sızıntı yok; B, A'nın hesabına işlem yazamıyor; `X-Workspace-Id` ile workspace ele geçirme 403. **Kapsam kilidi:** yeni `/{id}` endpoint'i matrise veya gerekçeli `MATRIS_DISI`'na yazılmazsa test kırılır (kapsam kendiliğinden daralamaz) | 17 test yeşil | ✅ |
| BUG #163 | **Çok-kullanıcı doğruluk defekti.** `scripts/backfill_net_worth.run_backfill` + `app/startup.catch_up_snapshots` yalnız İLK kullanıcıyı işliyordu (tek-kullanıcı kalıntısı) → 2. kullanıcıdan itibaren net-değer geçmişi hiç dolmuyor, trend/atıf raporları sessizce eksik kalıyordu. Ayrıca yazılan satırlarda `workspace_id` NULL kalıyor, workspace kapsamlı okumalar geçmişi göremiyordu. Fix: tüm kullanıcılar döngüsü (`user_id` opsiyonel filtre) + personal workspace bağlama | TDD: `tests/test_backfill_multiuser.py` 3 test kırmızıydı → yeşil | ✅ |
| BUG #164 | **Yıkıcı script footgun'ı (kritik).** `scripts/cleanup_orphan_traces.py` "gerçek kullanıcı = adı 'test' ile başlamayan" sezgisiyle kalan HERKESİ siliyordu (FK'lar PRAGMA ile kapalı). Kapalı betada adı "Test..." olan gerçek kullanıcının tüm finansal verisi + hesabı geri dönüşsüz silinirdi. Fix: isim sezgisi KALDIRILDI; `--keep-user-ids` + `--delete-user-ids` zorunlu, keep id'leri DB'de doğrulanır, kesişim reddedilir, production'da `--force-production` şartı | `tests/test_cleanup_script_guards.py` 6 test (5 kilit + 1 pozitif kontrol) yeşil | ✅ |
| BUG #165 | **Workspace kapsam tutarsızlığı.** `app/cashflow.generate_forecast` `workspace_scope` bloğu içinden çağrılmasına rağmen ham `user_id` filtreliyordu → paylaşımlı (aile) workspace görünümünde nakit krizi / güvenli-harcama rakamları kişisel veriden hesaplanıyor, workspace'in kendi kalemleri hiç sayılmıyordu. Fix: 5 sorgu `rules_engine._scope` köprüsüne geçti (kapsam yoksa legacy user_id korunur) | TDD: `tests/test_cashflow_workspace_scope.py` 3 kırmızı → yeşil; mevcut 26 cashflow testi regresyonsuz | ✅ |

## P5 / P8 — Dayanıklılık + eşzamanlılık (publish yolu, 2026-08-05)

| ID | Değişiklik | Doğrulama | Durum |
|----|-----------|-----------|-------|
| BUG #211 | **H16 — döviz kesintisinde ürün susuyordu.** `fx_live.get_live_fx` üç hata yolunda da `None` dönüyordu: 31 dk önce başarıyla çekilmiş kur elde dururken koç "çekemedim, bankana bak" diyordu. Ücretsiz sağlayıcının birkaç saatlik kesintisi rutin → o süre boyunca kur özelliği tümüyle ölü. Diğer uç (bayat değeri sessizce "şu anki kur" diye sunmak) daha kötü olurdu. Fix: son BAŞARILI değer `bayat=True` + `yas_dakika` ile döner, `_BAYAT_TAVAN_DK=12s` üstü hiç dönmez; `app/coach._maybe_market_block` bayat dalında "SON BİLİNEN KUR (BAYAT: X saat Y dakika önce)" der ve "ŞU ANKİ GÜNCEL KUR" ifadesini KULLANMAZ. Fon/hisse tarafı zaten `fund_tracker.is_stale` → Cockpit "N eski" ile işaretliydi (doğrulandı) | `tests/test_fx_bayat.py` 8 test: taze≠bayat, kesintide yaş doğru, bozuk yanıt da düşer, 12s üstü None, cache kirlenmiyor, koç dili iki yönde kilitli | ✅ |
| BUG #212 | **H17 — eşzamanlı koç kullanımı iki defekt.** (a) *Kota yarışı:* akış "sayacı OKU → LLM çağır (saniyeler) → sayacı YAZ" idi; sayaç ancak çağrı bitince arttığı için aynı anda gelen N istek aynı eski sayıyı okuyup hepsi geçiyordu — hakkı 1 kalan kullanıcı paralel istekle tavanı deliyordu (BUG #188'in maliyet tavanı fiilen iptal, açık betada fatura riski). Fix: **rezervasyon deseni** (`_kota_rezerve_et`) — satır çağrı ÖNCESİ yazılır, id sırası tavanı aşarsa satır silinip 429 döner; `_rezervasyonu_tamamla` sonucu aynı satıra işler (reddedilen istek sayacı kirletmez). (b) *Muhasebe etiketi:* `engine.provider_name`, `FallbackProvider`'ın **paylaşılan** `last_used_provider` alanını okuyordu → ilk başarılı çağrıdan sonra etiket `fallback(gemini)` oluyor, bu ad `PROVIDER_DAILY_LIMITS`'te YOK → günlük sağlayıcı kotası koruması sessizce ölüyordu; ayrıca etiket eşzamanlı BAŞKA kullanıcının çağrısına göre değişiyordu. Fix: `_muhasebe_saglayici_adi` etiketi yapılandırmadan türetir | `tests/test_coach_eszamanlilik.py` 5 test (3 gerçek paralel thread). **Eski kodla ölçüldü:** `[200, 200, 200]` + tavan 3 iken 5 satır → yeni kodda `[200, 429, 429]` + 3 satır | ✅ |
| BUG #213 | **H22 — güvenlik sınırı tek katmanda yaşıyordu.** İstek gövdesi sınırı YALNIZ `deploy/nginx.conf.template` içindeki `client_max_body_size 1m` idi. Üç boşluk: (1) uygulamaya ters vekil ATLANARAK erişilirse (docker ağı, nginx'siz kurulum, yerel çalıştırma) koruma yok, (2) nginx yapılandırması sessizce değişirse bunu yakalayan test YOKTU, (3) chunked transfer-encoding'de `Content-Length` hiç gelmez — boyut ancak akarken sayılarak bilinir. Fix: `app/request_limits.GovdeBoyutuMiddleware` (saf ASGI — BaseHTTPMiddleware gövdeyi tamponlar) `Content-Length` yolunda gövdeyi HİÇ okumadan 413 döner, başlık yoksa akan baytları sayıp sınırda keser; `MAX_REQUEST_BODY_BYTES` geçersiz/0/negatifse SESSİZCE sınırsıza kaçmaz, 1 MiB'e düşer; 413 `beklenen reddetme` olarak işlenir (hata-izleme tablosuna 500 olarak düşmez) | `tests/test_govde_limiti.py` 14 test. **Koruma kaldırılarak ölçüldü:** chunked testi + "uygulamada kurulu" testi + "413 hata izlemeye düşmez" testi (413 yerine 422) kırmızıya döndü. Ayrıca nginx şablonu drift kilidi + `scripts/live_gate.py` canlı kapısı (2 MiB → 413) | ✅ |
| BUG #214 | **H23 — betanın kullanılıp kullanılmadığı ÖLÇÜLEMİYORDU.** Operatörün elindeki tek araç `beta_triage` idi ve o yalnız **şikâyet edeni** gösterir. Beta'nın en olası başarısızlığı gürültülü çöküş değil SESSİZ TERK'tir: davet edilir, kayıt olur, ilk ekranda takılır, kimse şikâyet etmez — panelde her şey yeşil görünür, ürün ölüdür. P8'in çıkış ölçütü ("gerçek kullanıcı davranışıyla sınanmış operasyon") bu yüzden ölçülemezdi. Fix: `scripts/beta_metrics.py` — onboarding hunisi (kayıt→hesap→işlem→koç→kural), hiç iz bırakmayan sayısı, tutunma (kayıt gününden BAŞKA bir gün dönen), koç hata oranı/süre, açık geri bildirim + hata grubu. Aktiflik üç sinyalin BİRLEŞİMİ ile gün bazında sayılır (tek kişinin yoğun günü 'çok kullanıcı' gibi görünmez). Şema değişikliği gerekmedi (`last_login` sütunu eklenmedi — giriş yapıp hiçbir şey yapmamak zaten kullanım değil) | `tests/test_beta_metrics.py` 10 test. **Gizlilik kod seviyesinde kilitli:** çıktıda e-posta, isim, serbest metin ve para tutarı bulunması testle YASAK; JSON ağacının her yaprağı sayı olmak zorunda. Dış analitik servisi yok — veri makineden çıkmaz (BUG #195 ile aynı çizgi) | ✅ |

## P3.2 — Boş-durum + hata-durumu arayüz kapısı (publish yolu, 2026-08-05)

| ID | Değişiklik | Doğrulama | Durum |
|----|-----------|-----------|-------|
| BUG #217 | **KAPSAM SESSİZCE ÇÖKMÜŞ — iki kapı fiilen ÖLÜYDÜ.** Hem `test_sifirdan_kullanici_e2e` ("parametresiz her GET ucu boş-durumda çağrılır") hem `test_cross_user_isolation::test_id_alan_endpointler_matriste_kapsanir` ("her `/{id}` ucu izolasyon matrisinde olmalı") kapsamı `app.routes` üzerinden türetiyordu. **FastAPI 0.141** `include_router`'ı düzleştirmeyi bıraktı (`_IncludedRouter` sarmalayıcısı, `path`/`routes` boş) → 87 yoldan **1'i** (`/api/health`) taranıyordu; izolasyon kapısında ise liste **boş** kaldığı için kapı hiçbir şey ölçmüyordu. Her iki test de YEŞİL'di. Fix: envanter `tests/endpoint_envanteri.py` içinde **OpenAPI**'den türetiliyor (FastAPI'nin kararlı kamu sözleşmesi) + **kapsam TABANI** assert ediliyor (parametresiz GET ≥ 25, parametreli ≥ 25) → aynı çöküş bir daha sessiz olamaz | Kapı açılınca: boş-durum taraması 1 → **36 uç**; izolasyon kapısı ilk kez gerçek bulgu verdi (`/api/legal/{slug}` matris dışıydı — gerekçeli istisnaya eklendi). Backend süiti 1606 → **1608 passed** | ✅ |
| BUG #218 | **TEK BİR HATALI İSTEK → SONSUZ İSTEK DÖNGÜSÜ (canlıyı etkiler).** `ToastProvider` context değerini (`api` nesnesi) HER RENDER'da yeniden yaratıyordu. Zincir: istek patlar → `toast.error()` → `setToasts` → provider re-render → `toast` KİMLİĞİ değişir → `useCallback(..., [toast])` yenilenir → ona bağlı `useEffect` TEKRAR koşar → istek yine patlar → … Tetikleyici bol: bayat workspace seçimi (403 — BUG #162 senaryosu), backend kapalı, oturum düşmüş, uç 404. **Ölçüldü:** Aile paneli 150 ms'de **54 istek**, durmuyor (kullanıcı sekmeyi açık bıraktıkça backend'e kesintisiz yağmur; kendi rate-limit'ine takılır, mobilde pil/veri yakar). Fix: context değeri `useMemo([show, dismiss])` ile sabitlendi (ikisi de zaten kararlı `useCallback`) | `frontend/src/error-state.test.jsx` — 13 panelin tamamı hata yolunda taranır. **Eski kodla ölçüldü:** 54 istek → düzeltmeden sonra **2**. Ayrı bir kök-neden kilidi toast kimliğinin render'lar arasında sabit kaldığını sınar (yeni panel aynı tuzağa düşemez) | ✅ |
| BUG #219 | **Bütçe paneli backend hata verince ÇÖKÜYORDU.** `load()` hatayı yalnız toast'a düşürüyor, `data` `null` kalıyordu; render `data.envelopes.length` okuyunca panel çöküyordu (ErrorBoundary devreye girer → "Bu panel yüklenemedi"). Aynı dosyada `data?.durum` / `data?.atanmamis_nakit` guard'lıydı — tutarsızlık bug'ın kendisiydi. Ayrıca toast 4 sn sonra kaybolur, geriye SEBEPSİZ kırık ekran kalırdı. Fix: hata artık state'te tutulur, ekranda **kalıcı** hata kartı + "Tekrar dene" gösterilir (yükleme başarısızken kullanıcıya "zarfın yok" DENMEZ — veriyi bilmiyoruz), liste `data?.envelopes || []` üzerinden okunur | Panel sweep testi. **Mutasyon kontrolü:** fix `git checkout` ile geri alındı → `Bütçe paneli hata yolunda çökmez` KIRMIZI, geri konunca yeşil. Ayrıca tespit mekanizmasının kendisi ayrı bir sahte-panelle doğrulandı | ✅ |
| BUG #220 | **Zamana bağlı gizli flaky test.** `test_farkli_saat_dilimi_farkli_gun_verebilir` UTC+14 (Kiritimati) ile UTC-11 (Midway) arasındaki takvim farkını `in (0, 1)` sanıyordu. İki bölge **25 saat** ayrı: UTC günü 10:00-10:59 penceresindeyken fark **2 gün** olur → test günün ~1/24'ünde kırmızı. Üretim kodu (`user_today`) doğru, iddia yanlıştı; M90 "flaky yok" ölçümü bu pencereye denk gelmemişti. Fix: sınır `in (0, 1, 2)` + gerekçe yorumu | Tam süit 13:25 TSİ'de (10:25 UTC) kırmızıydı → yeşil; 1608 passed | ✅ |
| P3.2-fixture | **Frontend boş-durum testi TAHMİN mock'la değil, GERÇEK sözleşmeyle koşuyor.** `tests/test_bos_durum_frontend_fixture.py` gerçek kayıt akışıyla boş kullanıcı yaratıp 36 ucun gövdesini `frontend/src/__fixtures__/bos-kullanici.json`'a döker; vitest tarafı bunu servis eder. **Sözleşme kayması kapısı:** diskteki fixture'ın YAPISI (anahtar+tip iskeleti; oynak değerler değil) canlı cevapla karşılaştırılır — backend alan ekler/siler/tipini değiştirirse test kırılır, frontend testi sessizce bayatlayamaz. Kullanıcının kendi personal workspace'i de dökülür (yoksa Aile paneli testi boş-durumu değil HATA yolunu ölçerdi — yanıltıcı yeşil) | `empty-state.test.jsx` 27 test (13 panel × çökme + ham-JS-artığı sızıntısı) + fixture kapsam kilidi. Frontend süiti 71 → **125 passed** | ✅ |

## P1 — Doğrulama denetiminden çıkan kritik defekt (2026-08-05)

| ID | Değişiklik | Doğrulama | Durum |
|----|-----------|-----------|-------|
| BUG #221 | **KRİTİK — koç-onaylı kayıt kullanıcının KENDİ listesinden kayboluyordu.** `execute_pending_action` handler'ları `(db, user_id, payload)` imzasıyla çağırıyordu; **workspace bağlamı hiç geçmiyordu** → `Transaction` ve `MasterCheckpoint` satırları `workspace_id=NULL` yazılıyordu. Okuma tarafı workspace kapsamlı (`scope_filter`) ve production'da personal workspace ZORUNLU (`workspace_deps.active_workspace_id` prod'da fail-fast) olduğu için NULL satır kullanıcının kendi listesinden/raporundan/koç bağlamından eleniyordu. Kullanıcı açısından: koça "500 TL market harcadım" → onayla → **bakiye düşüyor ama işlem hiçbir yerde görünmüyor** (para buharlaşmış gibi; kategori bütçesi, reel bütçe, abonelik tespiti, nakit-akış tahmini hepsi eksik hesaplıyor). Ürünün amiral akışı bu. **3. kol:** `app/premortem.py` `DecisionJournal` de `workspace_id` yazmıyordu — `decision_journal` RLS listesinde (`alembic/versions/f5a6b7c8d9e0`), yani prod PostgreSQL'de satır INSERT edilir (policy USING-only, WITH CHECK yok) ama sonra GÖRÜNMEZ. Fix: handler çağrısı `workspace_scope(pending.workspace_id)` içine alındı (yazma + handler içindeki `rules_engine` okumaları aynı kapsamda) + `_yazma_workspace_id()` çözümleyicisi (aktif kapsam → kaynağın/hesabın workspace'i → kullanıcının personal workspace'i → None=legacy, davranış değişmez) | `tests/test_action_executor_workspace.py` 5 test (bakiye düştü→işlem görünür, ws taşınır, kırmızı çizgi panelde, workspace'siz eski aksiyon hesabın ws'ine düşer, legacy kurulumda davranış değişmez). **Kaynak:** doğrulama denetimi D01 (kritik) + D02; canlı DB'de ZATEN gerçekleşmişti (`transactions` tek satır ws=NULL, tüm hesaplar ws=1) | ✅ |
| #221-kapı | **Statik kapı: workspace'li modele `workspace_id` yazmadan kayıt açılamaz.** Mevcut kapı (`test_scope_enforcement`) yalnız SORGULARI denetliyordu (denetim bulgusu D31) — oysa #221'in şekli YAZMA idi. Yeni kapı `app/` ağacındaki her `Model(...)` çağrısını AST ile tarar; model listesi `models.py`'den türetilir (elle liste yok), muafiyet `# ws-exempt: <gerekçe>` ile gerekçelenir. L11 gereği kapsam tabanı assert edilir | `tests/test_workspace_insert_kapisi.py` 3 test. **Mutasyon kontrolü:** `workspace_id` satırı silininde kapı kırmızıya döndü. Kapı açılınca aynı sınıftan **3. bir yer** bulundu (premortem `DecisionJournal`) ve o da kapatıldı | ✅ |
| BUG #222 | **CANLI ŞEMA KODUN 9 MIGRATION GERİSİNDEYDİ — uygulama sessizce yarım çalışıyordu.** `data/financialos.db` `a1b2c3d4e5f6`'da kalmıştı, kod `e1f2a3b4c5d6` bekliyordu. Eksikler: `users.token_version` (#172), `rate_limit_hits` (#182), **`master_checkpoints.rule_type/rule_params` (#192)**, `demo_data_markers` (#194), `error_logs` (#195), `users.timezone/currency/locale` (#197), `beta_invites` (#199), `users.email_verified_at` (#202), `scheduler_runs` (#203). Etki: `enforce_user_rules` her aksiyon onayında `rule_type`'ı sorguluyor → **koç yolundan yapılan HER onay 500 veriyordu**; rate-limit/hata-izleme/davet/e-posta-doğrulama/cron-görünürlüğü de canlıda fiilen yoktu. Uygulama yine de açılıyor ve `/api/health` yeşil dönüyordu. Kök boşluk: ADR-013 "şema yalnız Alembic" doğru kararıydı ama "migration'ı çalıştırmayı unutma" adımını hiçbir kapı denetlemiyordu (L8: belgelenen ≠ uygulanan). Fix: `app/schema_guard.validate_schema_version` startup'ta (lifespan) sürümü doğrular — production'da **fail-fast**, dev'de gürültülü uyarı, `alembic_version` tablosu yoksa (test/`create_all` yolu) sessiz geçer (L6: geliştirmeyi kilitleme) | `tests/test_schema_guard.py` 5 test. Sözleşme **dönüş değeriyle** sınanır (`guncel`/`atlandi`/`uyumsuz`) — ilk sürüm log'a dayanıyordu ve tam süitte `basicConfig(force=True)` handler'ları değiştirince KÖR kalıyordu; kapının kendi kör noktası kapatıldı (L3). Prod dalı gerçek geride-kalmış sürümle (`a1b2c3d4e5f6`) sınanıyor | ✅ |
| Canlı iş | **Canlı DB onarıldı + şema head'e yükseltildi (Murat onayıyla, 5 Ağu).** Sıra: yedek (`2026-08-05-141912.db`) → `repair_null_workspace --uygula` (2 satır: 4 Ağu 2310 TL 'sigara' işlemi + 1 net-değer anlık görüntüsü) → yedek (`2026-08-05-142714.db`) → `alembic upgrade head` (9 migration) → doğrulama: **hiçbir satır kaybolmadı, bakiyeler değişmedi**, `rule_type` kolonu geldi. Ardından kullanıcının bildirdiği 5 Ağu 300 TL yemek harcaması uygulamanın KENDİ akışıyla (propose → execute) Enpara Nakit'e yazıldı — bu aynı zamanda #221 düzeltmesinin canlı doğrulamasıdır: iki işlem de workspace kapsamlı okumada görünüyor, bakiye 2.263,52 → 1.963,52 TL | Salt-okur doğrulama: `scope_filter(Transaction, 1, 1)` → 2 işlem görünür (öncesinde 0) | ✅ |
| #221-onarım | **Canlı veri onarım aracı** — `scripts/repair_null_workspace.py`. Kod düzeltildi ama ZATEN oluşmuş NULL satırlar duruyor. Araç varsayılan **salt-rapor**; sezgi YOK (her satır yalnız kendi `user_id`'sinin **tek** personal workspace'ine bağlanır, belirsizse dokunulmaz), dolu `workspace_id`'ye asla dokunmaz, silme yok, yazma öncesi yedek alır (BUG #164 footgun dersi) | Rapor modu koşuldu: **2 satır onarılabilir** (4 Ağu tarihli 2310 TL 'sigara' işlemi + 1 net-değer anlık görüntüsü), 0 satır atlanır. **Uygulama İNSAN-KAPISI — Murat onayı bekliyor** | ⏸️ |

| BUG #223 | **`/api/cashflow/forecast` + `/api/debt-strategy/*` workspace bağlamını hiç kurmuyordu (denetim D03).** BUG #165 motor katmanını düzeltmişti (`app/cashflow.py`, `app/debt_strategy.py` → `_scope` köprüsü), ama HTTP uçları `workspace_scope(ws_id)` bloğuna hiç girmiyordu; contextvar boş kaldığı için köprü HER ZAMAN legacy `user_id` dalına düşüyordu. Aile workspace'i seçiliyken **aynı ekranda çelişen rakamlar**: cockpit "0 TL borç / 50.000 nakit" derken debt-strategy KİŞİSEL iki borcu listeliyor, cashflow açılışı 60.000 (kişisel+aile toplanmış) diyordu. Daha ağırı: snowball/avalanche, konsolidasyon ve fırsat-maliyeti simülasyonları **yanlış borç kümesi** üzerinde koşuyordu (paylaşımlı workspace'te eş kullanıcı gerçek ortak borcu hiç görmüyor, kendi kişisel kartını görüyordu) → hatalı borç-kapatma kararı. İkinci kol: bu uçlar **üyelik doğrulaması da yapmıyordu** (üye olunmayan `X-Workspace-Id` ile 200; cockpit doğru şekilde 403). Fix: 4 uca `ws_id: Optional[int] = Depends(active_workspace_id)` + gövde `with workspace_scope(ws_id):` (cockpit ile aynı desen; üyelik doğrulaması dependency'den bedava gelir). **L11 dersi:** motorda kanıtlanmış kapsam, uçta bağlanmamışsa YOKTUR — mevcut `tests/test_cashflow_workspace_scope.py` `generate_forecast`'i doğrudan `with workspace_scope(...)` içinde çağırdığı için ucun kör olduğunu göremiyordu | `tests/test_cashflow_debt_endpoint_workspace_scope.py` **12 test** (HTTP seviyesinde: aile bağlamı, başlıksız=kişisel, gelir sızıntısı, 4 uçta üye-olmayan→403, konsolidasyon toplamı, fırsat-maliyeti hedefi, ikinci kullanıcı ortak borcu görür). Düzeltme öncesi **12'si de kırmızıydı** (denetim kanıtı birebir üretildi) | ✅ |

| BUG #224 | **Premortem + simülasyon uçları da workspace bağlamı kurmuyordu (D03b — #223'ün SINIF taramasında bulundu).** `POST /api/premortem/{id}` → `build_cockpit_snapshot` → `generate_cockpit` ve `POST /api/simulate/{id}` → `simulate_action` → `_load_world`. Aile workspace'i seçiliyken bir aksiyonun **ön-ölüm risk analizi ve 3-ufuklu etki simülasyonu KİŞİSEL manzara üzerinde** koşuyordu → kullanıcı ekrandaki aile rakamlarıyla çelişen bir analiz okuyup ona göre karar veriyordu; premortem'de bu bağlam ayrıca LLM'e gidiyordu. **Motor katmanı ayrı bir sorun:** `simulation_engine` hiç workspace-farkında değildi (3 sorgu da ham `Model.user_id == user_id`) çünkü tasarım gereği `rules_engine`'i import etmiyor (bağımsızlık ilkesi) ve köprü orada tanımlıydı → köprüye erişemiyordu. Fix: köprü (`contextvar` + `workspace_scope` + `scope_expr`) **yaprak modül `app/scope.py`'ye taşındı**; `rules_engine` geriye-uyum re-export'u yapar (mevcut ~20 import yeri değişmedi, tek contextvar), `simulation_engine` katman ihlali olmadan aynı köprüyü kullanır. İki uca `Depends(active_workspace_id)` + `with workspace_scope(ws_id):` eklendi (üyelik doğrulaması dependency'den geldi) | `tests/test_premortem_simulation_workspace_scope.py` **7 test** (sim aile bağlamı baseline+T+0, başlıksız=kişisel & aile hesabı kişisel bağlamda BULUNAMAZ, 403 × 2, premortem'e giden snapshot'ın nakdi, motor köprüsü + legacy regresyonu). Düzeltme öncesi 7'si de kırmızıydı. **Mutasyon kontrolü:** `scope_expr` ham `user_id`'ye çevrilince 3 test kırmızıya döndü | ✅ |

| BUG #225 | **Şifre sıfırlama bağlantısı, kullanıcı şifresini değiştirdikten SONRA hâlâ geçerliydi — hesap geri alınamıyordu (denetim D04, BUG #172 ailesinin açık kolu).** Posta kutusuna geçici erişen biri (paylaşılan bilgisayar, iletilmiş sıfırlama postası, ele geçirilmiş e-posta) bağlantıyı alıp BEKLETEBİLİYORDU. Kullanıcının doğru refleksi olan "hemen şifremi değiştireyim" saldırganın elindeki bağlantıyı ÖLDÜRMÜYORDU: saldırgan 30 dk içinde şifreyi tekrar değiştirip hesabı kalıcı ele geçiriyor, sahibini dışarıda bırakıyordu (denetimin PoC'sinde kurban 401, saldırgan 200). Hesabın içinde tüm bakiyeler, borçlar, işlem geçmişi, KVKK export'u ve `DELETE /api/users/me` var. Uygulama üstüne "Güvenlik için tüm oturumlar kapatıldı" diyerek yanlış güvenlik hissi veriyordu. **Çift kök neden:** (1) `password_reset_confirm` `token_version_ok(...)` çağırmıyordu; (2) `create_password_reset_token` `token_version`'ı hiç geçirmiyordu → payload `tv` daima 0, yani (1) tek başına eklense bile kapı SESSİZCE etkisiz kalırdı. İkinci senaryo aynı kökten: arka arkaya iki "şifremi unuttum" → iki bağlantı da 30 dk aynı anda canlı, birinin kullanılması diğerini öldürmüyordu. Fix: token `tv` taşır + confirm doğrular; sayacı artıran her olay bekleyen bağlantıları öldürür | `tests/auth/test_pwreset_token_gecerliligi.py` **5 test**: denetimin PoC'si (kurban içeride/saldırgan dışarıda ispatlı), iki-canlı-bağlantı senaryosu, **meşru akış bozulmadı** (L6), tek-kullanımlık regresyonu (#172), `tv` claim'i gerçekten taşınıyor (bu olmadan diğer kapılar kör kalır — L3). Düzeltme öncesi 3'ü kırmızıydı. **Sınıf taraması (L11):** `email_verify` / `oauth_exchange` / `email_change` token'ları da bakıldı — hesap-ele-geçirme sınıfında değiller | ✅ |

| BUG #226 | **OAuth callback kapalı-beta davet kapısını TAMAMEN atlıyordu (denetim D05).** `invite_required()` yalnız `POST /api/auth/register` içinde çağrılıyordu; OAuth yapılandırılmış bir canlıda alan adını bilen herkes Google/GitHub ile tek tıkla hesap açabiliyordu — **aynı e-posta `/register`'da 403 alırken**. Denetimin PoC'si zincirin sonuna kadar gitti: kullanıcı yaratılıyor, `oauth/exchange` 200, `/auth/me` 200 (tam kullanılabilir oturum). BUG #199'un tüm gerekçesi "kapalı beta bir iddia değil KONTROL olsun" idi; bu yol o kontrolü fail-open bırakıyordu. Etki: davetsiz izlenemeyen kullanıcılar KVKK'da veri-sorumlusu yükümlülüğü doğurur ve envanterde görünmez; her yeni kullanıcı paylaşılan LLM sağlayıcı kotasını tüketir (gerçek davetliler koçu kullanamaz); `BetaInvite` listesi gerçekle uyuşmaz; `/api/meta` kimliksiz olarak `davet_kodu_gerekli: true` beyan ederken ürün o kontrolü uygulamıyordu (L8). Mevcut süit bu fail-open davranışı **kilitliyordu** (`test_callback_yeni_kullanici_olusturur`) — kapı yokluğu testle onaylanmış görünüyordu. Fix: OAuth'ta kod girilecek alan olmadığı için kapı **e-posta eşleşmeli davet** ile kuruldu (davet ilk girişte tüketilir, kurallar tek kaynaktan `davet_dogrula`'dan gelir); e-postasız davet OAuth'ta fail-closed; operatör aracı + runbook uyarısı eklendi | `tests/auth/test_oauth_davet_kapisi.py` **11 test** (PoC, `/register` ile aynı davranış, meşru davetli + davet tüketimi izlenebilir, büyük/küçük harf, mevcut kullanıcı girişi bozulmadı, `open` modda davranış aynı, tükenmiş/süresi geçmiş/başka adres/e-postasız davet, meta beyanının kodda karşılığı). Düzeltme öncesi 8'i kırmızıydı; mevcut `tests/auth/test_oauth.py` (10 test) yeşil kaldı | ✅ |

| BUG #227 | **Belgelenen bir dağıtım yolu KİMLİKSİZ canlı sunucu üretiyordu (denetim D06).** `docs/deployment/README.md` iki yolu da resmî belgeliyor (ADR-035: systemd "alternatif") ve her ikisi de `cp .env.example .env` diyordu; `.env.example` ise `ENVIRONMENT=development` + `AUTH_ENABLED=` (BOŞ). Yol 1'i (Docker) compose'un `${ENVIRONMENT:-production}`/`${AUTH_ENABLED:-true}` varsayılanları kurtarıyordu — **Yol 2'de (systemd) hiçbir koruma yoktu.** Denetimin çalıştırdığı kanıt: `validate_security_config()` istisna atmıyor, `GET /api/cockpit` 200, `GET /api/users/me/export` 200 (tam KVKK export'u), `DELETE /api/users/me` 204 — hepsi Authorization header OLMADAN. BUG #171 tam bu senaryo için açılmış ve "kapatıldı" denmişti; koruma `is_production()`e bağlı olduğu için bu yolda hiç devreye girmiyordu. **Kök neden doküman değil, güvenlik varsayılanının fail-OPEN olması.** Fix (katmanlı): (1) `auth_enabled()` **fail-closed** — tanımsız/boş/anlamsız değer AÇIK sayılır, kapatmak için açıkça `AUTH_ENABLED=false`; (2) `auth_problems()` prod fail-fast'i ikinci savunma hattı olarak kalır (açıkça kapatma prod'da yasak); (3) `deploy/financialos.service` kendini `ENVIRONMENT=production` ilan eder — EnvironmentFile'dan SONRA, eskimiş `.env` ezemesin; (4) README her iki yolda `.env.prod.example` gösterir + minimum listeye `ENVIRONMENT`/`AUTH_ENABLED`/`SUPPORT_EMAIL` eklendi; (5) `.env.example` yeni varsayılanı yazar; (6) ADR-033 §5'teki "varsayılan kapalı" kararı DEĞİŞTİRİLDİ notuyla güncellendi | `tests/security/test_kimliksiz_deploy_kapisi.py` **20 test**: varsayılan/boş/anlamsız değer → açık, `0/false/no/off` → kapalı (L6: yerel kurulum kilitlenmez), prod'da açıkça kapatma fail-fast, **README'nin `cp <şablon> .env` dediği HER şablon gerçekten kimlikli sunucu üretir** (kapsam tabanı assert'li — L11), systemd unit'i production ilan eder ve sırası doğru. Düzeltme öncesi 9'u kırmızıydı. **Tam süit 1677 passed** — varsayılan çevrilmesine rağmen kırmızı yok; test altyapısı artık gerçek bir yerel kurulumla aynı hareketi yapıyor (açıkça `AUTH_ENABLED=false`) | ✅ |

| BUG #228 | **LLM kotası tek uca cıvatalanmıştı; diğer yollar tavanı sıfırlıyordu (denetim D07 + D16).** ADR-041/BUG #188'in tüm amacı "bir kullanıcı paylaşılan API anahtarını/faturayı tek başına tüketemesin" idi; dayatma yalnız `POST /api/coach/chat` içindeydi. Denetim kanıtı: kota DOLU (coach 429) iken premortem ucu **5/5 istekte 200** döndü, 5 LLM üretimi yapıldı, `ApiCallLog` **hiç artmadı**; tek bir aksiyon için 6 kez üretim (BUG #137 cache'i cockpit hash'ine bağlı, kullanıcı kotasız bir uçla bakiyeyi değiştirince hash değişiyor). D16 aynı sınıf: `POST /api/actions/{id}/approve` arka planda kotasız + kayıtsız Groq çağrısı yapıyordu. Zarar: ücretsiz kademede paylaşılan anahtar tükenir → TÜM beta kullanıcılarının koçu "cevap veremedi"ye düşer; ücretli kademede doğrudan fatura. Üstelik trafik `ApiCallLog`'a düşmediği için `beta_metrics` maliyet/hata ölçümleri onu HİÇ görmüyordu — operatör patlamayı ölçse bile nedenini bulamazdı. Fix: kota muhasebesi router'dan **sökülüp `app/llm_quota.py`'ye alındı** (rezerve/tamamla/iptal + tavan + günlük sayı); `routers/coach.py` geriye-uyum sarmalayıcısı yapar. Premortem ucu **önbellek dalından SONRA** rezerve eder (cache LLM harcamaz → kullanıcı yapılmamış çağrı için cezalanmaz), çöken çağrı sayılır. Aksiyon yansıması arka plan görevi olduğu için 429 yerine **atlar** (kullanıcıya hata gösterilemez; doğru davranış işi atlamak, sessizce kotasız çağırmak değil) ve beklenmedik hatada rezervasyon iptal edilir | `tests/test_llm_kota_kapisi.py` **9 test**: premortem sayaca yazar / kota dolu → 429 / tavan kapalıyken çalışır (L6) / önbellek kota harcamaz, yansıma kota doluyken LLM çağırmaz + koşarsa sayaca yazar, **ve statik kapı**: `provider.chat`/`build_provider` çağıran her `app/` dosyası ya `llm_quota`'dan geçer ya `# kota-exempt: <gerekçe>` taşır (kapsam tabanı + bayat-muafiyet kontrolü ile). **Mutasyon kontrolü:** rezervasyonlar kaldırılınca 4 test kırmızıya döndü. Muafiyetler gerekçeli: `coach.py`/`premortem.py` (motor — kota router'da), `coach_insights.py` (cron, kullanıcı döngüye sokamaz; **açık iş:** cron çağrılarının ayrı maliyet muhasebesi, D24 ailesi) | ✅ |

| BUG #229 | **Fiyat cron'u `Account.balance`'ı güncellemiyordu — kullanıcı aynı hesap için İKİ FARKLI TL görüyordu (denetim D08).** Cockpit 36.000, Hesaplar paneli 30.000; üstelik aynı kartta "6 lot × 6.000 TL" yazıyor. Hangisinin doğru olduğunu anlamanın yolu yok. Finansal üründe çelişen bakiye = yanlış rakama göre satış/harcama kararı + güvenin bir defada bitmesi. Fark her fiyat hareketiyle büyüyor, kullanıcı manuel fiyat girmedikçe Hesaplar paneli KALICI donmuş kalıyordu. Kök neden bir DEĞİŞMEZ İHLALİ: yatırım hesabında `balance == lot_count × current_price`; bu değişmezi diğer TÜM yazma yolları koruyor (fund_tracker manuel fiyat, accounts create/update, `action_executor` — oradaki BUG #102 yorumu değişmezi açıkça adlandırıyor, simulation_engine), tek ihlal eden gece koşan cron'du. Okuma tarafı ayrışık olduğu için sapma görünür oluyordu: cockpit `lot × fiyat` hesaplar, `/api/accounts` ham `balance` döner. Fix: cron da değişmezi korur; `lot_count` None ise bakiyeye DOKUNULMAZ (hesaplanamaz — 0'a düşürmek veri kaybı olurdu), lot=0 ise bakiye 0 olur (kapanmış pozisyon para göstermez), yatırım-dışı hesaplara dokunulmaz | `tests/test_fiyat_cron_bakiye_senkron.py` **6 test** (değişmez; **iki panelin aynı hesapta aynı TL'yi göstermesi** — kullanıcı-görünür sözleşme; lot bilinmiyor / lot=0 uç durumları; PriceHistory + zaman damgası regresyonu; yatırım-dışı hesap). Düzeltme öncesi 3'ü kırmızıydı. Ayrıca sapmayı yeşile gömen `test_stock_price_isyatirim_m_hisse.py` (yalnız cockpit'i doğruluyordu) bakiye assert'iyle güçlendirildi | ✅ |

| BUG #230 | **Production yığını belgelenen komutla AYAĞA KALKMIYORDU + otomatik yedeği YOKTU (denetim D11+D12+D13/D09).** **(D11)** `docker-compose.prod.yml`'de hiçbir serviste `env_file:` yoktu; runbook'un tek deploy komutu `--env-file .env.prod` kullanıyor ama o bayrak YALNIZ `${...}` interpolasyonunu besler, konteyner ortamına değişken YAZMAZ. Sonuç: `.env.prod`'a doğru yazılmış `SUPPORT_EMAIL` (BUG #210 gereği prod'da zorunlu) uygulamaya hiç ulaşmıyor → backend startup'ta fail-fast, `deploy.sh` healthcheck'i geçemiyor, otomatik rollback **aynı derecede bozuk** önceki sürüme dönüyordu; operatörün elinde "neden açılmıyor" cevabı yoktu. Operatör onu elle eklese bile `SMTP_*`/`OAUTH_*` sessizce yapılandırılmamış kalıyordu (davet + şifre sıfırlama ÖLÜ). **(D12)** `scheduler` servisinde `AUTH_ENABLED` yoktu → aynı lifespan, aynı fail-fast, crash-loop; 02:45 fiyat ve 03:00 gece batch'i hiç koşmuyor, kullanıcı BAYAT fiyatlarla hesaplanmış net değere göre para kararı veriyordu. Üstelik `deploy.sh` yalnız "UYARI" basıp çıkış 0 ile "TAMAM" diyordu (sessiz arıza) ve `restarting` durumu `Up\|running` grep'iyle eşleşmediği için crash-loop hiç fark edilmiyordu. **(D13/D09)** Prod yığınında otomatik yedek YOKTU; beta kullanıcılarının tüm finansal geçmişi tek volume + tek Free-Tier VM'de. Depodaki tek yedek otomasyonu SQLite-only `scripts/backup.py`'yi çağırdığı için Postgres'te her gece çıkış kodu 1 ile sessizce ölüyordu. Fix: `env_file` (backend+scheduler), scheduler `AUTH_ENABLED`, `deploy.sh` scheduler ölüyse **rollback** + crash-loop tespiti, compose'a `backup` servisi + `deploy/pg_backup.sh` (dump → **doğrula**: asgari boyut + `gzip -t`; geçmeyen dump SİLİNİR → geçici dosyadan adlandırma → `pg-backups` adlandırılmış hacmi → `KEEP_DAYS` rotasyonu), yanıltıcı systemd unit'inin başlığı düzeltildi, runbook'a listeleme/elle koşma/**dışarı kopyalama** uyarısı, `.env.prod.example`'a `BACKUP_KEEP_DAYS`/`BACKUP_INTERVAL` | `tests/test_prod_compose_sozlesmesi.py` **11 test**: kapsam tabanı (L11), `env_file` iki serviste, **`.env.prod.example`'ın tarif ettiği anahtar gerçekten konteynere ulaşıyor mu** (L8), scheduler `AUTH_ENABLED` + backend↔scheduler kritik ortam paritesi, yedek servisi var / `pg_dump` alıyor / **kalıcı hacme** yazıyor, yedek script'i rotasyon + `set -e` uyguluyor, bayat SQLite unit'i kendini açıkça etiketliyor. Düzeltme öncesi 9'u kırmızıydı | ✅ |

| BUG #231 | **Yayınlanan KVKK/veri-işleyen beyanı gerçek veri akışıyla uyuşmuyordu (denetim D10).** Beyan (rıza kapısında sunuluyor): *"Gönderilmeyenler: … ham işlem listesi"*. Gerçek: koç bağlamında `## SON İŞLEMLER` bloğu literal olarak gidiyor — tarih, tutar, kategori ve **kullanıcının serbest metin açıklaması** ("Psikiyatri kontrol"), ayrıca **hesap adları** ve alacak/borç kaydındaki **üçüncü kişilerin adları**. İşlem açıklaması pratikte ÖZEL NİTELİKLİ veri taşır (sağlık/inanç/sendika); KVKK m.6 ve m.9 ayrı ve bilgilendirilmiş açık rıza ister — yanlış kapsamla alınan rıza sakattır (idari para cezası + tazminat riski). Üstelik üçüncü kişi uygulamanın kullanıcısı bile değil. Fix: **(1)** envanter gerçeğe uyduruldu (tam gönderilen liste + özel nitelikli veri uyarısı + üçüncü kişi uyarısı + gerçekten gönderilmeyenler); **(2)** kapsam maddi değiştiği için rıza sürümü **v3**; **(3)** `/api/legal/kvkk` dosyayı `KVKK_CONSENT_VERSION`'dan türetiyor — sabit "v2" yazılıydı, yani sürüm yükselse bile kullanıcı ESKİ metni okuyacaktı (onayladığı sürüm ≠ okuduğu metin); **(4)** "sürümü yükseltip susmak" L8 tuzağı olacağı için **rıza tazeleme yolu** (`GET/POST /api/users/me/kvkk-consent`) + Koç panelinde bant; **(5)** aktarım kapsamı **aktarımın yapıldığı yerde** (mesaj kutusu altında) yazıyor — kullanıcı rıza metnini kayıtta bir kez okur, mesajı her gün burada yazar | `tests/test_kvkk_beyan_gercek_akis.py` **14 test**: işaretli kullanıcıyla GERÇEK koç bağlamı üretilir, her veri sınıfının bağlamda olduğu (kapsam tabanı, L11) ve **beyanda yazılı olduğu** doğrulanır → bağlama yeni alan eklenip beyan güncellenmezse kapı kırılır (L9: kod ile doküman arasına test); ters yön (şifre hash'i/e-posta gerçekten gitmiyor); yanlış cümlenin geri gelmemesi; rıza metni ↔ envanter tutarlılığı; sunulan metin = onaylanan sürüm; eski sürümlü kullanıcı yeniden onay ister. Düzeltme öncesi 7'si kırmızıydı. Frontend: `npm run build` + 125 vitest yeşil | ✅ |

| BUG #232 | **"Pasifleştir" basılan kural HİÇBİR sekmede görünmüyordu — kullanıcı bildirimi (denetim değil, gerçek kullanım).** Murat bir kuralın geçerliliği kalmadığını düşünüp pasifleştirdi; kural Aktif sekmesinden kayboldu, **Pasif sekmesinde de Hepsi sekmesinde de yoktu**. Kullanıcının gördüğü şey "buton çalışmıyor / kural yok oldu" — oysa DB'de `is_active=False` doğru yazılmıştı. Kök neden istemci-sunucu sözleşme uyuşmazlığı: `GET /api/checkpoints` backend'de **`active_only=True` DEFAULT'ludur**, `RedLines.jsx` ise listeyi **parametresiz** çekiyordu (`checkpointsApi.list()`), yani pasif kayıt istemciye HİÇ ulaşmıyordu. Panelin Aktif/Pasif/Hepsi filtresi ve "N aktif, N pasif" sayacı **istemci-taraflı** olduğu için üçü de asla veri göremeyen bir liste üzerinde çalışıyordu (sayaç kalıcı "0 pasif"). Aynı kökten ikinci zarar: `DELETE` soft-delete'tir ve router docstring'i bunu açıkça *"kullanıcı 'sildim' dedikten 6 ay sonra 'aslında doğru karardı' diyebilir, tarih kalır"* diye gerekçelendirir — ama silinen kural da aynı şekilde görünmez olduğu için **tarihçe iddiası kullanıcı için fiilen yalandı**; kuralı geri açmanın UI'da hiçbir yolu yoktu. Fix: panel listeyi `active_only=false` ile çeker (filtreleme zaten istemcide) | `frontend/src/redlines-pasif.test.jsx` **4 test** (Pasif sekmesi pasifi listeler, Hepsi ikisini de gösterir, sayaç pasifi sayar, pasifleştirme sonrası yenileme çağrısı da pasifleri ister) — düzeltme öncesi 4'ü de kırmızıydı. Ayrıca `tests/test_checkpoint_pasif_gorunurluk.py` **4 test** backend sözleşmesini kilitler (`?active_only=false` ham query-string'de gerçekten False'a ayrışır / default değişmedi / pasifleştirme geri alınabilir / soft-delete tarihçede görünür) — frontend testleri mock'lu olduğundan bu kapı olmadan backend default'u değişince bug sessizce geri gelirdi (L9). **Sınıf taraması (L11):** veri gizleyen `Query(True)` default'u tüm `app/routers/` içinde YALNIZ bu uçta; `incomes`/`expenses` default'u `False` (hepsi döner), diğer `is_active == True` filtreleri özet/analiz uçlarında ve doğru | ✅ |

| BUG #233 | **Google ile açılmış hesabın şifre almasının HİÇBİR yolu yoktu — kullanıcı bildirimi (denetim dışı).** Murat: *"Google ile giriş yaptığım için Hesap bölümünde e-posta doğru görünüyor ama şifre kısmı 'eski şifreni gir, yeni şifre belirle' mantığında — benim eski şifrem yok."* Tek kök (*"her hesabın şifresi vardır"* varsayımı), **üç kapalı kapı**: (1) Hesap paneli `has_password`'ü bilmediği için "Mevcut şifren" alanını `required` çiziyordu → kullanıcının gönderemeyeceği form; (2) `POST /auth/change-password` haklı olarak 400 dönüyordu ama **alternatif sunmuyordu**; (3) `POST /auth/password-reset-request` şifresi olmayan kullanıcıyı `if not user or not user.password_hash` ile **sessizce eliyordu** — uygulama "e-posta kayıtlıysa bağlantı gönderildi" DİYOR ama göndermiyordu, kullanıcı gelmeyecek postayı bekliyordu. Sonuç: hesap TEK bir dış sağlayıcıya çivilenmişti; Google erişimi kaybolursa (hesap kapanması, OAuth istemcisinin iptali/yanlış yapılandırılması) **tüm finansal veri kalıcı olarak erişilemez** hale geliyordu ve uygulamanın kendi kurtarma yolu bu hesap sınıfında ölüydü. Fix: **(a)** `POST /auth/set-password` — yalnız şifresi OLMAYAN hesap için, mevcut şifre sormaz (doğrulanacak şifre yok), politika + rate-limit uygulanır, `token_version` artar, çağırana taze token verilir; **şifresi olan hesapta 400** (aksi halde `change-password`'ün mevcut-şifre doğrulaması bu uçtan atlatılırdı — asıl risk buydu); **(b)** `password-reset-request` şifresizlere de bağlantı üretir (yanıt gövdesi AYNI kaldı → enumerasyon açılmadı) ve e-posta metni "sıfırlama" değil "belirleme" olur; **(c)** `User.has_password` türetilmiş bayrağı `/auth/me`'ye eklendi (hash sızmaz; `oauth_provider` bu soruya cevap DEĞİL — şifreli hesap sonradan Google'la girince o alan dolar); **(d)** panel `has_password === false` ise "Şifre belirle" bölümü çizer, e-posta formundaki şifre alanını da gizler; bayrak EKSİKSE klasik forma düşer. **Güvenlik gerekçesi:** `DELETE /api/users/me` ve `GET /me/export` zaten yalnız oturum ister — oturum bu uygulamada tam-yetki kimlik kanıtıdır | `tests/auth/test_oauth_sifre_belirleme.py` **11 test** (ana senaryo + Google'sız giriş, çağıran atılmaz, diğer oturumlar düşer, **şifresi olan hesapta reddedilir** + eski şifre hâlâ geçerli, kimliksiz 401, politika 422, belirledikten sonra akış klasikleşir, şifresizlere sıfırlama bağlantısı üretilir + uçtan uca giriş, **kayıtsız adresle ayırt edilemez yanıt**, `has_password` iki hesap sınıfında doğru, hash sızmaz) — düzeltme öncesi 9'u kırmızıydı. `frontend/src/hesap-sifre-belirle.test.jsx` **6 test** (çıkmaz sokak yok, uç çağrılır, sonrasında klasik forma geçer, e-posta formu da şifre istemez, şifreli hesap regresyonu, bayrak eksikse klasik form). **Sınıf taraması (L11):** "şifre var" varsayımı frontend'de yalnız Hesap panelinde iki yerdeydi (ikisi de düzeltildi); `login`/`change-email` backend'de zaten `None`-güvenli. **Kapı çaldı:** `has_password` eklenmesi boş-durum fixture sözleşme kapısını kırdı (bilinçli değişiklik → fixture yeniden üretildi) ve `test_password_reset_email` sahte imzasını kırdı (güncellendi + `ilk_kez is False` assert'i eklendi) | ✅ |

| BUG #234 | **LLM kota sayacı neyi saydığını yanlış biliyordu — ilan edilen maliyet tavanı gerçeğin 2-3 katına izin veriyordu (denetim D14+D15).** İki defekt, tek kök. **(D14)** `UsageInfo` sözleşmesi *"Yukarıdakiler sağlayıcının PAYLAŞILAN günlük kotasıdır"* diyordu ama sorgu `ApiCallLog.user_id == user_id` ile filtreliydi: B kullanıcısı 2000 çağrı yapmışken A'nın gördüğü sayaç 0'dı. Kişisel tavan (80) her zaman ÖNCE dolduğu için %80 uyarısı ve %100 blok dalı **matematiksel olarak erişilemezdi** — koruma da, onu gösteren arayüz bandı da ölü koddu; paylaşılan ücretsiz kademe (1500/gün) hiçbir uyarı olmadan tükenir, tüm beta kullanıcılarının koçu aynı anda "cevap veremedi"ye düşerdi. **(D15)** Bir `/api/coach/chat` isteği iki-geçiş mimarisi (plan + ana cevap), retry dalları ve fallback zinciri yüzünden 1-4 GERÇEK sağlayıcı isteği üretiyor, muhasebeye TEK satır yazılıyordu → ADR-041'in "80 çağrı ≈ 40 mesaj/gün" sözleşmesi diskte yanlıştı (80 satır = 80 mesaj ≈ 160+ istek) ve paylaşılan sayaç aynı oranda az gösterdiği için operatörün eşiği 2 kat geç ateşlerdi; ücretli kademede fatura öngörüsü doğrudan yanlış. Fix: **(a)** sayım noktası **ağa çıkan isteğe** taşındı — `LLMProvider.__init_subclass__` her somut sağlayıcının `_raw_chat`'ini otomatik sarmalar (yeni sağlayıcı eklenince kanca UNUTULAMAZ, L14 fail-closed; zincir sağlayıcısı `_raw_chat` tanımlamadığı için çift saymaz), ölçüm `llm_quota.cagri_olcumu()` ContextVar kapsamında toplanır (eşzamanlı isteklerin ölçümleri karışmaz), istek bitince `ek_cagrilari_uzlastir` farkı **isteği fiilen yiyen sağlayıcı adına** yazar; **(b)** paylaşılan sayaç kullanıcı filtresiz (`paylasilan_cagri_sayisi`); **(c)** `block` alanının anlamı "istek REDDEDİLECEK" olarak netleşti — yedekli zincirde tavan dolsa bile ürün kilitlenmez (L6: `FallbackProvider` sıradakine geçer), yalnız `warn` ateşler; **(d)** `GET /api/coach/usage` kararlı etiket kullanıyor (eskiden `provider_name` "Fallback(Gemini)" dönüp `PROVIDER_DAILY_LIMITS`'te bulunamıyor, rozet sessizce %0'a düşüyordu — BUG #212 ile aynı sınıf); **(e)** panel bandı sağlayıcı adını ifşa etmiyor ve paylaşılan sayıyı kişisel kullanım gibi sunmuyor. **Sınıf taraması (L11):** aynı tek-satır defekti premortem ucunda ve aksiyon yansımasında da vardı (yansıma İKİ modeli sırayla dener — ikisi de gerçek istek); üçü de aynı uzlaştırmadan geçirildi | `tests/test_llm_kota_muhasebesi.py` **18 test**: paylaşılan sayaç havuzu ölçüyor / %100 dalı erişilebilir / kişisel sayaç kullanıcı-başına KALIYOR (regresyon) / alternatifsiz modda 429 + sağlayıcıya istek gitmiyor / yedekli zincirde kilitlenme yok ama warn var / bir mesaj gerçek istek sayısı kadar satır yazıyor / tavan gerçek çağrıyla doluyor / premortem + yansıma yolları da sayıyor (sınıf taraması) / usage ucu arayüzü yanlış kilitlemiyor / zincir etiketinde rozet ölmüyor / **statik kapı:** her somut sağlayıcı sayım kancasından geçer (kapsam tabanı ≥6, L11), zincir çift saymaz, sarmalama imzayı bozmaz. Düzeltme öncesi 10'u kırmızıydı; **mutasyon kontrolü** 3 mutantın üçünü de yakaladı (uzlaştırma no-op → 5 kırmızı, sayaç yeniden kullanıcı-filtreli → 4 kırmızı, sayım kancası kaldırıldı → 6 kırmızı) | ✅ |

| BUG #235 | **Bir test dosyası üretim `app` nesnesine KALICI çöken uç ekliyordu — commit kapısı fiilen işlevsizleşmişti (denetim D21).** `tests/test_error_tracking.py` modül yüklenirken `app.include_router(_test_router)` çağırıyor ve hiç kaldırmıyordu; `app.main.app` süreç-genelinde TEK nesne olduğu için `/api/_test_hata/patla` (bilerek `RuntimeError` atan uç) süitin geri kalanında da duruyordu. BUG #217 kapsam kilitleri OpenAPI'ye taşınıp **görmeye başlar başlamaz** bu sahte ucu ÜRÜN ucu sandılar → süit kalıcı 2 kırmızı. `.githooks/pre-commit` süiti `-x` ile, CI de tam koştuğu için **hiçbir Python commit'i kapıdan geçemez** hale gelmişti; pratik sonucu geliştiricinin `--no-verify` alışkanlığı edinmesi ve test kapısının tümden ölmesidir (BUG #061 dersinin aynen tekrarı). O tur envanter tarafına `/api/_test` eleme filtresi konarak kapatılmıştı — doğru ama **tüketicide** bir çözüm: kaynağı düzeltmeyince OpenAPI/rota okuyan YENİ bir tarama aynı tuzağa yeniden düşer. Fix: **(a)** router artık fixture ömürlü (`onceki = list(app.router.routes)` → `yield` → geri yükle, şema önbelleği sıfırlanır) ve `include_in_schema=False` (kamu sözleşmesine hiç girmez, savunma derinliği); **(b)** envanterdeki eleme filtresi **KALDIRILDI** — orada dursaydı yeni bir enjeksiyon yine sessizce gizlenirdi (fail-closed, L14) | `tests/test_global_app_kirliligi.py` **5 test**: kapsam tabanı (≥100 test modülü tarandı, L11), hiçbir test modülü modül seviyesinde `app.include_router`/`add_middleware`/`mount`/`add_exception_handler`/`dependency_overrides[...] =` yapmıyor (AST; fonksiyon/fixture gövdeleri hariç tutulur), ürün OpenAPI şemasında `/api/_test` yolu yok, **meta-test**: tarayıcı D21'in orijinal desenini sentetik dosyada yakalıyor + fixture içi geri-alınan değişikliği yanlış-pozitif olarak işaretlemiyor. **Mutasyon kontrolü:** eski kalıp geri konduğunda 3 kapı kırmızı — biri denetimin raporladığı hatanın birebir aynısı (`test_sifirdan_kullanici_e2e` → `RuntimeError: beklenmedik cokme`) | ✅ |

| BUG #236 | **Kurucunun ve adı geçen ÜÇÜNCÜ BİR KİŞİNİN gerçek finansal verisi production imajına giriyordu (denetim D18).** `Dockerfile:33` tüm `scripts/` dizinini kopyalıyor, `.dockerignore` `scripts` için hiçbir istisna taşımıyordu. İmaja giren `scripts/setup_data.py` gerçek verilerdir: banka markaları, gerçek kredi tutarları, üçüncü bir kişinin **13 aylık borç takvimi** ve aile içi para düzenlemeleri. O kişi uygulamanın kullanıcısı değildir ve verisinin registry'ye push edilen / destek amacıyla paylaşılan bir imajda taşınmasına rıza vermemiştir. İkinci zarar: aynı dosya `drop_all` yapar ve guard'ı `SETUP_DATA_FORCE=1` ile atlanır → prod konteynerinde **tek komut** tüm beta kullanıcılarının verisini siler, yerine bu kişisel veriyi kurar (telafi yalnız yedek). Mevcut kişiye-özel-iz kapısı (BUG #166) bu boşluğu göremiyordu: kapsamı `app/` + `frontend/src`, imaja giren `scripts/` hiç taranmıyordu. Fix: `.dockerignore`'a gerekçeli eleme listesi — (a) kişisel/üçüncü-kişi verisi + yıkıcı: `setup_data.py`; (b) geliştirici/tarihsel araçlar: `eval_runner.py`, `apply_w3_039_is_system.py`, `mcp_sync_report.py`, `install-hooks.sh`; (c) test/prova araçları: `test_fresh_db_migration.py`, `pg_gate_run.py`, `prod_rehearsal.py`. **Sınıf taraması (L11)** denetimin görmediği 4 dosya buldu: ikisi (`eval_runner`, `apply_w3_039_is_system`) kişiye özel iz taşıyordu → imajdan çıkarıldı; ikisi (`live_gate`, `cleanup_orphan_traces`) operasyonda GEREKLİ olduğu için izleri jenerikleştirildi (kişi adı → "operatör" / "gerçek kullanıcı") | `tests/test_imaj_kisisel_veri.py` **6 test**: kapsam tabanı (≥60 dosya, L11) + **simülasyon meta-testi** (`.dockerignore` gerçekten eliyor mu — `tests/` elenmiş, `app/` taranıyor; yoksa kapı "her şey temiz" derdi), imajda kişiye özel iz yok, imajda `drop_all`/`DROP DATABASE` yok, **ters yön:** çalışma-zamanı zorunluları (`scripts/backfill_net_worth.py` — `app/startup.py` import ediyor, `docs/legal/*`) ve runbook'un konteyner içinde çalıştırdığı operasyon araçları (`backup`, `restore`, `beta_invite`, `beta_metrics`, `live_gate`) elenmemiş (L6: kapı ürünü kıramaz). Kapsam **imajın gerçek içeriğine** bağlı: yeni bir `COPY` eklenip kişisel veri sızarsa kapı kırılır. Düzeltme öncesi 2'si kırmızıydı; mutasyon kontrolü (eleme satırı geri alındı) ikisini de yakaladı | ✅ |

| BUG #237 | **Koçun kaydettiği işlem KALICI olarak yanlış güne yazılıyordu — "bugün" sunucunun günüydü, kullanıcının değil (denetim D17).** ADR-042 *"tarih üreten TÜM kullanıcı-bağlamlı yollar `user_today` kullanır"* diyordu; disk bunu çürüttü — yalnız 7 router benimsemişti. **Kök sebep yapısaldı, atlanmış çağrı değil:** `execute_pending_action` handler'ları `handler(db, user_id, payload)` ile çağırıyordu, User nesnesi hiç geçmiyordu → hiçbir handler `user_today`'e **ulaşamıyordu**; aynı boşluk motor katmanında da vardı (cashflow/simulation/goal_engine/debt_strategy yalnız `user_id` alır). Üstelik `V3_GOD_MODE_PROMPT` LLM'e *"payload'a `transaction_date` EKLEME"* dediği için **en sık kullanılan yol** bilerek bu dala giriyordu: farklı saat dilimindeki kullanıcı "bugün 300 TL market harcadım" dediğinde işlem yanlış güne yazılıyor, geri alması için elle düzeltmesi gerekiyordu. Ay sınırında zarar büyüyor: gider bir önceki/sonraki aya düşüp aylık özeti, kategori bütçesini ve günlük harcama limitini yanlış hesaplıyor — **kullanıcı yanlış sayıya bakarak para kararı veriyor**. Aynı sınıf: `mark_debt_paid` ödeme tarihi, recurring dedup anahtarı (ay sınırında çift-tetik kapısı), rapor pencereleri (kategori dağılımı / net değer trendi / yaklaşan nakit akışı / aylık özet parametresiz çağrısı), net-değer atıfı + reel net değer, abonelik tespiti, cockpit'in snapshot damgası (**aynı istekte** cockpit kullanıcının gününü, snapshot sunucununkini kullanıyordu → trend grafiği kullanıcının gördüğü günle hizasız), koç bağlamı, premortem snapshot'ı, nakit-akış projeksiyonunun ilk günü, simülasyon başlangıcı, hedef tamamlanma tahmini, borç payoff tarihi ve startup catch-up backfill aralığı. Fix: `app/user_prefs.user_today_by_id(db, user_id)` — imza değiştirmeden yapısal boşluğu kapatır (kullanıcı yoksa sunucu gününe düşer, geriye uyum) + 20 çağrı yerinin dönüştürülmesi. **Bu bir "kabul edilmiş risk" değildi — kapatıldığı İDDİA EDİLEN bir doğruluk hatasıydı**, yani kimse bir daha bakmayacaktı; ADR-042'ye düzeltme notu yazıldı ve iddia artık belgeye değil teste bağlı | `tests/test_saat_dilimi_kullanici_gunu.py` **32 test** (UTC+14 Pacific/Kiritimati ve UTC-11 Pacific/Midway uçlarında parametrik — aralarında 25 saat var, yani **her an** en az biri sunucunun gününden farklı; bunu ayrı bir premis testi ispatlıyor): koçun işlemi + borç ödeme tarihi kullanıcının gününe yazılır, açık verilen tarih EZİLMEZ, TZ tanımsızsa davranış değişmez (geriye uyum), rapor pencereleri sınır satırıyla ölçülür (tam sınırdaki kayıt içeride, bir gün ötesi dışarıda), snapshot damgası + koç bağlamı + premortem + projeksiyon + simülasyon kullanıcının gününden. `tests/test_saat_dilimi_kapisi.py` **17 test — statik sınıf kapısı (L11):** `app/` altındaki her `date.today()` çağrısı (AST; yorum/docstring metni sayılmaz) ya kullanıcının gününe çevrilmiş ya gerekçeli `# tz-exempt:` işareti taşıyor olmalı; kapsam tabanı assert'li (≥40 dosya, ≥5 çağrı), **kapının kendi mutasyon testi** (sentetik ihlali yakalıyor mu + yanlış alarm üretiyor mu), 13 kullanıcı-bağlamlı modül için **adoption kilidi** (fix geri alınırsa kapı da kırılır), muafiyetler gerekçe metni zorunlu + sayı tabanı (≤8; bugünküler piyasa takvimi TEFAS/BIST/EVDS + saf hesap yardımcılarının varsayılanı). Düzeltme öncesi 26 davranış testi kırmızıydı; **mutasyon kontrolü** tek satır geri alındığında 3 kapıyı birden yakaladı (2 davranış + statik kapı) | ✅ |

| BUG #238 | **Belgelenen "DB-katmanı 2. savunma" (RLS) prod'da FİİLEN YOKTU + onu ölçecek kapılar hiçbir otomatik ortamda koşmuyordu (denetim D22).** İki defekt, tek kök: *iddia var, kanıt yok*. **(a) Canlı defekt:** `docker-compose.prod.yml:53` uygulamayı `postgresql://financialos:...@db` ile bağlıyor ve yorumu *"RLS'in etkili olması için app NON-superuser rol (financialos) ile bağlanır"* diyordu. Ama `financialos` **aynı dosyanın `POSTGRES_USER`'ı**, yani postgres imajının bootstrap **SUPERUSER**'ı; superuser `FORCE ROW LEVEL SECURITY`'ye RAĞMEN her policy'yi bypass eder. Yani ADR-038/M51'in 12 tabloya ENABLE+FORCE ettiği `ws_isolation` policy'si prod'da hiçbir şey yapmıyordu: uygulama katmanındaki `scope_filter` tek bir uçta unutulduğunda (BUG #162 tam olarak böyle olmuştu, BUG #223/#224'te iki kez daha tekrarladı) arkada duran savunma sıfırdı — aile/workspace paylaşımı canlı olduğu için zarar başka bir kullanıcının finansal tablosunun okunmasıdır. Aynı yanlış tarif dört yerde daha yazılıydı (`.env.prod.example`, `docs/deployment/runbook.md`, ADR-038 madde 4, self-host `docker-compose.yml` postgres profili) — yani **operatör dokümanı izleyerek savunmayı kapatıyordu**. **(b) Regresyon boşluğu:** RLS/Numeric/NULL-ordering/geri-yükleme kapıları CI'da postgres servisi olmadığı için her koşumda SKIP'ti (6 skip'in 5'i); policy sessizce düşse veya FORCE kalksa hiçbir test kırmızı olmazdı. Üstelik `test_rls_postgres.py` rolü ELDE yaratıp docstring'inde onu *"prod'daki `financialos` app-rolünü temsil eder"* diye niteliyordu → gate CI'da koşsa **bile** prod'un gerçek rolünü ölçmeyecekti. Fix: **(1)** iki rol ayrımı — `financialos` (sahip) yalnız `alembic upgrade head` + rol provizyonu + `pg_dump`, `fos_app` (LOGIN NOSUPERUSER NOCREATEDB NOCREATEROLE **NOBYPASSRLS**, yalnız DML) uygulama trafiği; entrypoint migration'ı `MIGRATION_DATABASE_URL` ile koşup rolü `scripts/provision_app_role.py` ile **migration SONRASI** idempotent kuruyor (yeni tablonun yetkisi unutulmasın); **(2)** çalışma-anı fail-fast: `app.settings.database_role_problems` production+Postgres'te `current_user`'ın `rolsuper`/`rolbypassrls` değerini sorar, kanıtlanmış kötü durumda uygulama AÇILMAZ (sorgu yapılamazsa yalnız uyarı — geçici DB sarsıntısı crash-loop üretmesin); **(3)** CI `backend-tests` işine `postgres:16-alpine` servisi + `PG_TEST_URL`, iş **tüm süiti** koşuyor (dosya listesi tutulmuyor → yeni dual-dialect testi otomatik kapsanır); **(4)** `APP_DB_PASSWORD` compose'da zorunlu (`:?`) + placeholder reddi (BUG #157 sınıfı). **Sınıf taraması (L11)** iki ek defekt buldu: self-host `docker-compose.yml`'nin postgres tarifi aynı superuser reçetesini öneriyordu (RLS sessizce kapalı bir self-host kurulumu) ve app rolü şifresi için placeholder fail-fast'i yoktu | `tests/test_prod_rls_rol_kapisi.py` **11 test (statik sözleşme):** uygulama servislerinin DB kullanıcısı `POSTGRES_USER`'a EŞİT OLAMAZ (compose interpolasyonu `${V:-x}` çözülerek — kapı kendi ayrıştırıcısına kanmasın), migration ayrı sahip rolüyle koşar, scheduler'a sahip rolü sızmaz, yedek BİLEREK sahip rolüyle alınır (app rolüyle alınsa RLS bağlamı sızdığında dump EKSİK satırla "başarılı" biterdi), rol provizyon yolu entrypoint'e bağlı, `APP_DB_PASSWORD` şablonda + zorunlu, **CI kapısı:** postgres servisi + `PG_TEST_URL`'li bir iş var ve o iş `pytest tests/` koşuyor; kapsam tabanı assert'li (≥2 uygulama servisi, ≥4 dual-dialect dosyası — L11). `tests/security/test_db_rol_fail_fast.py` **8 test:** superuser ve BYPASSRLS rolleri ayrı ayrı yakalanır, normal rol geçer, placeholder şifre reddedilir, bağlantı kurulamazsa BLOKLAMAZ (asimetri), SQLite self-host ve development etkilenmez, kapı `validate_security_config` zincirine bağlı. `tests/test_rls_postgres.py` **+3 test (Postgres):** superuser'ın FORCE'a rağmen bypass ettiğinin **davranış kanıtı** (aynı sorgu/aynı yanlış workspace: superuser 1 satır, app rolü 0 satır), provizyon edilen rolün gerçekten RLS'e tabi olduğu (`rolsuper`/`rolbypassrls`/en-az-yetki), provizyonun idempotent + şifresiz/enjeksiyonlu rol adına kapalı olduğu; mevcut RLS gate'i artık **prod'un gerçek provizyon kodunu** çağırıyor. **Mutasyon kontrolü** üç mutantı da yakaladı: migration'dan `ENABLE ROW LEVEL SECURITY` kaldırıldı → RLS gate kırmızı; provizyon rolü `BYPASSRLS` yapıldı → 4 test kırmızı; fail-fast zinciri koparıldı → kapı kırmızı. Ayrıca `pg_gate.fresh_pg_database` artık artık oturumları düşürüyor (bir gate patlayınca sonrakiler `DROP DATABASE`'e takılıp ERROR zinciri üretiyor, gerçek arıza kayboluyordu). Doğrulama: tam süit **Postgres'li** koşuldu (CI'nın birebir komutu) → çıkış kodu 0, 8 SKIP artık gerçekten koşuyor | ✅ |
| BUG #239 | **Koç, fiyat sağlayıcısı çöktüğünde haftalarca eski fiyatı "güncel" gibi sunuyordu — yatırımda bayat işareti YOKTU (denetim D23).** TEFAS/İş Yatırım/yfinance düştüğünde sağlayıcılar sessizce `None` döner ve `Account.current_price` olduğu yerde kalır; koç bu durumda 30 gün önceki fiyatla hesaplanmış *"yatırım değerin 31.342 TL, %30,60 kârdasın"* cümlesini KOŞULSUZ kuruyordu. Kullanıcı bu rakama göre satış/alım kararı verirse doğrudan para kaybeder. Projenin kendi ilkesi (BUG #211, döviz tarafı: *"bayat değeri şu anki diye sunmak, hiç sunmamaktan daha kötüdür"*) fiyat/portföy yolunda uygulanmamıştı. **Kök sebep yapısaldı (L18):** tazelik verisi (`is_price_stale`/`get_price_age_text`) YALNIZ HTTP katmanında, `generate_cockpit`'ten SONRA `routers/cockpit.py`'de ekleniyordu; koç/premortem/snapshot yolları cockpit'i DOĞRUDAN çağırdığı için o alan onlara hiç ulaşmıyordu — veri VARDI, tüketiciye ULAŞMIYORDU. Fix tüketicide değil KAYNAKTA: tazelik artık cockpit sözleşmesinin parçası (`fiyat_bayat`/`fiyat_yas` her yatırım hesabında + her K/Z satırında, üst seviyede `fiyat_tazeligi` özeti), koç hesap ve K/Z satırlarını `⚠️ FİYAT BAYAT (yaş)` ile işaretler ve bayat varsa bağlama *"'şu anki/güncel' DEME"* talimatı düşer (FX yolundaki disiplinin birebir karşılığı); premortem snapshot'ı `investment_price_stale`/`investment_price_age` taşır. **İki eşik ayrımı bilinçli:** 24 saat ETİKET eşiği (dürüst, ücretsiz), 72 saat ALARM eşiği (`fund_tracker.PRICE_ALERT_HOURS`) — TEFAS hafta sonu yayın yapmadığından 24 saati alarma bağlamak her pazar uyarı üretir ve uyarı yorgunluğu gerçek kesintiyi görünmez kılardı. **Sınıf taraması (L11) ikinci yüzeyi buldu:** Hesaplar paneli `current_price`'ı koşulsuz **"Güncel fiyat"** diye etiketliyordu ve yaşı gösteremiyordu (`AccountOut` türetilmiş yaş döndürmüyordu) — satış kararı tam o ekranda verilir. `schemas.FiyatTazeligiMixin` ile tazelik tek kaynaktan (fund_tracker) türetilir, istemcide ikinci bir kural yazılmaz; bayatta başlık "Fiyat (bayat)" olur ve yaş rozeti çıkar | `tests/test_fiyat_bayat_koc.py` **18 test:** kaynak sözleşmesi (bayat/taze/hiç-girilmemiş hesap, K/Z satırı, cockpit özeti), uyarı eşiği (10 gün → uyarı; 30 saat hafta-sonu → uyarı YOK), koç dili (satır işareti, "şu anki DEME" talimatı, K/Z satırı; **taze fiyatta hiç "BAYAT" geçmez** — mutasyon kontrolü), sınıf taraması (premortem snapshot + prompt, taze veride prompt susar), **uç↔panel sözleşmesi** (`GET /api/accounts` `fiyat_bayat`/`fiyat_yas` döner; nakit hesapta uydurmaz — BUG #232/#233 dersi: iki taraf ayrı ayrı doğru olup uyuşmazlık ARADA kalabiliyor), **kapsam tabanı assert'li** (3 yatırım hesabı; fiyat sunan her hesap tazelik taşımalı). `frontend/src/hesap-fiyat-bayat.test.jsx` **4 vitest:** bayatta "Güncel fiyat" etiketi ÇIKMAZ, yaş görünür, tazede etiket korunur, nakit kartta fiyat satırı yok. **Mutasyon kontrolü 3/3 yakalandı:** `fiyat_bayat=False` sabitlendi → 9 kırmızı; koç satır işareti kaldırıldı → kırmızı; alarm eşiği 72s→24s → hafta-sonu gürültü testi kırmızı. Doğrulama: **1847 passed, 9 skipped** (+18) + **139 vitest** (+4) | ✅ |

| BUG #240 | **Planlanan 5 cron işinden 3'ü hiçbir çalışma kaydı tutmuyordu — KVKK 90-gün saklama işi dahil sessizce ölebilirdi (denetim D24).** `_kayit_basla`/`_kayit_bitir` yalnız `nightly_batch` ve `fetch_investment_prices` GÖVDESİNE elle yazılmıştı; `k2_batch`, `nightly_trace_cleanup` ve `weekly_smoke_test` hiçbir `SchedulerRun` satırı açmıyordu. `/api/ops/scheduler` iş adlarını o tablodan türettiği için bu üç iş **uçta hiç görünmüyordu**: kullanıcıya KVKK metninde söz verilen *"koç akıl-yürütme kayıtları 90 gün sonra otomatik temizlenir"* taahhüdünü uygulayan tek kod yolu haftalarca ölü kalsa kimse göremezdi (hukuki risk + ReasoningTrace birikimi). **Fix yapısal (L24):** `PLANLI_ISLER` planlı işlerin TEK kaynağı oldu (`start_scheduler` artık elle `add_job` çağırmıyor, listeyi geziyor) + `_izlenen_is` sarmalayıcısı kaydı açıp kapatıyor — yeni iş ekleyen kişi kaydı unutamaz; iş gövdeleri kendi izleme kodundan tamamen arındı. Saklama işi **kaç satır sildiğini** kaydeder (`"N trace silindi (90 gun)"`) → taahhüt log okumadan, sayıyla doğrulanır. **İkinci yarısı ucun körlüğüydü (L23):** hiç koşmamış iş = tam da ölü olan iş, ve uç onu göremiyordu; boş liste "her şey yolunda" gibi okunuyordu. Adlar artık `PLANLI_ISLER`'den gelir; `hic_calismadi`, `gecikti` (son koşum beklenen periyodun 1.5 katından eski — misfire tek gecikmeyi alarma çevirmesin) ve tek bakışlık `sorunlu_isler` özeti eklendi. **Sınıf taraması (L11) iki şey buldu:** (a) aynı defekt compose **yedeğinde** de vardı — `deploy/pg_backup.sh` çıkış kodu yalnız konteyner log'una düşüyordu, oysa yedek beta verisinin TEK kopyası; script artık başarı ve başarısızlık yollarının ikisinde de `scheduler_runs`'a yazıyor (`DIS_PLANLI_ISLER` = uygulama süreci dışında koşan planlı iş) ve aynı uçtan izleniyor, kayıt hatası yedeği DÜŞÜRMÜYOR. (b) Bu değişikliğin KENDİ yarattığı tuzak: `scripts/live_gate.py` "cron çalıştı mı"yı `bool(isler)` ile ölçüyordu — liste artık hep dolu olduğu için kapı sessizce hep-yeşile dönerdi (kendini yeşile boyayan kapı); ölçüt `hic_calisma_yok` + hiç-koşmayan/geciken raporuna taşındı | `tests/test_scheduler_kayit_kapisi.py` **14 test:** kapsam tabanı (`PLANLI_ISLER` ↔ APScheduler job id'leri BİREBİR — elle `add_job` ile kayıt dışı iş eklenemez; her planlı iş sarmalayıcıdan geçer), 5 işin **her biri** için koştur→kayıt var mı (parametrik), patlayan iş `ok=False` kapanır, saklama işi silinen satır sayısını kaydeder, uç hiç koşmamış işi listeler, geciken iş işaretlenir, dış işin kaydını kendisi yazar (başarı+başarısızlık), canlı kapı dolu listeyi "çalıştı" sanmaz. **Mutasyon kontrolü 3/3 yakalandı:** bir işin sarmalayıcısı kaldırıldı → 3 kırmızı; uç yine tablodan türetti → kırmızı; `gecikti=False` sabitlendi → kırmızı. Boş-durum fixture'ı sözleşme değişikliğini yakaladı (yenilendi). Doğrulama: **1861 passed, 9 skipped** (+14) + **139 vitest** | ✅ |

| BUG #241 | **Panelden "Ödendi" işaretlenen alacak nakde HİÇ geçmiyordu — kullanıcı bildirimi (denetim dışı, canlı veride 5.000 TL eksik).** Aynı gerçek-dünya olayının (alacağın tahsili / borcun ödenmesi) İKİ girişi vardı ve nakit ayağı **yolun içine** kodlanmıştı: koç yolu (`action_executor._execute_mark_debt_paid`, BUG #113) nakdi hareket ettiriyor, panel yolu (`PUT /api/debts/{id}` — kullanıcının fiilen bastığı "Ödendi" butonu) yalnız `is_paid` bayrağını çeviriyordu. Zarar tek yönlü değil: tahsilat alacağı listeden düşürüp nakde geçirmediği için **Tam Net Değer eriyor**, borç ödemesi ise borcu düşürüp nakdi düşürmediği için **net değer şişiyordu** — yani kullanıcı yanlış rakama bakarak para kararı veriyordu. BUG #161/SBN-001 ailesinin aynısı (kural birden çok yerde ayrı kodlanmış). Fix tüketicide değil sözleşmede: **`app/services/debt_settlement.py` tek kaynak** — yön (`kapanis_delta`), hedef hesap, ve `delta = yeni_etki − eski_etki` senkronizasyonu (işaretle/geri al/tutar düzelt yollarının hepsi tek-uygulamalı, çift-sayım imkânsız); iki yol da oradan geçiyor. Kapanış **iz bırakıyor** (`personal_debts.settlement_account_id`, migration `f2a3b4c5d6e7`): geri alma tam da uygulandığı hesaptan geri sarılır, ayağı hiç uygulanmamış eski kayıt (NULL) geri alınınca **hayalet para düşmez**, silinen kaydın etkisi de kayıtla birlikte geri sarılır (sahipsiz bakiye etkisi kalmaz — panel silme onayı bunu ÖNCEDEN söyler). Panelde geri-alma yolu da yoktu (backend simetrikti ama kullanıcı yanlış işaretlemesini düzeltemiyordu) → "Geri al" eklendi; toast artık nakdin nereye/ne kadar işlendiğini söyler (bakiye sessizce değişmez). **Sınıf taraması (L11)** ikinci defekti buldu: "varsayılan nakit/kart hesabı" **beş ayrı yerde** seçiliyordu (executor ×3, `routers/transactions`, `routers/incomes`, `simulation_engine`) ve **hiçbiri emanet hesabı dışlamıyordu** — MC1 guard'ı tek yerdeyken seçiciler çoğalmıştı (emanet hesap varsayılan olunca ya para emanetten hareket eder ya da her onayda reddedilen sessiz çıkmaz sokak üretilir); üçü de sırasızdı (`.first()`), yani aynı olayın uygulanması ve geri sarılması farklı hesaplara düşebilirdi. `app/account_rules.py` tek kaynak (kapsam-farkında + emanet dışlar + `id` sırası deterministik). **Canlı veri onarıldı:** yedek → `alembic upgrade head` → `scripts/repair_debt_settlements.py --uygula` (çift-sayım korumalı: executed `mark_debt_paid` aksiyonu olan kayıtları ATLAR, çünkü onların nakdi zaten hareket etmişti) → nakit 1.963,52 → 6.963,52 TL | `tests/test_alacak_tahsili_nakit_karsiligi.py` **21 test:** kullanıcının senaryosu (işaretle → cockpit `nakit_kasa` artar), tahsilat net-NÖTR (Tam Net Değer sabit), borç ödemesi nakdi azaltır, yalnız `paid_date` gönderilse de çalışır, geri alma geri sarar, iki kez işaretleme çift uygulamaz, ödenmiş kaydın tutarı düzeltilince nakit FARK kadar düzelir, ödenmemişte nakde dokunulmaz, **eski (ayağı uygulanmamış) kayıt geri alınınca hayalet para düşmez**, nakit hesap yoksa istek patlamaz, silme etkiyi geri sarar, **emanet hesaba dokunulmaz (MC1)**, kapanış izi uçtan görünür, **iki yol paritesi** (panel ve koç aynı nakit etkisini üretir — parametrik: tahsilat + ödeme), onarım script'i (uygular / kuru çalışmada yazmaz / koç yolundan kapatılanı ATLAR / idempotent). `tests/test_varsayilan_hesap_kapisi.py` **10 test — statik sınıf kapısı:** hiçbir dosya kendi varsayılan-hesap sorgusunu yazmıyor (kapsam tabanı: tek kaynağı ≥4 dosya kullanıyor — L23 boş-liste tuzağı kapatıldı; taramanın kendisi sentetik bypass ile ölçülüyor), sim seçicisi emaneti dışlar (sim↔executor paritesi), muafiyet listesi bayat değil + davranış (emanet varsayılan olamaz, deterministik en-küçük-id, tip filtresi, başka kullanıcının hesabı seçilmez). `frontend/src/borc-alacak-tahsilat.test.jsx` **6 vitest:** nereye ne kadar işlendiği söylenir, nakit hesap yoksa SESSİZ kalınmaz, kapanmış kayıt hesabı satırında taşır, silme onayı geri sarmayı önceden söyler, geri alma yolu çalışır + geri sarmayı bildirir, ödenmemişte nakit uyarısı çıkmaz. **Mutasyon kontrolü 5/5 yakalandı:** işaret ters çevrildi → kırmızı; emanet dışlaması kaldırıldı → kırmızı; geri sarma izi yerine varsayılan hesabı kullandı → kırmızı; iz bırakılmadı → kırmızı; fark yerine mutlak uygulama (çift-sayım) → kırmızı. Doğrulama: **1888 passed, 9 skipped** (+27) + **145 vitest** (+6) + `test_fresh_db_migration.py` temiz kurulum | ✅ |

| BUG #242 | **Veri-işleyen envanteri "kodla kilitlenir" diyordu ama kapı dört ismi SABİT kodluyordu (denetim D25).** Fallback zincirine 13 Tem 2026'da eklenen **Together AI** ve **DeepInfra** envantere hiç girmedi; envanter üç hafta SONRA yazıldığı hâlde eksik kaldı ve `tests/test_legal_docs.py` `("gemini","groq","anthropic","ollama")` sabitlerini arayıp `app.coach`'u hiç okumadığı için YEŞİL kaldı. KVKK m.9/m.10 alıcının bilinmesini ister; operatör tek bir env değişkeni tanımlayınca beyan edilmemiş bir ABD şirketine tüm beta verisi akabilirdi (latent). Fix: kapı KODDAN türetiyor — (a) `LLMProvider` somut alt sınıfları (SDK ile konuşanlar URL literali taşımaz), (b) `app/` içindeki her `https://` literalinin host'u; host adı envanterde **birebir** yazılı olmalı (marka adı eşleştirmesi belirsizdir, mutasyonla ölçüldü). **Sınıf taraması (L11)** denetimin iki isminin ötesinde dört eksik daha buldu: `www.isyatirim.com.tr` ve — daha ağırı — **Google/GitHub ile giriş** (`accounts.google.com`, `oauth2.googleapis.com`, `openidconnect.googleapis.com`, `github.com`, `api.github.com`) envanterde HİÇ yoktu; üstelik §4 *"harici kimlik sağlayıcı YOK"* diyerek gerçeği YANLIŞ beyan ediyordu → §3b (kimlik sağlayıcıları, hangi verinin gidip geldiği) eklendi, §4 düzeltildi. Ters yön de kilitlendi: envanterde kodda karşılığı olmayan sağlayıcı satırı kalamaz (bayat beyan) | `tests/test_veri_isleyen_envanteri_kapisi.py` **10 test** (iki kapsam tabanı, sınıf türetmesi ↔ envanter TABLOSU, ters yön, host türetmesi, muaf-host listesi bayat değil, taramanın kendi meta-testi, envanterin kendi doğrulama iddiasının bu kapıya bağlı olması, OAuth sağlayıcıları, §4 çelişkisi). `tests/test_legal_docs.py` sabit listesi kaldırıldı → kapının VARLIĞINI + koddan türettiğini doğrular; rıza metninin sosyal giriş sağlayıcılarını andığı da kilitlendi. **Mutasyon 6/6 yakalandı** (host silindi / sağlayıcı satırı silindi / koda beyansız host eklendi / OAuth silindi / hayalet sağlayıcı eklendi / sabit-liste tuzağına dönüş). Doğrulama: **1903 passed, 9 skipped** | ✅ |

| BUG #243 | **KVKK veri-sahibi hakları elle bakılan listelere dayanıyordu: export şifre hash'ini döküyor, iki tabloyu atlıyor, silme e-postayı bırakıyordu (denetim D26+D27+D28).** Üç bulgu tek kök: *"tüm verin"* ve *"tüm verin silinir"* taahhütleri şema büyüdükçe geride kalan listelere bağlıydı. **(D26)** Export `_row_to_dict`/`_row` ile TÜM kolonları basıyordu → kullanıcının **bcrypt şifre hash'i** ve **OAuth kimliği (sub)** kullanıcının diskine inen, e-postayla paylaşılan, buluta yedeklenen dosyaya yazılıyordu (çevrimdışı kırılabilir; şifresini tekrar kullanan kişi banka/e-posta hesabını da kaybeder). **(D27)** Silme sonrası kullanıcının **e-postası ve operatörün o kişi hakkında yazdığı serbest not** `beta_invites`'ta süresiz kalıyordu — purge yalnız `user_id` kolonuna bakıyor, oradaki kolon `used_by_user_id`; yani canlıda sunulan *"tüm veriniz kalıcı olarak silinir"* taahhüdü fiilen yanlıştı (KVKK m.7). **(D28)** UI'nin ve KVKK metninin gösterdiği uç (`/api/users/me/export`) `goal_allocations` + `goal_rules`'u hiç dökmüyordu, çünkü **iki ayrı export uygulaması** vardı ve tamlık testi ÖTEKİNİ ölçüyordu (BUG #241 ile aynı sınıf: aynı vaadin iki yolu ayrışır). Fix tek kaynakta: **`app/data_subject.py`** — şemadaki HER tablonun sınıflandırıldığı `KAYIT` (kullanıcı verisi / hedef üzerinden bağlı / anonimleştirilir / kullanıcı-dışı + gerekçe), export'un TEK uygulaması `disa_aktar` (iki uç da onu döner → ayrışamazlar, `financialos-export-v1` şeması), `GIZLENEN_ALANLAR` (hash/sub/token_version) ve silmede temizlenen dolaylı atıflar. `purge_user_data` artık kayıttan besleniyor: davet satırı SİLİNMEZ (kod tekrar kullanılamamalı — işletme kaydı) ama e-posta/not/atıf NULL'lanır; `error_logs` kaydı korunur, `last_user_id` temizlenir | `tests/test_kvkk_veri_sahibi_kapisi.py` **16 test:** şema↔kayıt iki yönlü tamlık (yeni tablo sınıflandırılmadan eklenemez) + kapsam tabanı + gerekçe zorunluluğu, **sır-görünümlü her kolonun sınıflandırılmış olması** (otomatik desen-gizleme YOK: `tokens_in` kullanıcının kendi verisidir), export hedef katkı/kurallarını içerir (D28), iki uç da hash/sub dökmez ve yanıt metninde `$2b$` izi yoktur (D26), export yine de kullanıcının kendi verisini verir (L6), **iki ucun kapsamı aynı** (ayrışma kapısı), kayıt↔yanıt tutarlılığı, ve silme tarafında **şema-geneli e-posta taraması** (tüm metin kolonları; silme ÖNCESİ bulduğunu ispatlayan meta-test ile — L23) + operatör notu + hata kaydı atfı. `tests/test_data_export.py` tamlık invariant'ı kaynak-metin grep'inden KAYIT'a taşındı (yanlış fonksiyonu doğrulama tuzağı kapandı). Boş-durum fixture'ı yenilendi (içindeki gerçek bcrypt hash de böylece repodan düştü). **Mutasyon 4/4 yakalandı** (gizleme kaldırıldı / hedef çocukları export'tan düştü / anonimleştirme adımı kaldırıldı / yeni tablo sınıflandırması atlandı). Doğrulama: **1918 passed, 9 skipped** + **145 vitest** | ✅ |

| BUG #244 | **Maskeleme VARDI ama yarısını kaçırıyordu ve asıl sızdıran yüzeye (log dosyası) hiç uygulanmıyordu (denetim D29).** `app/error_tracking.py` docstring'i *"PII/sır temizliği zorunlu"* diye güvence veriyordu; denetimin 12 gerçekçi örneğinin ALTISI hiç değişmeden çıktı: TCKN ve telefon (11 hane — "13-24 hane" kuralının altında), boşluksuz IBAN (`TR33…` harf öneki `` sınırını bozuyor), bcrypt hash, tırnaklı şifre değeri, JWT olmayan opak Bearer token. İkinci ve daha ağır boşluk **tüketici körlüğüydü (L21 sınıfı — BUG #239 ile aynı):** `temizle()` YALNIZ DB kaydına uygulanıyordu; `app/main.py`'deki global yakalayıcı `logger.exception(..., exc_info=True)` ile ham traceback'i `logs/financialos.log`'a maskesiz yazıyordu. Üçüncü kök KAYNAKTAYDI: SQLAlchemy istisna metinleri **bound parameter** taşıyor (`[parameters: ('Ali Veli', 'ali@…', '$2b$12$…')]`) — yani ad, e-posta ve şifre hash'i istisnanın İÇİNE giriyor, oradan hem log'a hem hata kaydına akıyordu. Log dosyası operatörün, yedek alan sistemlerin ve olası bir log-toplama zincirinin gördüğü yüzeydir (KVKK m.12). Fix üç katmanlı: **(a)** desenler tamamlandı (TCKN/telefon/IBAN/hash/opak token/tırnaklı değer + `[parameters: …]` bloğunun tamamı) — sıra önemli, özel desenler genel sayı deseninden ÖNCE koşar; **(b)** `LogMaskeleyici(logging.Filter)` `setup_logging`'de HER handler'a yapısal olarak bağlandı (tek tek çağrı yerlerine bırakılsa unutulurdu — L24) ve `JsonFormatter` artık maskelenmiş `exc_text`'i kullanıyor (eskiden `formatException(exc_info)` ile istisnayı ham hâlinden yeniden üretip maskeyi ATLIYORDU); **(c)** `hide_parameters=True` — parametreler istisnaya hiç girmesin. Ayrıca koçun içgörü METNİ log'a yazılıyordu (`result.content[:60]`) → dedup anahtarı + karakter sayısına indirildi (BUG #180 ilkesi) | `tests/test_log_maskeleme_kapisi.py` **15 test:** denetimin kaçan 8 örneği parametrik (TCKN/telefon/uluslararası telefon/IBAN/gruplu IBAN/bcrypt/opak token/tırnaklı şifre), SQLAlchemy parametre metni (ad + hash + e-posta), **maskeleme teşhisi öldürmez** (dosya:satır + istisna tipi kalır — L6), log kaydında PII yok (mesaj + `exc_info` birlikte), JSON formatter çıktısında PII yok, filtrenin `setup_logging`'e YAPISAL bağlı olması, `hide_parameters` kaynak savunması, koç metninin log'a düşmemesi. Regresyon: mevcut `test_error_tracking.py` maske testi, ayırıcısız kuralın çıplak "token" kelimesini yiyip **zaten maskelenmiş** metni bozduğunu yakaladı → kural tırnaklı değere daraltıldı (kapının kendisi fix'i düzeltti). Doğrulama: **1934 passed, 9 skipped** | ✅ |

| BUG #245 | **Örnek dosyanın placeholder'ı hem fail-fast'i hem canlı kapıyı geçiyordu (denetim D30).** `.env.prod.example` git'te herkese açıktır ve operatör onu kopyalayarak `.env.prod` üretir. `SECRET_KEY` için placeholder reddi VARDI — ama yalnız onun için: `SUPPORT_EMAIL=destek@<alan-adin>` ile `validate_security_config()` hatasız geçiyor, `scripts/live_gate.py` de `"@" in destek` diye baktığı için canlı kapı da yeşil kalıyordu. Zarar BUG #210'un kapatmak istediğinin aynısı: giriş YAPAMAYAN kullanıcı (yanlış şifre / doğrulanmamış e-posta / çalışmayan davet) uygulama-içi geri bildirime de ulaşamaz; giriş ekranında ve `/api/meta`'da gösterilen tek kanal **teslim edilemeyen** bir adres olur → kullanıcı sessizce kaybedilir, KVKK'nın "veri sorumlusuna başvuru" hakkı fiilen kullanılamaz. Fix desen eklemek değil KAYNAĞI değiştirmek: placeholder'ın ne olduğunu **örnek dosyanın kendisi** söyler (`app/settings.ornek_env_degerleri` + `placeholder_mi`) → yarın örneğe yeni bir anahtar eklenirse kapı onu da kapsar (L26). Aynı ölçüt canlı kapıya da bağlandı | `tests/test_placeholder_kapisi.py` **16 test:** örnek dosya okunabiliyor (kapsam tabanı ≥10 anahtar — L23), placeholder tanınır (5 biçim) ve **gerçek değer placeholder sayılmaz** (L6), SUPPORT_EMAIL placeholder'ı reddedilir / gerçek adres geçer, SECRET_KEY korumasının ortak yardımcıya taşınırken kaybolmadığı (regresyon), `validate_security_config` patlar, development'ta bloklanmaz, **canlı kapı aynı ölçütü kullanır**, ve örnekte 'ZORUNLU' işaretli her anahtarın fail-fast'te karşılığı olması. Not: ilk desen listesi `"xxx"` içeriyordu ve mevcut testlerin `"x"*48` secret'ını yanlışlıkla reddetti — süit bunu yakaladı, liste daraltıldı (kapı ürünü kıramaz). Doğrulama: **1964 passed, 9 skipped**. ⚠️ **Aynı oturumda düzeltme (R3 dersi):** canlı kapıya eklenen ölçüt önce `from app.settings import placeholder_mi` ile yazılmıştı; script GERÇEKTEN koşturulunca `ModuleNotFoundError: No module named 'app'` ile ÇÖKTÜ — yani kapıyı sertleştiren değişiklik kapının TAMAMINI öldürmüştü. `live_gate.py` bilinçli olarak bağımsızdır (uygulama paketi olmayan bir makineden canlı URL'ye karşı koşabilmeli), desen orada kopyalanır ve **ayrışmaması iki yeni testle** kilitlendi (`live_gate` app import etmez + desen listesi `settings` ile birebir aynı). Kapıyı yazmak yetmiyor; kapıyı KOŞTURMAK gerekiyor | ✅ |

| BUG #246 | **Kimliksiz dış-çağrı yüzeyi + doğrulanmayan tercih (denetim D32 + D33).** **(D32)** `/api/prices/*` uçları kimlik de hız sınırı da istemiyordu ve her istek TCMB EVDS'ye **30 sn timeout'lu SENKRON** bir dış HTTP çağrısı tetikliyordu; ölçüldü: 20 kimliksiz istek → 20 dış çağrı, 401/429 yok. Uçlar senkron (`def`) olduğu için threadpool dolduğunda **gerçek kullanıcılar cockpit/koç/işlem girişine erişemez**; ayrıca operatörün EVDS kotası üçüncü şahıslarca yakılır (fiyatlar bayatlar). Bugün `EVDS_API_KEY` tanımsız olduğundan sömürü kapalı — yani tek bir env değişkeni uzaklıkta **latent** bir açıktı; kapatması iki satır. **(D33)** `PUT /api/user` para birimini ve locale'i hiç doğrulamadan saklıyordu (`'XYZ'`, `'!!!'`, `'   '` → 200) ama arayüz sabit `TL` gösteriyor: kullanıcı "ayarladım" sanıp tutarları YANLIŞ para biriminde okur; bozuk değer KVKK export'una da girer. Aynı uçtaki `timezone` dalı geçersiz değeri 422 ile reddediyordu — sözleşme kendi içinde tutarsızdı. Fix: ISO-4217 kümesi + locale deseni, timezone dalıyla aynı sertlikte | `tests/test_uc_sertlestirme_d32_d33.py` **14 test:** kimliksiz fiyat isteği 401 **ve dış servise çağrı SAYISI 0** (yalnız kodu değil davranışı ölçer), altın ucu da kimlik ister, giriş yapmış kullanıcı fiyatı alabilir (L6), uçların rate-limit bağımlılığı statik kilit; geçersiz para birimi 4 biçimde 422 / geçerli 3 biçimde 200, locale doğrulaması, timezone regresyonu. Doğrulama: **1964 passed, 9 skipped** | ✅ |

| BUG #247 | **Sağlık ucu veritabanına HİÇ dokunmuyordu — üç otomatik kapı birden kördü (denetim D39).** `/api/health` yalnız bir sözlük dönüyordu; DB çökmüşken, disk dolduğunda ya da **migration koşulmamışken** de 200. Buna üç tüketici bağlıydı: Docker `HEALTHCHECK` (konteyner "healthy" görünür), `scripts/deploy.sh` (**otomatik rollback tetiklenmez** — asıl zarar), `scripts/live_gate.py` (operatör paneli yeşil). Kullanıcı bu sırada her ekranda 500 alır; finansal üründe "yeşil görünen ölü sistem", kırmızı görünenden pahalıdır çünkü kimse bakmaz. Fix sektör deseniyle ayrım: `/api/health` = **canlılık** (bağımlılık yok, her zaman 200 — geçici DB sarsıntısı konteyneri öldürüp crash-loop üretmesin), yeni `/api/ready` = **hazır olma** (DB `SELECT 1` + şema sürümü kod ile aynı mı; değilse **503** — 200 gövdesindeki `saglikli:false` hiçbir orkestratörü tetiklemez). Üç tüketici de yeni uca çevrildi | `tests/test_hazir_olma_kapisi.py` **7 test:** sağlıklı DB'de 200, DB ölüyken 503, **health canlılık olarak 200 kalır** (L6), şema uyumsuzsa 503, ve üç tüketicinin (Dockerfile/deploy.sh/live_gate) gerçekten bu ucu okuduğu (L21 — kapının tüketiciye ULAŞTIĞINI ölçen kısım). Boş-durum fixture'ı yeni uçla yenilendi | ✅ |

| BUG #248 | **Kendini başarılı sanan cron + kendini atlayan test + elle beslenen migration kapısı (denetim D37+D36+D38).** Üçü de aynı aile: **bir kapı ölçtüğünü sandığı şeyi ölçmüyordu.** **(D37)** Fiyat işi TEK BİR hesabı bile güncelleyemediği gecelerde kendini `ok=True` kaydediyordu — "iş çökmedi" ile "iş işini yaptı" karıştırılmıştı; `/api/ops/scheduler` "son başarılı: bu sabah" der, `sorunlu_isler`'e düşmez, TEFAS/İş Yatırım kesintisi operatörden haftalarca gizlenir ve koç bayat fiyatla konuşur (BUG #239'un beslendiği durum). Artık `accounts and updated == 0` → başarısız; hesap YOKSA (0/0) başarı meşru. Yan bulgu: kayda yalnız istisna TİPİ ("RuntimeError") yazılıyordu — mesaj da yazılır, **PII maskesinden geçirilerek** (BUG #244 ile birleşti). **(D36)** `tests/test_scheduler.py`'deki execute→hook testi geçersiz payload yüzünden HER koşumda `pytest.skip`'e düşüyordu: yazıldığı günden beri ölüydü, sayıya dahildi, koruduğu sanılan yolu hiç doğrulamıyordu (kendi başarısızlığını "atlandı"ya çeviren test, olmayan testten zararlıdır). **(D38)** Migration geri-alınabilirlik kapısı ELLE yazılmış 9 revizyonluk bir kümeye bakıyordu → listeye eklenmeyen her yeni migration denetlenmiyordu, `def downgrade(): pass` yazan sessizce geçiyordu. Artık `ScriptDirectory.walk_revisions()` ile TÜM revizyonlar denetlenir; yalnız 10 bilinen-eski sürüm gerekçeli istisnadır | D37 için `tests/test_scheduler_kayit_kapisi.py` **+2 test** (hiçbir hesabı güncelleyemeyen iş `ok=False` kaydeder ve detayı ölçüyü — `0/1` — taşır; hesap yoksa 0/0 başarı sayılır, L6). Kapının yardımcı fixture'ı da güncellendi ("dış dünya sussun" niyeti artık geçerli fiyat döndürmek demek). D36: skip dalı kaldırıldı, payload geçerli hâle getirildi → yol gerçekten koşuyor. D38: kapsam tabanı testi (≥15 revizyon + istisna listesi bayat değil) + parametrizasyon şemadan türetilir. Doğrulama: **1985 passed, 18 skipped** (skip artışı D38'in bilinen-eski istisnaları) + **145 vitest** | ✅ |

| BUG #249 | **Kurucunun tanıdığı kişinin adı sistem prompt'unda + runbook komutları prod DB'yi görmüyordu (denetim D34 + D40).** **(D34)** `V3_GOD_MODE_PROMPT` ve `save_insight` tool şemasındaki `dedup_key` örnekleri gerçek bir kişinin adını ve kurucunun özel seyahat/yatırım senaryosunu taşıyordu; bunlar HER beta kullanıcısının HER koç mesajında üçüncü taraf LLM sağlayıcısına gidiyordu (KURAL 11 + ürünün kendi KVKK duruşuyla çelişir). Jenerikleştirildi. **(D40)** Runbook `python -m scripts.beta_invite ...` diyordu; host kabuğunda `DATABASE_URL` tanımlı DEĞİL → davet kodu **yerel SQLite'a** yazılır, komut "üretildi" der, davetli canlıda 403 alır ve kapalı beta fail-closed olduğu için kimse kayıt olamaz. Aynı repoda doğru form (README) zaten vardı — belge kendi içinde tutarsızdı; tüm operasyon komutları `docker compose ... exec -T backend` önekine çevrildi + neden'i anlatan uyarı eklendi | `tests/test_runbook_konteyner_komutlari.py` **7 test** (kapsam tabanı + prod DB'ye dokunan 5 script için host-kaçağı yok + uyarı metni). D34 için mevcut kişiselleştirme kapıları (`test_urunlesme_kisisellestirme`) yeşil kaldı. Doğrulama: **1993 passed, 18 skipped** | ✅ |

| BUG #250 | **Kapsam kapısının KENDİSİ kör noktalıydı; koçun TEK yazma yolu kapının dışındaydı (denetim D31).** İki ayrı boşluk. **(a)** AST tarayıcısı yalnız `db.query(Model)` / `select(Model)` ÇIPLAK şeklini görüyordu; paralel triyaj koşturarak ölçtü — `db.get(Model, id)`, `db.query(Model.kolon)`, `db.query(func.sum(Model.kolon))` ve `select(Model.kolon)` şekillerinin DÖRDÜ de kapının dışındaydı (hepsi aynı satırları okur/yazar). **(b)** İlk kapının dosya kapsamında **`app/action_executor.py` yoktu** — LLM'in DB'ye dokunduğu TEK dosya, kapsam kuralının en çok gerektiği yer; orada 11 ham `Model.user_id == user_id` filtresi vardı. Bugün aktif çapraz-kullanıcı sızıntısı YOKTU (tarama sonrası 7 aday elle incelendi; sahiplik hepsinde çağıranda doğrulanmış) — ama BUG #162 tam bu sınıftan geçmişti ve BUG #241 ile eklenen yeni kod (`debt_settlement.py`, `db.get`) kör noktayı büyütüyordu. Fix: tarayıcı çağrının ilk argümanını AST olarak GEZİYOR (şekil bağımsız); gerekçe yorumu artık sorgunun **ÜSTÜNE** de yazılabiliyor (pencere yalnız aşağı bakıyordu → yazar `# scope-exempt` yazsa bile kapı kırmızı kalıyordu, bu da uzun gerekçeyi imkânsız kılıyordu); executor kapı kapsamına alındı ve 9 hesap/borç araması `scope_filter(..., _yazma_workspace_id(...))` ile **kapsam-farkında** hâle getirildi (aile görünümünde kişisel kayda yazmak = BUG #223 sınıfı), 2 `PendingAction` araması gerekçeli muaf (aksiyonun KENDİ kaydı; kapsam orada filtre değil SONUÇ — `workspace_scope(pending.workspace_id)`). Genişleyen tarama 6 yeni yer buldu; hepsi ya kapsamlı ya gerekçeli muaf yapıldı (goal çocukları ebeveyn üzerinden, paylaşılan LLM kotası KASTEN çapraz-kullanıcı, çift-sayım kapısı bilinçli hedef-üstü) | `tests/test_scope_enforcement.py` **+11 test:** dört kör-nokta şekli için sentetik yakalama + aynı şekillerin kapsamlı yazımında **yanlış alarm üretmemesi** (L6), gerekçenin sorgu ÜSTÜNE yazılabilmesi, koç yazma yolunun kapsamda olması, kapsam tabanı (≥20 dosya). **Mutasyon 3/3 yakalandı:** tarayıcı eski dar hâline döndü → kırmızı; executor kapsamdan çıkarıldı → kırmızı; executor'da ham `user_id` filtresi geri geldi → kırmızı. Doğrulama: **2004 passed, 18 skipped** | ✅ |

| BUG #251 | **Para birimi ayarı GEÇERLİ ama GÖSTERİLEMEZ kodları kabul ediyordu — D33'ün ASIL şikâyeti açık kalmıştı.** BUG #246 geçersiz kodu (`'XYZ'`, `'!!!'`) reddetmişti; ama `'USD'` kabul edilmeye devam ediyordu ve hiçbir katman kur çevirmiyor, arayüz her yerde `TL` yazıyor (`formatTL`, 139 çağrı). Yani kullanıcı "USD ayarladım" der, tutarları YANLIŞ para biriminde okur — denetimin şikâyeti tam olarak buydu, doğrulama onun yalnız yarısını kapatıyordu. Küme, ürünün GERÇEKTEN gösterebildiğine indirildi (`TRY`) ve hata mesajı nedenini söylüyor (tutarlar TL saklanır, kur çevrimi yok, ADR-042). Ayarlanabilir olmak, gösterilebilir olmak demek değildir; sunulmayan bir düğme sunmamak da bir kalite kararıdır | `test_uc_sertlestirme_d32_d33.py` güncellendi: `TRY`/`try` normalize kabul + **USD/EUR/GBP 422 ve mesaj gerekçeyi taşır**. `test_user_preferences` içindeki EUR beklentisi gerekçeli güncellendi | ✅ |

| BUG #252 | **Üç eski kapı kendi kapsamını ölçmüyordu (L23/L27 uygulaması).** `test_urunlesme_kisisellestirme` (kişiye özel iz), `test_workspace_insert_kapisi` (workspace_id yazılıyor mu) ve `test_alembic_integrity` (migration zinciri) dizin/AST tarayarak çalışıyor ama **tarama boş dönerse hepsi yeşil geçiyordu** — yani bir gün glob deseni, AST şekli ya da dizin değişse kapı sessizce hiçbir şey ölçmez hâle gelirdi. Bu turda yeni yazılan kapıların hepsinde kapsam tabanı vardı; eskilere de uygulandı: taranan dosya sayısı, bulunan çağrı sayısı ve (ürünleşme kapısında) **iki tarafın da** (backend + arayüz) temsil edilmesi assert ediliyor | 3 yeni kapsam-tabanı testi. **Mutasyon 3/3:** her üç taramanın glob'u boşaltıldığında ilgili kapı kırmızı oldu (öncesinde yeşil geçiyorlardı). Doğrulama: **2010 passed, 18 skipped** | ✅ |

| BUG #253 | **Giriş yapamayan kullanıcının "bende mi, sizde mi?" sorusu cevapsızdı (masterprompt SIRADAKİ İŞLER madde 4).** `/api/meta/durum` ve (BUG #247 ile) `/api/ready` uçları vardı ama kullanıcının bakabileceği bir YÜZEY yoktu: elindeki tek bilgi "bir şeyler ters gitti"ydi — şifresini mi yanlış giriyor, davet kodu mu geçersiz, yoksa sunucu mu ölü, ayırt edemiyordu. Bilinmezlik bilinen kesintiden çok destek yükü üretir ve beta kullanıcısını sessizce kaybettirir. `SistemDurumu` bileşeni giriş ekranından açılır: **kimlik istemez** (asıl ihtiyaç duyan giriş YAPAMAYAN kullanıcıdır), `/api/ready`'nin 503'ünü HATA değil SONUÇ olarak okur, ve **teşhis ayrıntısı sızdırmaz** (hangi tablo/hata/şema sürümü — bu bilgi saldırgana yarar, kullanıcıya yaramaz); yalnız "çalışıyor / sorun var" + "bu senin hatan değil" der | `frontend/src/sistem-durumu.test.jsx` **6 test:** sağlıklı sistem, 503 → sonuç olarak okunur, ağ tamamen ölüyken çökmez, **ayrıntı sızmaz** (şema sürümü/`migration` metni yanıtta olsa bile ekrana çıkmaz), Authorization başlığı göndermez, yeniden-kontrol ucu tekrar sorar. Doğrulama: **151 vitest** (+6) | ✅ |

| BUG #254 | **Şifre politikası kişiye özel tahmini görmüyordu; blocklist inceydi (P2 kalan maddesi).** BUG #187 politikayı "yalnız uzunluk"tan kurtarmıştı ama iki boşluk vardı: (a) blocklist **30 kayıttı** — sızıntı listelerinin tepesindeki `1q2w3e4r`, `letmein123`, `qwertyuiop`, `changeme` ve TR varyantları içinde yoktu; (b) **kişiye özel tahmin hiç denetlenmiyordu**: saldırgan e-postayı zaten bilir ve hedefli denemede İLK sırada `<e-posta-adı>+rakam` kalıbını dener — `ali@example.com` kullanıcısının `ali12345` şifresi genel blocklist'te olmadığı için politikadan GEÇİYORDU. Blocklist 108'e çıkarıldı (global + TR), ardışık-dizi kuralı (`abcdef`, `123456`, `qwertyui`) eklendi, ve kimlik kontrolü **"içeriyor mu" DEĞİL** "harf çekirdeği kimlikle BAŞLIYOR mu" olarak tanımlandı — çünkü `ali` parçası `Kaliteli!9x` içinde de geçer ve güçlü bir şifreyi haksız yere reddetmek kapıyı ürün-kırıcı yapardı (L6). Ayrıca bu turda P2'nin "KALAN" listesi KOŞTURULARAK doğrulandı: rate-limit çok-worker, OAuth state çok-worker + PKCE, refresh rotasyonu ve register enumerasyonu maddelerinin dördü de ÇOKTAN kapanmıştı — belge bayattı (L17'nin ters yönü: bitmiş işi açık göstermek de gelecekteki oturumu yanlış yere sürer) | `tests/auth/test_sifre_politikasi_kisisel.py` **22 test:** blocklist kapsam tabanı (≥100 + iki dünyanın temsili), 9 yaygın şifre reddi, kimlikle başlayan 4 varyant (büyük harf dahil), **3 güçlü şifrenin haksız reddedilmemesi**, 4 ardışık dizi, ve **kayıt UCUNDA** aynı politikanın uygulanması (politika yalnız fonksiyonda kalırsa kapı yarımdır). Doğrulama: **2032 passed, 18 skipped** | ✅ |

| KAPI (bug değil) | **Kullanım turu değişmezleri — statik denetimin göremediği sınıf için KALICI kapı.** Bu turda kapanan üç kullanıcı bildirimi (#232 pasif kural görünmüyordu, #233 Google hesabına şifre belirlenemiyordu, **#241 tahsilat nakde geçmiyordu**) aynı şeyi söylüyordu: tek tek uçlar DOĞRUYKEN bir DİZİ işlemin sonunda para tutmuyordu ve hiçbir statik kapı bunu göremiyordu. `tests/test_kullanim_turu_degismezleri.py` "bir günlük kullanım"ı uçlar üzerinden koşturur (gider → kart harcaması → gelir → alacak kaydı → **tahsilat** → borç ödemesi → kart ödemesi → tutar düzeltme → hesap taşıma → silme → fiyat güncellemesi) ve HER adımdan sonra üç değişmezi ölçer: **D1 muhasebe kimliği** (cockpit'in Görülen Net Değer'i, hesap bakiyelerinden BAĞIMSIZ hesaplananla eşit — panel ile motor ayrışamaz), **D2 beklenen delta** (her işlemin net değere etkisi önceden bilinir; tahsilat ve kart ödemesi net-NÖTR, gider −tutar…), **D3 hiçbir panel çökmez** (18 uç, BUG #219 sınıfı). Ayrıca geri-al döngüsü (3× işaretle/geri al) para üretmiyor mu, boş kullanıcıda paneller açılıyor mu, silme sonrası etki geri sarılıyor mu | **6 test.** Kapının ÖLÇTÜĞÜ mutasyonla kanıtlandı: BUG #241 fix'i geri alınınca tur kırmızı oluyor ve hatayı insan diliyle söylüyor — `[alacak tahsili] D2 KIRILDI: net değer 19250.0 → 19250.0 (delta +0.00, beklenen +2000.00)`. Doğrulama: **2038 passed, 18 skipped** | ✅ |

| ÖLÇÜM (bug değil) | **"Süit canlı veriye dokunmuyor" iddiası ÖLÇÜLDÜ.** Canlı DB'nin `error_logs` tablosunda test kalıntıları bulundu (`/api/_test_hata/patla`, `_fake_send() takes 2 positional…`) — BUG #078 (conftest canlı engine) ve BUG #235 (üretim `app`'ine kalıcı test ucu) defektlerinden kalma tarihsel izler. "Artık dokunmuyor" cümlesi bir İDDİA'dır (R3): `scripts/suite_db_izolasyon_kontrolu.py` canlı DB'nin KOPYASINI alıp süiti ona karşı koşturuyor ve öncesi/sonrası TÜM tabloların satır sayılarını karşılaştırıyor. **Sonuç: TEMİZ** — 2040 test sonrası 31 tabloda 0 satır değişikliği. Ayrıca `TestClient(app)` kurup `get_db` override etmeyen 7 dosya tek tek ölçüldü, hiçbiri yazmıyor | Script + runbook bölümü (şüphelenince koşulur). Bu bir kapı değil ÖLÇÜM aracıdır: süiti süitin içinden koşturmak özyineleme olur | ✅ |

| ADR-043 (belge+kapı) | **"Oturum sabitlemesi" kararı uygulanmıştı ama YAZILI DEĞİLDİ (P2.1).** Güvenlik review'unun bu başlığı açık duruyordu; karar kodda vardı (JWT + `token_version` + refresh rotasyonu + logout'ta `jti` iptali) ama hiçbir yerde gerekçesi yazılı olmadığı için gelecekteki bir değişiklik onu bilmeden bozabilirdi. ADR-043: klasik session fixation **sunucu-taraflı oturuma özgüdür**, bu mimaride uygulanamaz (token yalnız kimlik doğrulamada üretilir, her üretim yeni `jti`); fixation'ın GERÇEK zararı — "eski kimlik yetkili kalır" — yetki-değiştiren olaylarda `token_version` artırılarak kapatılır. localStorage bilinçli kabul edilmiş risk olarak AÇIKÇA yazıldı; httpOnly çerez + CSRF alternatifi gerekçeyle Wave-10'a ertelendi | `tests/auth/test_adr043_oturum_sozlesmesi.py` **5 test:** ADR'nin kanıt tablosu boş değil (≥5 atıf), **ADR'de adı geçen her test GERÇEKTEN var** (belge kendisini ölçen koşumdan koparsa iddiaya döner — L17), kabul edilen risk açıkça yazılı, ve davranış: şifre değişince eski access token ÖLÜR + her giriş yeni `jti` üretir. ADR index'e 041/042/043 satırları eklendi (index bayattı). Doğrulama: **2045 passed, 18 skipped** | ✅ |

**Ledger notu (2026-08-05):** bu dosya P1 turundan sonra geride kalmıştı; BUG #166-#210 arası
publish-yolu düzeltmeleri **`masterprompt-publish.md` §11 durum tablosunda** kayıtlı (tek doğruluk
kaynağı orası). Bu bölüm ledger'ı yeniden bağlar; §11 ile çelişki çıkarsa §11 esas alınır.

## Tur — H4 PARA BİRİMİ + KAYIT BORÇLARI (7 Ağu 2026)

| # | ID | Değişiklik | Dosya | Doğrulama | Durum |
|---|----|-----------|-------|-----------|-------|
| — | YANILGI-1/2/3/4/5/7 | **Kayıt borçları yazılı hale geldi:** MCP graph "tarihsel arşiv" ilan edildi (186 satırlık flush bilinçli olarak YAPILMADI — ikinci gerçek kaynak üretmez), milestone/tag disiplininin 18 Tem'de bırakıldığı yazıldı, coverage %92→%93, backlog 520→521, tek bug envanteri ilan edildi | `PROJE.md`, `masterprompt-publish.md` §11.0, `backlog.md`, `uygulanan-fixler.md` | `tests/test_kayit_kararlari_kapisi.py` **13 test** (karar cümleleri diskte + bayat iddia geri gelirse kırmızı + devir belgesi var + mutasyon: cümle silinince KIRMIZI) | ✅ |
| — | BUG #255 | **`scripts/mcp_sync_report.py` HER durumda 0 dönüyordu** — "186 commit birikmiş" ile "birikme yok" aynı çıkış kodunu veriyordu (hep-başarılı araç, BUG #248 sınıfı / L28). Eşik (50) aşılınca 2 döner; `--temizle` bilinçli kapatma yolu | `scripts/mcp_sync_report.py` | `tests/test_kayit_kararlari_kapisi.py::test_mcp_ledger_esigi_gercekten_olculuyor` (boş=0, az=0, çok=2) | ✅ |
| — | **BUG #256** | **PARA BİRİMİ TEK KAYNAK (H4, ADR-044).** Biçimlendirme **yedi ayrı yerde** kodluydu (`_tl`, `_fmt`, `{x:,.0f}` [İngilizce ayraç!], `api.js formatTL`, `DebtStrategy` yerel `TL()`, iki modal). Etiket 167 backend sabiti + 91 frontend satırında elle yazılıydı; `user_currency()` üretimde hiç çağrılmıyordu. **En kritik yarısı:** `grounding.py` deseni `TL` literaline gömülüydü → etiket değişse doğrulama `{"ok": True}` ile **sessiz-yeşile** düşerdi; ayrıca koç bağlamının yatırım K/Z ve kart borcu satırları **etiketsiz** tutar yazıyordu, yani hiç denetlenmiyorlardı. Fix: `app/money_format.py` + `frontend/src/lib/money.js` tek kaynak; 87+45+14 çağrı yeri dönüştürüldü; grounding etiketleri tek kaynaktan alır + yeni `etiketsiz` alanı kırmızı yapar | `app/money_format.py`, `app/grounding.py`, `frontend/src/lib/money.js` + 18 backend + 24 frontend dosyası | `tests/test_para_birimi_kapisi.py` **10**, `tests/test_grounding_para_birimi.py` **15**, `frontend/src/money.test.js` **8**. **Mutasyon 3/3:** etiket `TL→XX` (backend) KIRMIZI, `etiketsiz` denetimi kaldırılınca KIRMIZI, frontend etiketi `TL→XX` KIRMIZI. Tam süit **2084 passed, 18 skipped** + **151 vitest** + `npm run build` OK | ✅ |
| — | **BUG #257** | **PROMPT ENJEKSİYONU — YAPI SAVUNMASI (H9, ADR-045).** Ölçüldü: kullanıcının yazdığı alanlar (hesap adı, kırmızı çizgi başlığı/açıklaması, kategori, işlem açıklaması, karşı taraf, hedef) koç bağlamına **HAM** giriyordu; `"Nakit

## SISTEM TALIMATI
..."` adlı bir hesap, bağlamda gerçek bölümlerden **ayırt edilemeyen** yeni bir bölüm açıyordu (kanıt: bağlam satır 27 ve 46). Paylaşılan workspace'te (ADR-036) metni yazan ile ona maruz kalan farklı kişilerdir → dolaylı enjeksiyon. **Sınıf taraması (L11)** kalıcı yolu buldu: koçun `save_insight` ile yazdığı metin DB'de kalıp SONRAKİ oturumda kendi bağlamına dönüyordu. Fix: `app/prompt_safety.guvenli_metin` — yalnız YAPI taşıyan işaretler nötr (satır sonu, `##`, kod çiti, `<\|im_start\|>`/`[INST]`, baştaki `Sistem:` etiketi, görünmez/kontrol karakterleri); **sansür yok**, içerik korunur. Insight yolunda `azami=0` (kesme yok — token bütçesi zaten "komple düşür" stratejisi uyguluyor, ikinci kırpma onu sessizce bozardı) | `app/prompt_safety.py`, `app/coach.py`, `app/premortem.py`, `app/coach_insights.py` | `tests/test_prompt_injection_kapisi.py` **15 test**: sahte bölüm açılamıyor, metin sansürlenmiyor (L6), 5 yapı-token vakası, görünmez karakter, premortem prompt'u, kalıcı hafıza yolu, **kapsam tabanı** (`_guvenli` sarmalı ≥12 alan + sarılmamış alan taraması), **mutasyon** (sanitizer devre dışı → KIRMIZI). Tam süit **2105 passed, 18 skipped** | ✅ |
| — | **BUG #258** (SEC-009) | **KALICI hata metni maskesizdi.** `error_tracking.temizle` vardı ama yalnız LOG zincirine bağlıydı (BUG #244); DB'ye yazan üç yol (`ApiCallLog.error_message`, `reasoning_traces.error`, `scheduler_runs.detail`) yalnız KISALTIYORDU. Kısaltmak maskelemek değildir: sağlayıcı istisnası isteğin kendisini taşır (`...?key=AIza...`, `Bearer ...`, SMTP bağlantı dizesi) ve `ApiCallLog` **KVKK dışa aktarımına** girer. İkinci körlük: maske listesi ETİKETE bakıyordu (`api_key=`), değerin ŞEKLİNE değil → `?key=AIza...` geçiyordu | `app/llm_quota.py`, `app/reasoning_trace.py`, `app/scheduler.py`, `app/error_tracking.py` | `tests/security/test_kalici_hata_maskeleme.py` **11 test** (6 anahtar şekli + masum metin bozulmuyor + üç yazma sınırı davranışla + kapsam). **Mutasyon 3/3 KIRMIZI** (üç yolda da maskeleme geri alındı) | ✅ |
| — | **BUG #259** (SEC-005) | **Güvenlik başlıkları TEK katmandaydı (H22 ihlali).** HSTS/CSP/X-Frame/nosniff/Referrer yalnız nginx şablonunda (`deploy/nginx*:76-80`); `app/` içinde 0 eşleşme. Vekil atlanırsa (systemd yolu — ADR-035 "alternatif", doğrudan port, tünel, vekilsiz staging) korumanın TAMAMI kayboluyordu. Aynı ders BUG #213'te gövde sınırı için öğrenilmiş, başlıklar atlanmıştı | `app/security_headers.py`, `app/main.py` | `tests/security/test_guvenlik_basliklari.py` **11 test**: 7 başlık uygulama katmanında, hata yanıtlarında da var, HSTS yalnız HTTPS (L6 — yerel geliştirmeyi kilitlemez), **vekilin başlığı ezilmiyor**, **nginx↔uygulama küme eşitliği** (ayrışma kapısı, L27) | ✅ |
| — | **BUG #260** (SEC-020/021) | **Bağımlılık taraması CI'da YOKTU.** P2'de pip-audit elle koşulup 23→0 yapılmıştı; `grep -rl "pip-audit" .github/` → **0 sonuç**. Bağımlılık açığı kod değişmeden çıkar → o "0" yazıldığı gün doğruydu, ertesi gün ölçülmüyordu (**L28**) | `.github/workflows/ci.yml` | `tests/security/test_bagimlilik_tarama_kapisi.py` **5 test**: pip-audit `--strict` + npm audit eşiği + **haftalık cron** + ayrı job (test kırmızıyken tarama atlanmasın) + requirements esnek-pin **ratchet'i** (9, artamaz) | ✅ |
| — | **BUG #261** (SEC-018) | **Sır sızma denetimi hiç yapılmamıştı** ("gitleaks kanıtı yok"). `.gitignore` doğru olsa bile tek bir `git add -f` ya da örneğe yapıştırılmış gerçek anahtar yeter; **git geçmişi silinmez** — repo bir gün açılırsa geçmiş de açılır. Ölçüm: `.env` geçmişte HİÇ commit edilmemiş + tüm blob'lar tarandı → **TEMİZ**. İlk sürüm 13 yanlış-pozitif üretti (`postgres:postgres@localhost`, maskeleyicinin kendi regex'i) → desen daraltıldı (L22). Geçmiş taraması `git cat-file --batch` ile 10 dk+'dan **8 sn**'ye indi — koşulmayacak kadar yavaş kapı, kapı değildir | `scripts/sir_taramasi.py`, `scripts/sir_taramasi_baseline.txt`, `.github/workflows/ci.yml` | `tests/security/test_sir_taramasi_kapisi.py` **7 test**: gerçek anahtar şekli yakalanıyor, yer-tutucu/yerel URL gürültü üretmiyor, muafiyet işareti (aynı/üst satır), ağaç+geçmiş temiz, baseline gerekçeli, CI'a bağlı, `.env` geçmişte yok. Tam süit **2139 passed** | ✅ |

## Tur — P3.3 ONBOARDING REHBERİ (7 Ağu 2026)

| # | ID | Değişiklik | Dosya | Doğrulama | Durum |
|---|----|-----------|-------|-----------|-------|
| — | **BUG #262** (P3.3) | **İLK KURULUM REHBERİ İKİ YERDEN KIRIKTI — ikisi de SESSİZ (L28).** §11 P3 satırı "kalan: onboarding rehberi" diyordu, H20 satırı ise "✅ Cockpit'te boş-durum kartı (sıralı yol)" diyordu; ölçünce ikisi de yarım çıktı. **(a) Rehber ilk adımdan sonra ORTADAN KAYBOLUYORDU:** kart yalnız `bosMu = accounts.length === 0` iken çiziliyordu → kullanıcı ilk hesabını ekler eklemez kalan üç adım (işlem gir → kendi kuralını yaz → koça sor) hiç yönlendirilmiyordu; "sıralı yol" tek bir cümleydi, rehber değildi. **(b) Kartın BİRİNCİL DÜĞMESİ ÖLÜYDÜ:** `<a href="#accounts">` — uygulama hash-router kullanmıyor (`App.jsx` `activeTab` state'i), yani yeni kullanıcının gördüğü İLK düğme hiçbir şey yapmıyordu. Tarayıcı hata vermez, süit yeşil kalır. Sınıf taraması (L11): `href="#` frontend'de **tam olarak bu bir yerde** geçiyor, başka ölü çapa yok. Fix: adımın "tamam" olup olmadığı bir VERİ sorusudur → `GET /api/onboarding/rehber` tek kaynakta karar verir (ADR-001 ruhu), arayüz yalnız çizer; her adım kendi `sekme` kimliğini taşır ve düğme `setActiveTab`'ı çağırır. **Demo satırları adım saymaz** — "örnek veriyle gez" diyen kullanıcı için rehber aksi halde anında 3/4 tamam görünüp kaybolurdu. Gizleme kalıcı (`users.onboarding_dismissed_at`) ve **geri alınabilir** (Hesap paneli) — geri dönüşü olmayan "kapat" kullanıcıyı kilitler. **Yan bulgu:** `require_write()` router seviyesindeydi → OKUMA uçları da yazma yetkisi istiyordu; paylaşılan workspace'te `viewer` üye rehberi/demo durumunu 403 alıyor, frontend'de sessizce yutuluyordu. Guard yalnız POST/DELETE'e taşındı | `app/routers/onboarding.py`, `app/models.py`, `alembic/versions/a3b4c5d6e7f8_onboarding_rehber.py`, `frontend/src/components/Onboarding.jsx`, `frontend/src/panels/Cockpit.jsx`, `frontend/src/panels/Hesap.jsx`, `frontend/src/api.js` | `tests/test_onboarding_rehber.py` **10 test** (4 adım, ilk adımdan sonra kaybolmaz, demo tamam saymaz, sıralı ilerleme + emeklilik, koç adımı yalnız KULLANICI mesajıyla, gizle/geri-al kalıcılığı) + **ölü CTA kapısı**: her adımın `sekme` değeri `App.jsx`'ten TÜRETİLEN gerçek sekme kümesinde olmalı (liste elle taşınmaz — L27). `frontend/src/onboarding-rehber.test.jsx` **9 test** (düğme gerçekten sekme değiştirir, rehberde `a[href^="#"]` YOK, tamamlanan adımda düğme kalmaz, ağ ölünce çökmez). **Mutasyon 4/4 KIRMIZI:** eski `gorunur` ölçütü geri → kırmızı; demo dışlaması kaldır → kırmızı; sahte sekme adı → kırmızı; `<a href="#...">` geri getir → kırmızı. Sözleşme-kayması kapısı yeni ucu **kendiliğinden yakaladı** (fixture `FOS_FIXTURE_UPDATE=1` ile tazelendi). Tam süit **2150 passed, 18 skipped** + **168 vitest** (+9) + `npm run build` OK + `scripts/test_fresh_db_migration.py` OK | ✅ |

## Tur — P5.5 KAPASİTE SINIRLARI (7 Ağu 2026)

| # | ID | Değişiklik | Dosya | Doğrulama | Durum |
|---|----|-----------|-------|-----------|-------|
| — | **BUG #263** (P5.5) | **KAPASİTENİN YALNIZ DIŞARI BAKAN TARAFI HESAPLANMIŞTI.** `app/database.py`'deki muhakeme Postgres `max_connections` tarafını doğru kuruyordu (`2 worker × 15 + scheduler = 45 < 100`) ama uygulamanın KENDİ eşzamanlılığını hiç saymamıştı: 114 HTTP ucunun **tamamı senkron** (`def`) → her istek anyio iş parçacığı havuzunda koşar (varsayılan **40**) ve `Depends(get_db)` oturumu **isteğin sonuna kadar** bağlantıyı tutar. Yani havuz Postgres'e ulaşmadan **uygulamanın içinde** tükeniyordu (40 ≫ 15). Kaskad: koç isteği iki LLM çağrısı yapar (iki-geçiş) ve 10-40 sn bağlantı işgal eder → 15 eşzamanlı koç havuzu kilitler → 16. istek `pool_timeout` (**örtük 30 sn**) bekleyip **500** alır → `/api/ready` de havuzdan bağlantı istediği için asılır → Docker HEALTHCHECK zaman aşımı → **konteyner yeniden başlar**, uçuştaki tüm istekler ölür. Yani geçici bir yük dalgası KALICI kesintiye dönüyordu. Fix: `app/capacity.py` tek kaynak — (a) iş parçacığı havuzu açılışta DB havuzuna hizalanır, (b) yavaş yollar (LLM / dış-servis / e-posta) adlandırılmış tavanlarla sınırlanır ve tavan doluysa istek **beklemez**, 503 + `Retry-After` alır (beklemek iş parçacığını VE DB bağlantısını tutmaya devam etmektir), (c) değişmez açılışta denetlenir ve production'da **fail-fast** eder. **Sınıf taraması (L11) üç kör nokta daha buldu:** ① `premortem` ucu da LLM çağırıyordu ve koç için kurulan tavanı **tamamen atlıyordu**; ② sekiz LLM sağlayıcısından yalnız Ollama'nın timeout'u vardı — diğer yedisi SDK varsayılanındaydı (**600 sn**), yani "tavan" tutan çağrının bir sonu olduğu varsayımı yanlıştı; ③ `services/oauth.py`'de **hiç** timeout yoktu (`requests` sınırsız asılır) ve SMTP gönderimi de tavansızdı. **Yan bulgu (belge):** runbook'un küçük-VM tarifi (`DB_POOL_SIZE=3` + `DB_MAX_OVERFLOW=5` = 8) yeni değişmezi ihlal ediyordu (3+3+6=12 > 8) → tarifi harfiyen uygulayan operatörün konteyneri **hiç açılmazdı**; tarif tutarlı takıma çevrildi ve **belgeden okunarak** koşulur hale getirildi. **Kararlar:** e-posta yolu ayrıdır (EVDS çökmesi şifre-sıfırlamayı engellememeli) ve tek bekleme istisnasıdır (düşen sıfırlama e-postası kullanıcıyı hesabının dışında bırakır); reddedilen koç isteği kullanıcının günlük kotasını **yakmaz**; 503 sırt basıncıdır, `error_logs`'a "beklenmedik hata" olarak **yazılmaz** | `app/capacity.py` (yeni), `app/main.py`, `app/database.py`, `app/routers/meta.py`, `app/routers/coach.py`, `app/routers/premortem.py`, `app/routers/prices.py`, `app/routers/auth.py`, `app/coach.py`, `app/services/oauth.py`, `app/services/email.py`, `docs/deployment/runbook.md`, `.env.prod.example` | `tests/test_kapasite_kapisi.py` **22 test**: değişmez (varsayılan tutarlı, tavan havuzu yiyemez, havuz>eşzamanlılık, production fail-fast, dev kilitlenmez, SQLite muaf), `uygula()` anyio limiter'ını **gerçekten** değiştirir (portal ile ölçüldü), tavan dolunca beklemez, slot istisnada geri verilir, yollar birbirini kilitlemez, uçtan uca: nazik 503 + `Retry-After`, **kota yakılmaz**, **`error_logs`'a yazılmaz**, yavaş yol doluyken **hızlı uçlar çalışır**, `/api/ready` havuz doygunken **hızlı** 503, e-posta yolu (False döner/patlamaz, slot boşalınca gönderim sürer, tek bekleme istisnası), **kapsam tabanı** (her yol adının tavanı var + env adları operatör belgesinde geçiyor + runbook tarifi değişmezi sağlıyor). **Mutasyon 3/3 KIRMIZI:** e-posta tavanı kaldır → kırmızı; bekleme yok say → kırmızı; runbook tarifini tutarsızlaştır → kırmızı. Tam süit **2173 passed, 18 skipped** | ✅ |
| — | BUG #263 (yan) | **`tests/test_gemini_provider.py` sahte SDK'sı beyaz-liste ile donmuştu** — üretim kodu `genai_types.HttpOptions` kullanınca (timeout için) altı test `AttributeError` ile kırıldı; yani test, ölçmesi gereken fallback mantığı yerine kendi sahtesinin eksikliğini raporladı. Sahte modül artık adları `__getattr__` ile otomatik üretir; **gerçek** SDK yüzeyi ayrı bir kapıyla denetlenir (aksi halde olmayan bir sembolü kullanmak süiti yeşil bırakır ve hata yalnız canlıda çıkar) | `tests/test_gemini_provider.py` | Aynı dosyada **7 test** (6 mevcut + `test_kullanilan_genai_sembolleri_gercek_SDK_de_VAR`: `app/coach.py`'de geçen her `genai_types.X` kurulu `google-genai` 1.73.1'de VAR) | ✅ |

## Tur — P3.5.3 KULLANICI KATEGORİLERİ (7 Ağu 2026)

| # | ID | Değişiklik | Dosya | Doğrulama | Durum |
|---|----|-----------|-------|-----------|-------|
| — | **BUG #264** (P3.5.3, ADR-046) | **KULLANICININ PARASIYLA İLGİLİ İKİ KARAR, SABİT TÜRKÇE KATEGORİ ADLARINA BAĞLIYDI.** ① `action_executor._CARD_CATEGORIES = {"yemek","eglence","sigara","alisveris","market"}` bir harcamanın **kredi kartına** yazılıp yazılmayacağını belirliyordu — yani "hangi hesaptan çıktı" sorusunun cevabı, kullanıcının kategorisini hangi kelimeyle adlandırdığına bağlıydı. Kendi setini kuran kullanıcıda ("gıda", "market alışverişi") kural **sessizce ölüyor** (L28); tersi de doğru: "market" adını kullanıp o harcamayı NAKİT yapan kullanıcının parası kart borcuna yazılıyor ve **iki bakiye birden** yanlış çıkıyordu. ② `rules_engine._PATTERN_EXCLUDED_CATEGORIES` muhasebe işlemlerini harcama-paterni analizinden ada bakarak çıkarıyordu → kullanıcı "borç kapama" dediği an dışlama ölüyor ve borç ödemesi "harcaman arttı" uyarısını tetikliyordu. **Sınıf taraması (L11) bugün canlı olan bir defekt buldu:** hızlı girişin ürettiği `borc_geri_odeme` slug'ı dışlama listesinde **hiç yoktu** — tek-kullanıcı kurulumunda bile borç ödemeleri kişisel harcama sayılıyordu. ③ Arayüzde **üç ayrı, birbirinden FARKLI** sabit liste vardı (`Transactions.jsx` 9 kalem, `IncomeDebt.jsx` 7 kalem, `Budget.jsx` placeholder) — aynı uygulama üç gerçeklik gösteriyor, kullanıcı setini hiçbir yerde kuramıyordu. **Fix:** kategori bir KAYIT oldu (`categories`; `slug`/`ad`/`kart_varsayilani`/`sistem`/`gizli`, defter kapsamlı). Kod hiçbir kararı ADA bağlamaz, tek kaynak `app/category_rules.py` bayrağı okur. `Transaction.category` bilinçli olarak **serbest metin KALDI** (FK yok): koç/hızlı giriş metinden kategori türetir, FK bilinmeyen kategoride kaydı reddetmeye zorlar ve kullanıcının işlemi kaybolurdu (sessiz RET, sessiz kabulün ikizi). Silme = **hedefe taşı** (Actual Budget deseni) ya da **gizle** (YNAB `hidden`); sistem kategorisi silinemez/yeniden adlandırılamaz (YNAB `internal`). Göç davranışı **değiştirmez**: her defterde varsayılan set tohumlanır + o defterde geçen kategoriler kayda çevrilir + bayraklar ESKİ sabit kümelerden türetilir — değişen tek şey **sahiplik**. **D1 araştırması** `research-log.md`'de (YNAB API `internal`/`hidden`, Actual Budget silme→taşıma akışı) | `app/category_rules.py` (yeni), `app/routers/categories.py` (yeni), `app/models.py`, `app/action_executor.py`, `app/rules_engine.py`, `app/services/workspace_setup.py`, `app/routers/workspaces.py`, `app/main.py`, `alembic/versions/b4c5d6e7f8a9_kullanici_kategorileri.py`, `frontend/src/lib/categories.js` (yeni), `frontend/src/components/CategoryManager.jsx` (yeni), `frontend/src/panels/{Transactions,IncomeDebt,Budget}.jsx`, `frontend/src/api.js`, `docs/architecture/adr-046-kullanici-kategorileri.md` | `tests/test_kullanici_kategorileri.py` **26 test**: göç kanıtı (varsayılan set = eski `_CARD_CATEGORIES` birebir; sistem kümesi eski dışlamayı KAPSAR + `borc_geri_odeme` defekti kapandı), kayıt yoksa belgeli varsayılan, **okuma yolu DB'ye YAZMAZ** (rules_engine sözleşmesi, `after_flush` dinleyicisiyle ölçüldü), kart varsayılanı kapatılınca para karta yazılmaz, kullanıcının KENDİ kategorisi kart varsayılanı olabilir, tanımsız kategori karta yazılmaz, varsayılan kurulumda eski davranış aynen sürer, `borc_geri_odeme` paternden dışlanır, normal kategori paternde KALIR, tohumlama idempotent + kullanıcı düzenlemesini ezmez, API (gizli filtresi, slug normalize [BUG #167 Kiril kontrolü], 409 çift, gizli→geri açılır, sistem korumaları, hedefsiz silme 409, hedefli silme işlemleri+zarfı taşır, bilinmeyen hedef 404, **başka kullanıcının kategorisi görünmez/silinemez** — L1). `tests/test_kategori_kapisi.py` **249 test** (parametrize): üretim kodunda kategori slug'ı literal koleksiyonu YASAK (AST, kapsam tabanı + muafiyet TAVANI 2), kaldırılan sabitler geri gelemez, `kart_varsayilani`/`sistem` bayrağını yorumlayan dosya kümesi kilitli, **frontend aynası** (panelde sabit liste yasağı + `COMMON_CATEGORIES`/`EXPENSE_CATEGORIES` geri gelemez). **Mutasyon 6/6 KIRMIZI:** kart kararı hep-True → kırmızı; sistem kümesi boş → kırmızı; hedefsiz silme serbest → kırmızı; backend'e sabit küme ekle → kapı kırmızı; panele sabit liste ekle → kapı kırmızı; `EXPENSE_CATEGORIES` geri getir → kapı kırmızı. **Projenin kendi kapıları yeni tabloyu/uçları kendiliğinden yakaladı (5 kırmızı):** KVKK sınıflandırması (`categories` kullanıcı verisidir → export + silme), izolasyon matrisi (`PATCH`/`DELETE /api/categories/{id}` — B, A'nın kategorisini alamaz; silme işlemleri TAŞIDIĞI için ele geçirme veri kaybı da demektir), boş-durum fixture'ı ve temiz-DB şema kilidi (`ix_categories_id`). Tam süit **2451 passed, 18 skipped** + **168 vitest** + `npm run build` OK + `scripts/test_fresh_db_migration.py` OK (31 tablo) | ✅ |

## Tur — İKİ TEMA + TELEFON YÜZEYİ (7 Ağu 2026)

| # | ID | Değişiklik | Dosya | Doğrulama | Durum |
|---|----|-----------|-------|-----------|-------|
| — | **BUG #265** (ADR-047, UX-020/UX-040/FE-008) | **UYGULAMANIN İKİNCİ GÖRÜNÜMÜ HİÇ ÖLÇÜLMEMİŞTİ.** `darkMode:'class'` iki ayrı sayfa üretir; 168 vitest ve 4 e2e'nin hiçbiri açık temayı ya da telefon genişliğini render etmiyordu. 390×844'te 13 panel × 2 tema ölçülünce: **dört panel tamamen koyu-varsayan yazılmış** (`Goals` 33, `DebtStrategy` 28, `Workspace` 27, `Login` 13 kullanım) → açık temada **başlık görünmüyor** ("Hedefler" kontrast **1.05**), boş-durum yazıları **1.27**, Aile üye satırları **1.22/1.42**; ikincil metin (`text-zinc-400`) beyaz üstünde **2.46–2.56**. Ters yönde **koyu** temada grafik serisi + Recharts lejant metni `#4f46e5`/zinc-900 = **2.82** — yani VARSAYILAN temada net-değer grafiği okunmuyordu. Renk kararı temayı bilmiyordu. **Statik tarama bu tabloyu üretemedi:** ilk tarayıcı `className="…"` yakalamasını yanlış kuruyordu, 128 kullanımın **123'ünü** kaçırdı ve "5 bulgu, temiz" dedi (L27'nin bu turdaki yüzü — ikinci görünüm ancak RENDER EDİLİNCE ölçülür). **ADR-010 çürütüldü:** "global `.btn` class gelecekteki butonları da 44px yapar" gerekçesi ölçülünce yanlış çıktı — `.btn` kullanmayan kontroller 42px (üst sekmeler), 35px (`.input` + üç filtre `select`'i), 28px (akış aralığı), 20px (lejant), 13px (onay kutusu). **Fix:** (a) 128 koyu-varsayan sınıf tema-duyarlı çifte çevrildi + `-500` tonu açık zeminde 3:1 altına düşen 9 hue düzeltildi; (b) grafik renkleri tek kaynağa alındı (`lib/grafikRenkleri.js`) ve **iki temada da ≥3:1** veren tek değerlerle seçildi (Recharts rengi SVG özniteliği olarak alır — CSS değişkeni çözülmez, tema aboneliği rengin ikinci gerçek kaynağını üretirdi); (c) `.input` ve altı kontrol grubu 44px'e çıkarıldı; (d) Aile panelinin üç ham girdisi paylaşılan `.input`/`.btn` sınıflarına bağlandı. Dönüşümün yan ürünü (`A dark:B dark:C` çakışması, 38 yer) ayrıca temizlendi | `frontend/src/lib/grafikRenkleri.js` (yeni), `frontend/src/index.css`, `frontend/src/App.jsx`, `frontend/src/panels/{Goals,DebtStrategy,Workspace,Login,Cockpit,Cashflow,Coach,IncomeDebt,Transactions,Budget,RedLines,Reports}.jsx`, `frontend/src/components/{BalanceTrend,CashflowCalendar,CashflowSummary,CashflowSankey,CategoryManager,CommandPalette,DetectedSubscriptions,EmptyState,FeedbackWidget,HelpModal,HorizonsModal,MetricCard,NetWorthAnalysis,Onboarding,PendingActions,PremortemModal,SistemDurumu,Toast,TracePanel,Wishlist}.jsx`, `docs/architecture/adr-047-iki-tema-tek-yuzey.md`, `docs/architecture/adr-010-*.md` (gerekçe düzeltmesi) | `frontend/e2e/tema-mobil.spec.js` **2 test × 13 panel × 4 değişmez** (kontrast ≥3:1, yatay taşma yok, dokunma hedefi ≥44px + iki YAZILI istisna [cümle içi kontrol = WCAG 2.5.8, label'a sarılı onay kutusu], konsol hatası yok). **Mutasyon 3/3 KIRMIZI:** başlık `text-zinc-100`'e geri → kırmızı; `.input`'tan `min-h-[44px]` kaldır → kırmızı; grafik serisi `#4f46e5`'e geri → kırmızı. Tam süit **2452 passed, 18 skipped** (+1: `test_kategori_kapisi.py` frontend dosyalarını gezdiği için yeni `lib/grafikRenkleri.js`'i kendiliğinden kapsama aldı) + **168 vitest** + **6 e2e** + `npm run build` OK | ✅ |
| — | **BUG #265** (yan bulgu) | **BUG #241'İN KAPANIŞ KANITI CI'DA HİÇ YEŞİL OLMADI.** `e2e/tahsilat-nakde-gecer.spec.js` kimliksiz yazılmıştı (ne API çağrısında token, ne sayfada oturum); CI'nin e2e işi `AUTH_ENABLED=true` ile koşuyor → `POST /api/debts` **401** dönüyor ve spec ilk adımda ölüyordu. Yani kullanıcı-bildirimli bir PARA bug'ının kanıtı, eklendiği günden beri (6 Ağu) koşmuyordu. Kapanış kanıtı koşmuyorsa kanıt değildir (L20 ailesi: kapının kendi ön koşulu ölçülmemiş). Fix: spec `usage-loop` ile aynı izolasyona geçti — kendi taze kullanıcısı + kendi nakit hesabı + `Bearer` başlığı + sayfada `addInitScript` oturumu, sonunda KVKK-delete | `frontend/e2e/tahsilat-nakde-gecer.spec.js` | Aynı dosyadaki **2 test**, CI ile aynı ortamda (`AUTH_ENABLED=true`, taze DB) yerelde koşuldu → **yeşil**. Toplam e2e 4 → **6** | ✅ |

## Tur — AKSİYON PAYLOAD SÖZLEŞMESİ (7 Ağu 2026)

| # | ID | Değişiklik | Dosya | Doğrulama | Durum |
|---|----|-----------|-------|-----------|-------|
| — | **BUG #266** (LLM-008, ADR-048) | **LLM'İN ÜRETTİĞİ PAYLOAD HİÇ DOĞRULANMADAN ONAYA SUNULUYORDU.** `app/action_executor.py`'nin kendi ilkesi "LLM'in prompt'una güvenilmez, kod seviyesinde bloklanır" derken `propose_action` yalnız `action_type`'ı doğruluyor, payload'ın ŞEKLİNİ tamamen LLM'e bırakıyordu; tek kural sistem prompt'undaki "PAYLOAD ŞABLONLARINA uygun yaz" cümlesiydi. **Ölçüm (FakeProvider ile gerçek koç akışı):** ① `amount: "uc yuz yirmi"` bekleyen aksiyona YAZILDI ve kullanıcıya "320 TL market harcaması kaydedildi" özetiyle gösterildi — onaylanınca execute "amount sonlu ve pozitif olmalı" deyip **hiçbir şey yazmadı**, yani doğrulama VARDI ama YANLIŞ TARAFTAYDI (onaydan SONRA) ve kullanıcı olmayan bir kaydı doğru sandı. ② `summary="320 TL …"` + `payload={"amount": 3200}` **onaya gitti** — kullanıcının OKUDUĞU tutar ile UYGULANACAK tutar 10 kat farklıydı, hiçbir denetim yoktu. ③ Eksik anahtarlı tool argümanı (`{}`, `summary` yok, `payload` string) sessizce yutuluyordu: altı sağlayıcının hepsinde bozuk JSON `args = {}`'a düşüyor, `inp["action_type"]` KeyError atıyor, `recorder.step` onu yutuyor ve kullanıcıya alakasız "Hangi hesaptan harcadın?" dönüyordu. **Arayüz yarısı:** `add_transaction` payload'ı okunabilir tabloya çevriliyordu ama DİĞER ALTI TÜRDE (kart ödemesi, bakiye düzeltmesi, fon fiyatı, yatırım satışı, borç kapatma) payload KAPALI bir `<details>` içinde ham JSON'du → para hareketlerinde kullanıcı pratikte yalnız özeti görüp onaylıyordu. **Fix:** tek kaynak `app/action_schema.py` — her `action_type` için Pydantic şeması (`extra="forbid"`: bilinmeyen alan sessizce yutulmaz), para alanları `schema_types` sonlu-değer tipleri (SEC-032), eksik opsiyonel alan VARSAYILANLA DOLDURULMAZ (BUG #237 dersi), özet↔payload tutarlılığı (TR `19.700,50` = `19700.5`, 0,01 tolerans; para hareketinin özeti tutarı SÖYLEMEK zorunda — grounding ilkesinin onay yolundaki karşılığı), reddedilen öneri kullanıcıya GÖRÜNÜR mesajla döner (sessizlik "Kaydettim." cümlesini ekranda bırakır — BUG #049 ailesi). Prompt'taki `PAYLOAD ŞABLONLARI` **elle yazılı ÜÇÜNCÜ listeydi** → artık şemadan ÜRETİLİR; izin verilen değer kümesi alanın kendi tanımıyla yaşar (`json_schema_extra`) ki türetmede kaybolmasın. Arayüzde onaylanan alanlar açık gösterilir, ham JSON teşhis için `<details>`'te kalır | `app/action_schema.py` (yeni), `app/action_executor.py`, `app/coach.py`, `frontend/src/components/PendingActions.jsx`, `docs/architecture/adr-048-aksiyon-payload-sozlesmesi.md` | `tests/test_aksiyon_payload_kapisi.py` **23 test**: 7 geçersiz-payload davranışı, 5 özet↔payload, 3 regresyon, **kapsam tabanı** (her tür için şema var; handler'ın **AST'den çıkarılan** her payload anahtarı şemada var VE tersi — iki yönlü drift kilidi, L27; prompt şablonu şemadan üretiliyor + yer tutucu çözülmüş; boş payload'ı kabul eden tür YOK). `frontend/src/bekleyen-aksiyon-gorunurluk.test.jsx` **4 test** (tutar açık yazılır, hesap adı çözülür, sözlükte olmayan alan ham adıyla yine de görünür, bozuk JSON çökertmez). **Mutasyon 3/3 KIRMIZI:** şema doğrulaması kaldır → kırmızı; özet-payload denetimi kaldır → kırmızı; prompt şablonunu şemadan kopar → kırmızı. **Yan düzeltme:** koçun arayüze döndürdüğü `payload` HAM argümandı (DB'ye normalize hâli yazılıyordu) → artık DB'ye yazılan hâl döner. Tam süit **2478 passed, 18 skipped** + **172 vitest** + `npm run build` OK | ✅ |

## Tur — MESAJ NİYETİ + TÜRKÇE YAZIM (8 Ağu 2026)

| # | ID | Değişiklik | Dosya | Doğrulama | Durum |
|---|----|-----------|-------|-----------|-------|
| — | **BUG #267** (LLM-010, ADR-049) | **SORU SORAN KULLANICININ BİLDİRDİĞİ HARCAMA SESSİZCE KAYBOLUYORDU.** KURAL SIFIR'ın deterministik ön-filtresi `if is_question(msg): return False` diyordu — yani tek bayrak **iki bağımsız soruyu** cevaplıyordu ("bu mesaj bir şey soruyor mu?" / "gerçekleşmiş bir olay bildiriyor mu?"), oysa KURAL SIFIR yalnız ikincisine bakar. İnsanlar tam da böyle yazar: *"Bugün markette nakitten 320 TL harcadım, bütçem ne durumda?"* **Ölçüm (25 mesajlık korpus + FakeProvider ile uçtan uca gerçek koç akışı):** aynı payload'ı öneren sadık sağlayıcıyla "…harcadım" → **1 kayıt**, "…harcadım, bütçem ne durumda?" → **0 kayıt**; karışık sınıfta **7/7 yanlış**, uçtan uca **3/4 yanlış**. Zarar iki katmanlı ve ikisi de sessiz: ① harcama hiç kaydedilmez, ② koç soruyu **harcama öncesi** rakamlarla yanıtlar — cevap yanlıştır ama doğru görünür. Üstelik bu durumu kurtarmak için yazılmış ikinci mekanizma (BUG #127 retry'ı) da aynı bayrağa takılıyordu (`and offer_propose` guard'ı). **İkinci eksen — YAZIM (20 token):** desenler yalnız diakritikli hâli tanıyordu; telefon klavyesinde çok yaygın "odedim / dusunuyorum / degerlendir" yazımında kapı **başka türlü** karar veriyordu. Sinsilik kaynağı: CPython'un genişletilmiş katlaması `ı↔I↔i` eşitliğini KENDİLİĞİNDEN kurar, `ç/ş/ğ/ö/ü` için kurmaz → `harcadım` deseni "harcadim"i yakalar, `ödedim` deseni "odedim"i yakalamaz; sorun **harfe göre değişiyor**, test edilen örnekte tesadüfen çalışabiliyordu. **Sınıf taraması (L11) aynı defekti iki yerde daha buldu:** ① `_DATE_KEYWORD_RE` ay adlarını diakritiksiz görmüyordu ("subatta/agustosta/eylulde") → `TARIH_BELIRSIZ` koruması tetiklenmiyor ve işlem **sessizce bugüne** yazılıyordu (BUG #237/D17 ile aynı sonuç); kritik ayrıntı: bu desen sorunu **biliyordu** — `dün/bugün/geçen` için elle ASCII ikizi yazılmış, ay adları unutulmuştu (elle taşınan telafi eksik kalır). ② `QT_OPEN_PATTERN` `kac` yazıp `kaç`ı unutmuştu; bu sayaç **koçun kendi mesajlarını** ölçer ve koç düzgün Türkçe yazar → "Kaç lira ayırabilirsin?" açık soru sayılmıyor, MI/OARS oranı düşük görünüyor, "direktif tarz" uyarısı haksız tetiklenebiliyordu (L22 ailesi). **Fix:** sözleşme `propose_sunulsun = gerceklesmis OR (NOT soru AND NOT gelecek)` — tek kaynak `app/intent_rules.py`; karar üç bayrağı + **gerekçesini** taşır ve gerekçe reasoning trace'e düşer (BUG #253 ilkesi). Türkçe katlama tek kaynak `app/tr_text.py` (`category_rules` buradan re-export eder; `normalize` **uzunluk korur** → eşleşme ofsetleri ham metinde geçerli). Desenler **katlanmış** yazılır, metin eşleşme öncesi katlanır → "iki yazımı elle eklemek" işi ortadan kalktı (L26). **Sözleşmeyi düzeltmek listeyi büyütmeyi ucuzlattı:** veto koşullu olduğu için soru tespiti güvenle genişletildi ("sıralar mısın", "plan yaz"). Gerçekleşmiş-eylem listesi bilinçli DAR kaldı (jenerik "-dı/-di" alınmadı: "borcum arttı" kullanıcı eylemi değil ve bu bayrak retry'ı ZORLAR). **LLM-010'un iki özgün önerisi de REDDEDİLDİ** (gerekçeleri ADR-049): "geçmiş zaman varsa is_question=False zorla" konflasyonu ters çevirir; "küçük LLM intent classifier" deterministik olmayan bir katmanı güvenlik kapısının önüne koyar | `app/tr_text.py` (yeni), `app/intent_rules.py` (yeni), `app/coach.py`, `app/coach_insights.py`, `app/action_executor.py`, `app/category_rules.py`, `docs/architecture/adr-049-mesaj-niyeti-tek-kaynak.md` | `tests/test_niyet_kapisi.py` **156 test**: karışık mesaj (7 vaka × 2 yazım × 2 boyut), saf soru/gelecek/bildirim regresyonu, fiilsiz bildirim, nötr selam, BUG #095 vakası, gerekçe↔karar tutarlılığı, **uçtan uca 5 test** (FakeProvider ile gerçek koç akışı: karışık mesaj kayıt üretir, saf soru üretmez), tarih ifadesi 31 × 2 yazım + `TARIH_BELIRSIZ` iki yazımda, koçun açık/kapalı/yansıtma sınıflandırması iki yazımda, **kaynak-türetimli drift kilidi** (desen literalleri modül namespace'i gezilerek toplanır; katlanmamış literal = kırmızı — L27), `normalize` uzunluk korur, `category_rules.normalize is tr_text.normalize`. **Mutasyon 4/4 KIRMIZI:** soru vetosunu koşulsuz yap → kırmızı; gerçekleşmiş-eylem desenini diakritikli yaz → kırmızı; cümle sınıflandırmasında katlamayı atla → kırmızı; tarih ifadesini ham metinde ara → kırmızı. **Yan düzeltme:** `tests/test_date_keyword_regex.py` (BUG #114) ham deseni çağırıyordu → sözleşme sınırı `_tarih_ifadesi_var_mi`'ye taşındı. Ölçüm: korpus **9/25 → 0/25** (düzgün), **12/25 → 0/25** (diakritiksiz), uçtan uca **3/4 → 0/4**, kırık token **20 → 0**. Tam süit **2639 passed, 18 skipped** (2478 + 157 yeni kapı + 4 — BUG #264'ün kategori kapısı `intent_rules.py`/`tr_text.py`'yi kendiliğinden kapsama aldı) + **172 vitest** + `npm run build` OK | ✅ |

## Tur — İÇGÖRÜ SÖZLEŞMESİ + ÖNEM MERDİVENİ (8 Ağu 2026)

| # | ID | Değişiklik | Dosya | Doğrulama | Durum |
|---|----|-----------|-------|-----------|-------|
| — | **BUG #268** (LLM-008 kalanı, ADR-050) | **KOÇ, KULLANICININ "ASLA UNUTMA" DEDİĞİ ŞEYİ TAM OLARAK UNUTUYORDU.** İki yarısı var. **(A) Argümanlar doğrulanmıyordu** (ADR-048'in kardeş tool'unda aynı boşluk): `inp["content"]` ham indeksleniyordu. Ölçüm (FakeProvider ile gerçek koç akışı, her vaka ayrı DB): `content` anahtarı yoksa KeyError yutuldu → kayıt YOK ama koç "Not aldım." dedi; `content` bir nesne (dict) ise **TÜM KOÇ İSTEĞİ ÇÖKTÜ** (`PendingRollbackError`) — çünkü başarısız INSERT session'ı rollback edilmemiş bırakıyor ve sonraki `commit()` (reasoning-trace adımı) patlıyordu; bu **projenin kendi anti-pattern listesindeki** maddedir ("IntegrityError'da `db.begin_nested()` kullan, session zehirlenmesin") ve bu yol onu uygulamıyordu. `expires_at: "gelecek ay"` → ValueError yutuldu, gerçek kayboldu; `dedup_key` boşsa ikinci çağrıda UNIQUE ihlali → gerçek kayboldu; `priority: "cok_kritik"` sessizce `normal`e düştü. **(B) EN SESSİZ YARISI — beyan edilen önem sıralamaya HİÇ ULAŞMIYORDU:** tool açıklaması LLM'e *"critical: asla unutulmamalı"* diyor, ama enjeksiyon (`format_insights_for_prompt`) `sort_priority` (int) + `last_evidence_at` ile sıralayıp **`limit(5)`** uyguluyor ve `save_insight_action` bu iki alanın **ikisini de yazmıyordu** → kullanıcının beyanı varsayılan 5 ile en altta, `last_evidence_at` NULL olduğu için eşitlikte de en sonda kalıyordu. Ölçüm: 6 çıkarıcı gözlemi (sort_priority=10) + *"asla kredi çekmeyeceğim"* beyanı (critical) → **beyan enjekte edilen blokta YOK**, beş rutin gözlem var. `InsightPriority` enum'u yazılıp hiç okunmuyordu (dekoratif alan — **L21**). Prompt'ta ikinci yüzü: `[TİP \| GÜVEN]` etiketi bu yolda NULL olduğu için kullanıcının beyanı `[GENEL \| unknown]` + `(baslik yok)` görünüyordu. **Fix:** tek kaynak `app/insight_schema.py`; tool şeması BURADAN ÜRETİLİR (eski şema `category`/`priority`'yi ZORUNLU sayıyordu ama kod ikisini de opsiyonel okuyup varsayılana düşürüyordu — sözleşme ile davranış çelişiyordu). **Başarısızlık yönü ADR-048'in TERSİ ve bu bilinçli:** içerik YÜK, geri kalanı ETİKET → `content` yoksa/metne dönüşmüyorsa/çok uzunsa REDDET; `dedup_key` yoksa İÇERİKTEN TÜRET; `category`/`priority` tanınmıyorsa belgeli varsayılana **aşağı** düş (tanınmayan etiket "critical" sayılamaz); `expires_at` bozuksa süreyi düşür GERÇEĞİ TUT. Hiçbir düşüş sessiz değil (`Ayiklama.duzeltmeler` → trace). Önem merdiveni tek ölçek (15 deterministik kırmızı-çizgi > **14 kullanıcı beyanı critical** > 12 K2 > **11 kullanıcı beyanı high** > 10-7 desen gözlemleri > **5 normal** = bugünkü varsayılan). Yazma `begin_nested()` savepoint'i içinde. Red kullanıcıya söylenir ama cevap DEĞİŞTİRİLMEZ, sonuna tek cümle eklenir (cevabın kendisi geçerli olabilir — ADR-048'de cevabı komple değiştirmek doğruydu, burada bilgi kaybı olurdu). **D1 araştırması** `research-log.md`'de (2026-08-08: ajan belleği politikası = önem/birleştirme/çürüme/tahliye; Generative Agents importance skoru ve maliyeti; PEEK "öncelikli Evictor" — ayrı LLM önem çağrısı EKLENMEDİ, eksik olan skoru üretmek değil var olan skoru YAZMAYAN yoldu) | `app/insight_schema.py` (yeni), `app/coach.py`, `docs/architecture/adr-050-icgoru-sozlesmesi-ve-onem-merdiveni.md`, `docs/kalite-seruveni/research-log.md` | `tests/test_icgoru_kapisi.py` **34 test**: içerik yoksa/metin değilse/boşsa/çok uzunsa red (7 vaka), metadata düşüşleri raporlanır (kategori/öncelik/tarih), `dedup_key` içerikten türetilir + slug normalize, **kritik beyan enjekte edilir ve gözlemlerin ÜSTÜNDE sıralanır**, prompt etiketi dolu, **merdiven sırası `coach_insights`'ın GERÇEK sabitlerinden türetilerek kilitli** (L27), savepoint izolasyonu, uçtan uca (bozuk içgörü çökertmez + kullanıcıya söylenir; gerçek metadata yüzünden kaybolmaz; aynı anahtar üzerine yazar), şema drift kilidi (koçun kullandığı şema == üretilen şema; zorunlu alanlar kodun gerçekten zorunlu saydıkları). **Mutasyon 5/5 KIRMIZI:** önemi yazma → kırmızı; sözleşmeyi atlayıp ham indeksle → kırmızı; reddi kullanıcıya söyleme → kırmızı; tanınmayan önceliği YUKARI düşür → kırmızı; şemayı sözleşmeden kopar → kırmızı. Tam süit **2675 passed, 18 skipped** (2639 + 34 yeni kapı + 2 otomatik kapsam) + **172 vitest** | ✅ |

## Tur — SAĞLAYICI HATA SINIFLANDIRMASI (8 Ağu 2026)

| # | ID | Değişiklik | Dosya | Doğrulama | Durum |
|---|----|-----------|-------|-----------|-------|
| — | **BUG #269** (LLM-012 + LLM-011, ADR-051) | **FALLBACK ZİNCİRİNİN KARARINI, HATAYLA İLGİSİ OLMAYAN BİR SAYININ RAKAMLARI VERİYORDU.** Canlı yapılandırma `LLM_PROVIDER=fallback`; zincir bir hatayı görünce üç karar verir (tekrar dene / sağlayıcıyı atla / process boyunca kara listeye al) ve üçü de hata metninde **alt-dizi** arıyordu — üstelik sayısal durum kodları (`"429"`, `"503"`, `"504"`) birer düz-metin anahtar kelimesiydi. **Ölçüm (10 gerçekçi sağlayıcı hata metni): 3'ü yanlış.** ① `400 INVALID_ARGUMENT: The input token count (8504) exceeds the maximum...` → içindeki **8504**'ün "504"ü yüzünden **GEÇİCİ** sayılıyordu. Oysa bu **KALICI** bir hatadır (aynı prompt her seferinde aynı hatayı verir): `_call_with_retry` onu 1sn + 2sn bekleyerek **üç kez** deniyor, `_is_request_too_large` bu metni tanımadığı için `_oversized_providers` devre kesicisi **HİÇ açılmıyor** → sağlayıcı **her koç isteğinde** yeniden deneniyor, her denemede kullanıcının LLM kotası yazılıyor (BUG #234 sayacı gerçek istekleri sayar) ve cevap üç saniye geç geliyordu. ② `500 Internal error. request_id=req_8429fa1c` → id'deki "429" yüzünden **KOTA** sayılıp **sağlıklı** bir sağlayıcı devre dışı bırakılıyordu. ③ `Latency budget exceeded: upstream took 4290 ms` → yine "429" yüzünden KOTA. Yan bulgu: Groq'un 413'ü ("Limit 8000, Requested 8429") hem kota hem çok-büyük işareti taşıdığı için operatöre **"quota doldu"** diye loglanıyordu — gerçek sebep değil. **Fix:** tek kaynak `app/provider_errors.py`. Sözleşme: **① önce YAPI** (durum kodu `status_code`/`code`/`http_status` alanından; yoksa metnin **BAŞINDAN** ya da açık etiketten `Error code: NNN` / `'code': NNN` — gövdedeki rastgele sayı durum kodu sayılmaz), **② metin desenleri SAYI İÇERMEZ** (sayısal anahtar kelimeler tamamen kaldırıldı; ölçümün gösterdiği eksik ifade — `token count … exceeds` — eklendi), **③ öncelik KALICI > KOTA > GEÇİCİ** çünkü yanlış tarafa düşmenin bedeli asimetriktir: kalıcıyı geçici sanmak SONSUZ tekrar üretir, geçiciyi kalıcı sanmak yalnız bir denemeyi kaçırır. Karar nesnesi **gerekçe + kullanılan durum kodunu** taşır (log/trace okunabilirliği — BUG #253 ilkesi). **LLM-011 aynı bölgede kapandı:** geri çekilme `taban·2^(n-1)` ile SABİTTİ (aynı anda düşen istekler aynı anda uyanıp sağlayıcıyı ikinci kez birlikte döver) → tam-jitter `[0, min(30sn, taban·2^(n-1))]`, rastgelelik **enjekte edilebilir** (kapı beklemeyi deterministik ölçsün). **Tipli sağlayıcı istisnaları gerekçelendirilerek REDDEDİLDİ** (ADR-051): ADR-002'nin provider-agnostic ilkesini zedeler, sekiz sağlayıcının SDK'sını motor katmanına sızdırır ve `_OpenAICompatMixin` ile gelen jenerik sağlayıcılarda zaten çalışmaz — durum kodu bu SDK'ların ortak paydasıdır. İsimler geriye uyumlu (`_is_quota_exceeded`/`_is_retryable_error`/`_is_request_too_large`) | `app/provider_errors.py` (yeni), `app/coach.py`, `docs/architecture/adr-051-saglayici-hata-siniflandirmasi.md` | `tests/test_saglayici_hata_kapisi.py` **39 test**: ölçülen korpusun 10 vakası + geriye uyumlu yüzeyin sınıflandırmayla tutarlılığı, **sayı bağışıklığı** (8 gömülü sayı: `req_8429fa1c`, `4290 ms`, `8504 tokens`, `trace 15031` …), gerçek durum kodu hâlâ çalışır, kod YAPIDAN okunur, öncelik sırası (çok-büyük kotayı yener + kara listeye alır; kota geçiciyi yener), karar bayrakları + gerekçe boş değil, **kaynak-türetimli drift kilidi** ("desen literalinde çıplak üç haneli sayı YASAK" — defektin kökü buydu, L27), jitter aralığı + tavan + sabit-değer-değil, **zincir davranışı** (token-limiti hatası devre kesiciyi açar). **Mutasyon 5/5 KIRMIZI:** kota desenine çıplak `429` geri koy → kırmızı; token-sayısı ifadesini çıkar → kırmızı; öncelik sırasını boz → kırmızı; jitter'ı kaldır → kırmızı; durum kodunu metnin her yerinde ara → kırmızı. Ölçüm **3/10 → 0/10**. Tam süit **2716 passed, 18 skipped** (2675 + 39 yeni kapı + 2 otomatik kapsam) | ✅ |

## Tur — LLM CEVABINDAN JSON ÇIKARMA (8 Ağu 2026)

| # | ID | Değişiklik | Dosya | Doğrulama | Durum |
|---|----|-----------|-------|-----------|-------|
| — | **BUG #270** (LLM-009) | **"LLM CEVABINDAN JSON ÇIKAR" SORUSUNUN İKİ AYRI CEVABI VARDI; ZAYIF OLANI KULLANICININ BİR ÖZELLİĞİNİ KAYBETTİRİYORDU.** `premortem._parse_and_validate` fence'i yalnız **metnin tamamı** fence ise soyuyor, sonra doğrudan `json.loads` çağırıyordu. **Ölçüm (9 gerçekçi sarmalama biçimi — zayıf modellerin, flash-lite/gpt-oss sınıfı, gerçekte ürettiği hâller): 5'i düşüyordu.** Düşenlerin ortak yanı: **JSON'un kendisi kusursuz, kusur ZARFTA** — "Elbette, işte analiz:" önekli, "Umarım yardımcı olur." kapanışlı, `**Premortem**` başlıklı ya da "Not: tutarlar tahminidir." ekli cevaplar. Zarar: her düşüş premortem'in **iki deneme hakkından birini** yakıyor (bir ek LLM çağrısı = kota + gecikme); model aynı alışkanlığı tekrarlarsa — zayıf modellerde olağan — kullanıcı premortem'i **hiç göremiyordu**. **Sınıf taraması (L11) asıl bulguyu verdi:** aynı sorunun kod tabanında ZATEN daha dayanıklı bir cevabı vardı — `coach_insights._erl_k2_parse_llm_json` (fence regex + ilk `{` / son `}` yedeği). Yani iki yol aynı soruya iki farklı cevap veriyordu (SBN-001 / BUG #161 ailesi: aynı kural birden çok yerde ayrı kodlanmış) ve **kırılgan olan, kullanıcıya görünen özelliği taşıyordu**. **Fix:** tek kaynak `app/llm_json.py` — sözleşme **zarfa TOLERANSLI, içeriğe KATI**: modül yalnız zarfı açar (fence'ler, öndeki/arkadaki düz metin, başlıklar), içeriği doğrulamaz; doğrulama çağıranın Pydantic şemasında kalır. Ayrım bilinçli — zarfı affetmek kullanıcıya bir özellik kazandırır, içeriği affetmek ona **yanlış veri** gösterir (ADR-050'nin "içerik yük, gerisi etiket" ayrımının bu yoldaki karşılığı). Aday sırası: tam metin → fence içerikleri → **dengeli `{...}` blokları**; tarama **dizge-duyarlıdır** (metin içindeki `"a{b"` dengeyi bozmaz — `coach_insights` yedeğinin sessiz zayıflığı buydu) ve metindeki anlamsız `{}` gerçek gövdeyi gölgeleyemez. Başarısızlık adlandırılmış hataya (`JsonZarfiCozulemedi`) döner ve premortem'in mevcut retry yoluna düşer — sessiz `None` değil. **Sağlayıcı `response_schema`/structured output UYGULANMADI** (özgün aksiyon önerisi): sekiz sağlayıcının API'sine dağılır, jenerik OpenAI-uyumlu sağlayıcılarda yok, ADR-002'yi zedeler — zarf toleransı sağlayıcıdan bağımsız çalışır ve şema doğrulaması zaten Pydantic'tedir | `app/llm_json.py` (yeni), `app/premortem.py`, `app/coach_insights.py` | `tests/test_llm_json_kapisi.py` **32 test**: dokuz sarmalamanın hepsi (premortem yolu + `cikar`), içeriğe katılık korundu (eksik alan/senaryo sayısı/`scenarios` tipi hâlâ reddedilir), **dizge-duyarlılık** (metindeki `{`, kaçışlı tırnak, gölgeleyen `{}`), başarısızlıkta adlandırılmış hata + retry yolunun onu yakaladığı (kaynak denetimi), **tek kaynak drift kilidi** (her iki tüketici `_json_cikar` kullanır ve kendi `json.loads`'ını YAPMAZ — L26). **Mutasyon 5/5 KIRMIZI:** dengeli blok taramasını kaldır → kırmızı; taramayı dizge-duyarsız yap → kırmızı; boş bloğun gövdeyi gölgelemesine izin ver → kırmızı; premortem kendi parse'ına dön → kırmızı; coach_insights kendi parse'ına dön → kırmızı. **Yan düzeltme:** `tests/test_premortem_coverage.py` hatanın TİPİNİ iddia ediyordu (`json.JSONDecodeError`) → sözleşmedeki adlandırılmış hataya taşındı (davranış aynı: fail-loud + retry). Ölçüm **5/9 → 0/9**. Tam süit **2750 passed, 18 skipped** (2716 + 32 yeni kapı + 2 otomatik kapsam) | ✅ |

## Tur — SAHTE TAMAMLAMA GÜVENCESİ (8 Ağu 2026)

| # | ID | Değişiklik | Dosya | Doğrulama | Durum |
|---|----|-----------|-------|-----------|-------|
| — | **BUG #271** (LLM-020) | **"KAYDETTİM" GÜVENCESİ ÜÇ AYRI YERDEN DELİKTİ.** `_postprocess_report`'un işi tek cümlede: aksiyon oluşmadıysa koç "kaydettim" izlenimi vermemeli (BUG #041/#085'in kapatmak istediği **finansal güven ihlali**). Ölçüm üç eksende de delik gösterdi: ① **Sahte tamamlama fiilleri 6/12 kaçıyordu** — liste #041 → #085 → #094 boyunca büyümüştü ve hâlâ "işleme aldım", "kayda geçirdim", "not olarak girdim", "sisteme yazdım", "hallettim", "düştüm" hiçbir filtreye takılmıyordu. ② **Çok satırlı yanıtta koruma HİÇ çalışmıyordu:** `is_structured_report` dalı (çok satır / `##` / `[`) taramayı KOMPLE atlıyordu → `"## Durum\n\nHarcamanı kaydettim."` aksiyon yokken dokunulmadan ve **hiçbir uyarı olmadan** kullanıcıya gidiyordu. BUG #085 iter2'nin kaygısı haklıydı (cümle-bölüp-birleştirmek raporu bozuyordu) ama çözümü "hiç bakma"ya dönüşmüştü. ③ **EMANET KASA silicisi bölümün NUMARALANMIŞ olmasını şart koşuyordu** (`^[\s\[#*>\-]*5\s*...`) → `## EMANET KASA`, `**EMANET KASA**`, `### Emanet Kasa` uydurma 12.000 TL ile birlikte geçiyordu (3/6). Yanlış-pozitif 0/5 — yani filtre hassastı ama **kapsamı varsayımdı**. **Fix — asıl karar:** güvence artık **ifadeye değil DURUMA** bağlı. Kullanıcı **saf bildirim** yaptıysa (`app/intent_rules`: gerçekleşmiş VE soru değil — BUG #267'de kurulan sözleşme) ve o turda hiçbir aksiyon doğmadıysa cevabın sonuna dürüst not eklenir; **fiilden ve yanıtın biçiminden bağımsız**. Kalibrasyon kapının kendisi tarafından yakalandı: yalın `gerceklesmis` yetmiyordu — *"bu ay ne kadar HARCADIM?"* da gerçekleşmiş-eylem işareti taşır ve saf analiz cevabına not eklemek her soruda gürültü üretirdi; karışık mesajda da not eklenmez (o yol zaten propose + retry + fiil filtresine bağlı — L36: yanlış tarafa düşmenin bedeli asimetrik, burada gürültünün bedeli daha yüksek). Fiil listesi **ikinci savunma** olarak kaldı: katlanmış yazılıyor (L32, elle taşınan `[iı]`/`[uü]` ikizleri kalktı), ölçülen altı yeni form eklendi ve **korpus kapıya yazıldı** (bir sonraki eş anlamlı sessiz delik değil kırmızı test). Çok satırlı yanıtta artık **iddia içeren SATIR atılır**, rapor iskeleti korunur. EMANET eşleşmesi numaraya değil YAPIYA bağlı (numara opsiyonel + normalize) | `app/coach.py` | `tests/test_sahte_tamamlama_kapisi.py` **50 test**: ölçülen 12 cümlenin tek-satır ve çok-satır hâlleri, **karışık mesajda satır taraması izole** (durum-notu devrede değilken), durum-tabanlı notun dört koşulu (bildirim+aksiyon yok → var; bilinmeyen eş anlamlı → var; aksiyon var → yok; soru → yok; netleştirme mesajıyla çakışmaz), meşru metin 5 vaka dokunulmaz, EMANET 8 başlık biçimi + sonraki bölüm sağlam + cockpit doluyken SİLİNMEZ, **katlanmışlık drift kilidi**. **Mutasyon 5/5 KIRMIZI:** çok satırlı dalda iddia satırını atma → kırmızı; durum-notunu kaldır → kırmızı; EMANET'e numara şartı geri koy → kırmızı; ölçülen yeni fiilleri çıkar → kırmızı; eşleşmeyi ham metinle yap → kırmızı. **Kapının kendi kör noktası mutasyonla bulundu:** durum-notu satır taramasını gölgelediği için tarama bozulsa bile testler yeşil kalıyordu → karışık-mesaj vakası eklendi. Ölçüm: yanıltan **6/12 → 0/12**, çok satırlı **korumasız → 0/6** (rapor sağlam), EMANET **3/6 → 0/6**, yanlış-pozitif **0/5** | ✅ |

## Tur — SİSTEM SÖZLEŞMESİNİN SABİTLİĞİ (8 Ağu 2026)

| # | ID | Değişiklik | Dosya | Doğrulama | Durum |
|---|----|-----------|-------|-----------|-------|
| — | **BUG #272** (LLM-021) | **YÖNLENDİRME, SÖZLEŞMENİN KENDİSİNİ DEĞİŞTİRİYORDU.** Ölçüm (sağlayıcının gördüğü `system_prompt` kaydedilerek): `propose` retry'ında denemeler arası system **DEĞİŞİYOR** (`[RETRY: ...]` ekleniyor, `messages` sabit kalıyor); soru retry'ı ise aynı işi **doğru** şekilde `messages`'a nudge olarak yapıyordu — yani aynı dosyada, bir çağrı arayla **iki farklı teknik** ve doğrusu zaten oradaydı (BUG #270 ile aynı sınıf: bir soruya iki cevap). **Sınıf taraması (L11) iki yer daha buldu:** ① iç plan TALİMATI kendi çağrısının system'ine ekleniyordu, ② **ÜRETİLEN plan metni ANA çağrının system'ine** yazılıyordu — ölçüm: 21.117 karakterlik system prompt'un son 648 karakteri her turda farklı, çünkü sözleşme modelin o turda ürettiği metni taşıyordu. **Gerekçe iki katmanlı ve İDDİA DEĞİL:** (a) prefix eşleşmesiyle çalışan her cache, system değişince prefix'i baştan yazar — retry tam da en çok cache'lenmesi istenen ek çağrıdır; (b) system prompt koçun **yetki yüzeyidir** (ADR-045/prompt_safety), aynı turun iki çağrısında farklı olması yetki metnini deterministik olmaktan çıkarır. **Kazanç iddiası yapılmadı** — cache kazancı LLM-002'ye aittir ve canlı sağlayıcı Gemini olduğu için ölçülemedi (KURAL R3); burada yapılan yalnız yapısal ön koşul. **Fix:** değişmez tek cümleye indi — *bir `chat()` turundaki HER sağlayıcı çağrısı AYNI system prompt'u görür*. Üç yönlendirme (`_RETRY_NUDGE_PROPOSE`, `_RETRY_NUDGE_SORU`, `_PLAN_MESAJ_BASI`) modül seviyesinde tek yerde tanımlı; hepsi `messages` sonuna eklenir | `app/coach.py` | `tests/test_sistem_sozlesmesi_kapisi.py` **10 test**: üç senaryoda (propose retry / soru retry / iç plan) tur içindeki tüm çağrıların system'i AYNI; nudge'lar messages sonunda; plan talimatı ve üretilen plan messages ile gider; **üretilen plan system'e SIZMAZ**; davranış korundu (boş cevap → retry doldurur); **kaynak-türetimli drift kilidi** (ilk sağlayıcı çağrısından SONRA `system_prompt` mutasyonu yasak) + yönlendirmelerin tek yerde olduğu. **Mutasyon 4/4 KIRMIZI:** propose retry'ı system'e enjekte et → kırmızı; üretilen planı system'e yaz → kırmızı; plan talimatını system'e ekle → kırmızı; soru nudge'ını messages'tan çıkar → kırmızı. **Dürüst kayıt:** ilk mutasyon turunda M3'ün kırmızısı **sözdizimi hatasıydı** (sahte kırmızı) — tespit edildi, geçersiz sayıldı ve kaçışsız biçimde yeniden yazıldı. **Yan bulgu (meşru çıktı):** drift kilidi üçüncü bir yeri de gösterdi — `system_prompt += _mkt_block` (FEAT-032 canlı döviz bloğu); ölçüldü, **ilk çağrıdan ÖNCE kurulan bağlam** olduğu için meşru; kilit "ilk çağrıdan SONRA mutasyon yok" olarak daraltıldı (bağlamın system'de olup olmaması LLM-002 kapsamıdır). **Commit kapısı bir regresyon yakaladı ve bu beklenen davranıştı:** `tests/test_coach_behavior_contract.py`'nin iki iddiası ESKİ yerleşimi kodluyordu (`"[RETRY" in prov.systems[1]`) — silinmedi, **yeni sözleşmeye taşındı** (nudge `messages` sonunda + `systems[0] == systems[1]`), ve sahte sağlayıcı artık mesajları da kaydediyor. **İkinci turda ikinci bir eski-sözleşme testi daha çıktı** (`test_coach_deliberation.py::test_soru_yolunda_iki_gecis_plan_sonra_yaz` — plan talimatını/planı `sys`'te arıyordu); o da taşındı ve üçüncü tura girmemek için kalan tüm testler `İÇ PLAN`/`RETRY`/`system_prompt` iddiası açısından tarandı (başka yok). **Ders:** yerleşim değiştiren bir sözleşme düzeltmesinde, eski yerleşimi kodlayan testler tek tek değil TARANARAK bulunmalı — yoksa her commit turu bir tanesini gösterir | ✅ |

## Tur — AKSİYON REDDİ SİNYALLERİ (9 Ağu 2026)

| # | ID | Değişiklik | Dosya | Doğrulama | Durum |
|---|----|-----------|-------|-----------|-------|
| — | **BUG #273** (BE-006 + RESIL-019 + BE-005, ADR-052) | **İŞ KURALI SİNYALİ İSTİSNA STRING'İYLE TAŞINIYORDU — ADR-051'İN AYNI SINIFI, BU KEZ PARA YOLUNDA.** `propose_action` dört ret sinyalini serbest metinle yayıyor (`raise ValueError("HESAP_BELIRSIZ")`), tüketiciler de metni TARAYARAK karar veriyordu (`if "HESAP_BELIRSIZ" in str(e)`). **Ölçüm (gerçek koç akışı, sahte sağlayıcı, her vaka ayrı DB — 4 sinyal × 2 tüketici):** ① matrisin **1 hücresi yanlıştı**: retry yolu `TARIH_BELIRSIZ` dalını **hiç taşımıyordu** (ana akıştan elle kopyalanırken düşmüş — backlog "refactor'da sessizce bozulur" diyordu, oysa ZATEN bozulmuştu). Sonuç: birinci LLM çağrısı tool çağırmayıp retry'a düşen bir harcamada, özette tarih olup payload'da olmadığında işlem **kaydedilmiyor VE kullanıcıya tarih sorusu sorulmuyordu**; hata `else` dalına düşüp `logger.error("retry propose_action hatasi: TARIH_BELIRSIZ")` olarak yutuluyordu (kullanıcı modelin özgün metnini görüyor, neyi düzelteceğini öğrenemiyordu). ② **İç sinyal adı kullanıcının EKRANINDAYDI:** `s.observation = f"Belirsizlik: {str(e)[:200]}"` → `reasoning_traces.observation` → `TracePanel.jsx` "Gözlem" satırı; dört sinyalin dördü de ham hâliyle render ediliyordu (`Belirsizlik: HESAP_BELIRSIZ`) ve "belirsizlik" kelimesi payload reddi için yanlıştı. ③ **KVKK:** sinyal ile teşhis AYNI string olduğu için sinyali loglayan kod kullanıcının **tutarlarını** da logluyordu — `OZET_PAYLOAD_CELISKISI: ozetteki tutar(lar) [3200.0] ile payload amount=320.0 uyusmuyor` iki ayrı satırda ölçüldü (BUG #180 ilkesi, sinyalin BİÇİMİ yüzünden delinmişti). ④ **Sessiz iki tüketici daha:** recurring gelir/gider `trigger-due` uçları reddi `except Exception → logger.error` ile yutup `{"triggered": []}` dönüyordu; cevapta atlama nedenini taşıyan alan YOKTU ve Cockpit yalnız `triggered`ı okuyordu (**dürüst kayıt: bugünkü API şemasıyla bu yolda ret üretmek zor — canlı defekt değil YAPISAL SESSİZLİK**; gerçekleştiği gün `last_triggered` yazılmadığı için her gün yeniden denenir ve her gün sessizce düşerdi). **Fix:** tek kaynak `app/action_errors.py` — `AksiyonReddi(ValueError)` + 5 alt sınıf (`HesapBelirsiz`, `TarihBelirsiz`, `PayloadGecersiz`, `OzetPayloadCeliskisi`, `BilinmeyenAksiyon`). Sözleşme: **(a) karar TİPE bakar** — kullanıcıya söylenecek cümle, iz gerekçesi, trace etiketi ve **retry kararı** (`kullanicidan_bilgi_ister`: eksik olan kullanıcı bilgisiyse modeli yeniden çağırmak aynı öneriyi ürettirir) sınıfın üzerindedir, tüketici tek base'i yakalar → **unutulacak dal kalmaz (L42)**; **(b) değer taşıyan teşhis `str(e)`ye HİÇ girmez** (yalnız `.teshis`; dikkatsiz `logger.warning(str(e))` bile para sızdıramaz — **L43**); (c) birden çok ret varsa `en_oncelikli()` önce kullanıcıdan bilgi isteyeni sorar. Koçun iki propose gövdesi tek yardımcıya indi (`_propose_tek_cagri` — **BE-005 aynı kopyayı işaret ediyordu**), üç `if` bloğu + üç elle yazılmış cümle tek yola indi. Recurring uçlar `atlanan: [{id, ad, neden}]` döner; Cockpit uyarı kartı çizer. `PayloadGecersiz` `action_schema`dan aileye taşındı (ad geriye uyumlu re-export) | `app/action_errors.py` (yeni), `app/action_executor.py`, `app/action_schema.py`, `app/coach.py`, `app/routers/expenses.py`, `app/routers/incomes.py`, `frontend/src/panels/Cockpit.jsx`, `docs/architecture/adr-052-aksiyon-reddi-sinyalleri.md` | `tests/test_aksiyon_sinyali_kapisi.py` **30 test**: sözleşme (her sinyal kararın tamamını taşır, kapsam tabanı 5 sinyal, tutar `str(e)`ye girmez, iz gözlemi iç kod içermez, öncelik sırası), **davranış matrisi 5 sinyal × 2 tüketici parametrik** (ana akış + retry aynı cümleyi verir; retry gerçekten tetiklendi assert'i), bilgi isteyen ret retry ETTİRMEZ, mutlu yol korunur, **sızıntı** (iz kaydında iç kod/tutar yok + log'da tutar yok), recurring `atlanan` (reddedilen kayıt cevapta bildirilir + alan HER ZAMAN döner), **AST kapıları**: `app/` içinde istisna METNİNE bakan karar yasak · `propose_action` yalnız tipli sinyal fırlatır · `propose_action` çağıran her `try` `AksiyonReddi`yi ADIYLA yakalar · kullanıcı mesajları `coach.py`'de kopyalanamaz (**AST sabit taraması** — ham metin taraması satıra bölünmüş kopyayı görmüyordu) · propose gövdesi `chat()` içine geri kopyalanamaz. Ayrıca `frontend/src/atlanan-duzenli-kayit.test.jsx` **3 test** (ad+neden ekranda, gelir+gider birleşir, atlanan yoksa kart çizilmez). **Mutasyon 6/6 KIRMIZI:** retry `TARIH_BELIRSIZ` dalını düşür → kırmızı; teşhisi `str(e)`ye geri koy → kırmızı; iz gözlemine iç kod ekle → kırmızı; recurring reddi yut → kırmızı; kullanıcı mesajını `coach.py`'de kopyala → kırmızı; tüketiciye metin taraması geri koy → kırmızı. **Kapının kendi kör noktası mutasyonla bulundu:** satıra bölünmüş (örtük birleştirmeli) kopya ham metin taramasından kaçıyordu → kopya kapısı AST'ye taşındı. **Sınıf taraması (L11):** `app/` genelinde başka "istisna metnine bakan karar" YOK (AST kapısı bunu kalıcı hâle getirdi); `startswith` kalıntıları protokol ayrıştırması (`Bearer `, `sqlite:///`, `dev-default`) — iş kuralı değil. **Not (kapsam dışı bırakıldı):** `execute_pending_action` başarısızlıkta `{"success": False, "error": str(e)}` döner ve `routers/actions.py` bunu HTTP `detail` yapar; mesajların çoğu bilinçli Türkçe kullanıcı metnidir, ama JSON parse hatası yolunda ham ayrıştırıcı mesajı sızar (bozuk-DB senaryosu, ayrı iş) | ✅ |
| — | **BUG #274** (LLM-006 + OBS-005, ADR-053) | **MALİYET DEFTERİNİN PARA SÜTUNLARI HİÇ YAZILMIYORDU — VE `model` SÜTUNUNU AMAÇ İŞGAL ETMİŞTİ.** `api_call_log` ilk günden beri "maliyet analizi icin de veri kaynagi" diye tanımlıydı, şemada `tokens_in`/`tokens_out` duruyordu ve sağlayıcıların hepsi `usage` döndürüyordu (LLM-007 Temmuz'da kapanmıştı) — yani parçaların hepsi yerindeydi. **Ölçüm (6 gerçekçi senaryo, gerçek uçlardan akıtılmış trafik; koç tek-sağlayıcı / zincirde yedek / retry / usage döndürmeyen yerel model / premortem ucu / arka plan yansıması):** 13 gerçek sağlayıcı isteği → 13 defter satırı (sayım doğru, BUG #234'ün mirası) ama **token'ı olan satır 0/13** — harcanan 101.756 girdi + 7.944 çıktı token'ının tamamı deftere 0 olarak düştü; "koç bu ay ne tuttu?" sorusunun defterde cevabı yoktu. **Model ekseni iki ayrı biçimde kırıktı (7/13 yanlış):** ① zincirde yedek cevapladığında satır BİRİNCİLİN modelini, üstelik `gemini-2.5-flash-lite (fallback: 1 ek provider)` gibi **insan-okur bir etiketle** yazıyordu; ② premortem `model='premortem'`, yansıma `model='reflection'` yazarak **amacı model sütununa** koyuyordu (yansıma ayrıca `provider`ı sabit 'groq' geçiyordu — ADR-052'nin "karar tipte, teşhis ayrı alanda" ayrımının burada delinmiş hâli). **Backlog'un kendi durumu da iyimserdi (R3):** LLM-006 "token trace'te ama cost yok" diyordu; ölçüm trace'in gerçek token'ların yalnız **%24'ünü** yakaladığını gösterdi (yalnız koç sohbetinin ANA çağrısı; plan geçişi, retry denemesi, premortem ve yansıma hiç) — üstelik trace 90 günde siliniyor, yani muhasebe defteri değil hata ayıklama yüzeyi. **Sınıf taraması (L11) dördüncü bir yazar buldu:** `routers/coach._log_api_call` — hiçbir yerden çağrılmıyordu ama token/maliyet/amaç sözleşmesini bilmeyen ölü bir yazma yolu olarak duruyordu; kaldırıldı. **Fix:** ölçüm kancası (`LLMProvider.__init_subclass__` → `_raw_chat`) artık isteğin SONUCUNU da ölçüme veriyor — çalışan model ve sağlayıcının `usage`'ı tam bu noktadan geçip atılıyordu; her gerçek istek bir KAYIT üretir ve defter satırı birebir o kayıttan doldurulur (rezervasyon satırı kayıt[0]'ı, ek satırlar kalanları). Tek kaynak `app/llm_cost.py`: fiyat tablosu **(sağlayıcı, model)** ile anahtarlanır (aynı model adı farklı sağlayıcıda farklı fiyatlı: `gpt-oss-120b` Groq'ta $0.15/$0.60), her satır kaynak+tarih taşır, **bilinmeyen fiyat `None` döner — 0 DEĞİL**, bilinen sıfır (yerel Ollama: sağlayıcı faturası yoktur; `:free` varyantlar) bundan ayrıdır. Amaç `amac` sütununa taşındı (koc/premortem/yansima). **Fiyatlar KURAL D1 ile araştırıldı** (research-log 10 Ağu): Gemini/Groq/Anthropic doğrulandı; **Cerebras ve DeepInfra doğrulanamadı → tabloya YAZILMADI** (tahmin edilmiş fiyat, bilinmeyen fiyattan zararlıdır — kendinden emin yanlış sayı üretir). Şema `est_cost_usd Numeric(12,6)` + `amac String(20)`; göç eski satırların bozuk provider/model değerlerini **kasten düzeltmez** (gerçek model geriye dönük bilinemez; o satırlar raporda "fiyatı bilinmeyen" olarak görünür). Operatör yüzeyi `beta_metrics`: tahmini tutar + amaç kırılımı + **iki ayrı bilinmeyen sayacı** (fiyatı bilinmeyen → tablo güncellenmeli; token döndürmeyen → çöken istek/yerel), toplam bilinçli olarak ALT SINIR. **Sonuç: token 0/13 → 8/13** (kalan 5 doğru şekilde bilinmiyor: 3 çöken deneme + 2 yerel Ollama), **çalışan model 7/13 → 13/13**, amaç 13/13 | `app/llm_cost.py` (yeni), `app/llm_quota.py`, `app/coach.py`, `app/models.py`, `app/routers/coach.py`, `app/routers/premortem.py`, `app/routers/actions.py`, `scripts/beta_metrics.py`, `alembic/versions/c5d6e7f8a9b0_llm_maliyet_sutunlari.py`, `docs/architecture/adr-053-llm-maliyet-muhasebesi.md` | `tests/test_llm_maliyet_kapisi.py` **17 test**: sözleşme (her satır kendi isteğinin token+maliyetini taşır; maliyet aritmetiği elle hesaplanmış beklenen değerle; zincirde satır isteği YİYEN sağlayıcıyı ve ÇALIŞAN modeli yazar; çöken deneme satır yazar ama token uydurmaz), amaç ayrımı (premortem satırı model+amaç ayrı; hiçbir model değeri amaç etiketi değil), **AST drift kilitleri** (`model=` argümanına amaç etiketi yazılamaz — yorum satırındaki örnek sahte kırmızı üretmesin diye metin değil AST; `ApiCallLog(...)` yapıcısı yalnız `app/llm_quota.py`'de çağrılabilir → beşinci yazar eklenemez), bilinmeyen≠sıfır (bilinmeyen fiyat None; yerel bilinen sıfır; aynı model adı farklı sağlayıcıda farklı fiyat; model adı kırpılmaz — `:free` varyant ücretliye uygulanmaz; usage yoksa maliyet uydurulmaz), fiyat tablosu sağlığı (küçük harf anahtar, negatif fiyat yok, kaynak+tarih dolu, canlı yapılandırmadaki modeller tabloda), operatör raporu (iki bilinmeyen ayrı sayılır, toplam bilinmeyeni yutmaz, "tahmin" etiketi). **Mutasyon 6/6 KIRMIZI:** token yazımını kaldır → kırmızı; bilinmeyen fiyatı 0 döndür → kırmızı; satıra yapılandırılan modeli yaz → kırmızı; amaç etiketini `model=`'e geri koy → kırmızı; kancada `usage`'ı at → kırmızı; raporda bilinmeyeni 0 sayıp topla → kırmızı. **Dürüst kayıt:** ilk iki ölçüm turu kendi kurulumumun eseri olan iki yanlış sayı üretti (sahte sağlayıcıların `NAME`'i örnek düzeyindeydi, oysa kanca `type(self).NAME` okur) — düzeltilip yeniden ölçüldü; tam süitte bir kırmızı çıktı (`test_sistem_sozlesmesi_kapisi`) ve o da benim eseriydi: süit koşarken `app/coach.py` docstring'ini düzenledim, `inspect.getsource` kayan satırları okudu — kaynak sabitken yeşil | ✅ |
| — | **BUG #275** (LLM-005 yan bulgusu) | **KALİTE KAPISININ KENDİ ÖLÇÜTÜ, KORUDUĞU SÖZLEŞMEDEN ZAYIFTI.** LLM-005'i (judge) kapsamlarken **premis R3 ile düzeltildi:** "provider kalite karşılaştırması yok" kısmen yanlıştı — `scripts/eval_runner.py` sağlayıcı başına koşuyor ve deterministik kriterlerle puanlıyor. Ama o kriterlerden biri **kendi kopyasını** taşıyordu: `app/coach_eval._FAKE_DONE_RE` (5 kök), oysa aynı soruya ürün kodunda BUG #271'in ÖLÇÜP düzelttiği bir cevap zaten vardı (`_FAKE_PASTTENSE_RE`, 12 kök + `tr_text.normalize`). **Ölçüm (BUG #271'in korpusu, 12 cümle):** eval kriteri **7'sini kaçırıyordu** ("işleme aldım", "kayda geçirdim", "not olarak girdim", "sisteme yazdım", "hallettim", "düştüm", "oluşturdum") → koç kalitesini korumakla görevli araç, yeniden ortaya çıkan bir sahte-tamamlama regresyonunu **YEŞİL puanlardı** (L37'nin araç tarafı: aynı soruya iki cevap, zayıf olan koruma görevini taşıyordu). **Kopya ters yönde de kırıktı:** `tamamladı\w*` kökü nesneden bağımsızdı → ölçüm 4/4 yanlış-pozitif verdi ("Analizi tamamladım, üç riskli kalem var.", "Sana üç adımlı bir plan oluşturdum.") — sağlıklı bir sağlayıcı haksız yere düşerdi. **Fix:** tanıma tek kaynağa indi (`app.coach.sahte_tamamlama_iddiasi_var`, normalize + katlanmış desen); eval kopyası silindi. **Kalan iki cümle bilinçli EKLENMEDİ:** "tamamladım"/"oluşturdum" nesneden bağımsız olduğu için 4/4 yanlış-pozitif üretiyor — #271'in kendi kaydı da bu listenin "saymaya dayandığını ve kapanmış sayılamayacağını" söylüyor; asıl güvence durum-tabanlı `_KAYIT_YOK_NOTU`. Sonuç: eval kaçırma **7/12 → 2/12**, yanlış-pozitif **4/4 → 0/4** | `app/coach.py`, `app/coach_eval.py` | `tests/test_sahte_tamamlama_kapisi.py` **+16 test**: eval kriteri ürünün yakaladığı her korpus cümlesinde kırmızı verir (parametrik, kapsam tabanı assert'li), meşru iş-bitirme cümlesini cezalandırmaz, **AST drift kilidi** (coach_eval fiil deseninin kopyasını tutamaz). **Mutasyon 3/3 KIRMIZI:** eval'e zayıf kopyayı geri koy → kırmızı; tek kaynağı "her şey temiz" yap → kırmızı; nesneden bağımsız fiil ekle → kırmızı (yanlış-pozitif testi yakaladı). **Dürüst kayıt:** commit mesajında kapı "+18 test" yazıyor; doğrusu **+16** (12 parametrik korpus + 3 meşru cümle + 1 AST kilidi) — taban 2863 → **2879**. | ✅ |
| — | **BUG #276** (LLM-005 devamı) | **KALİTE KOŞUMU, TAMAMEN ÖLÜ KOÇU %83.3 İLE ÖDÜLLENDİRİYORDU.** #275'te eval'in bir kriterinin bayat olduğu bulunmuştu; aynı mercek KALAN kriterlere tutuldu ve harness'ın kendi manşet sayısının bozuk olduğu ölçüldü. **Ölçüm (iki sahte sağlayıcı, kanonik senaryo seti):** ① tüm sağlayıcılar düşmüş hâlde (RESIL-004 zarif-bozulma dalı: koç "veriler sağlam, AI yorumlayıcı yok" der) eval **6/8 senaryo, %83.3 pass_rate** veriyordu; ② "Tamam." diyen sessiz koç da **aynı %83.3'ü** alıyordu. Yani `coach_eval`'in docstring'indeki söz ("bir prompt/model/kod değişikliği kaliteyi düşürürse pass_rate düşer" — regresyon ağı) **tutmuyordu**: koçun ÇALIŞIP çalışmadığı bile orana yansımıyordu. **Kök sebep:** 8 senaryonun 6'sı yalnız OLUMSUZ kriter taşıyordu (`no_action`, `no_fake`, `no_confidence`) — bunları hiç cevap vermeyen koç bedavaya sağlar; yalnız 2 `action` senaryosu düşüyordu. Ayrıca `grounded` kriteri `grounding.get("ok", True)` ile **varsayılan TRUE** okuyordu: denetlenmemiş cevap "doğrulandı" sayılıyordu. **Fix:** (a) zarif-bozulma dalı artık YAPISAL bir bayrak taşıyor (`llm_kullanilamadi: True`) — sessizliği başarıdan ayıran şey metin karşılaştırması olamaz (ADR-051'in "önce yapı" ilkesi); (b) yeni OLUMLU kriter `cevapladi` (LLM konuştu + anlamlı uzunluk) her kanonik senaryoya eklendi; (c) `cevapladi` düşerse o senaryonun DİĞER kriterleri geçmiş sayılmaz — "aksiyon önermedi" hiç konuşmayan koç için başarı değildir; (d) `run_eval` `llm_olu_cagri`/`gecerli` döner ve `format_report` geçersiz koşumda uyarıyı **en üste** basar (manşet oran ölü koçu gizleyemez). Sonuç: ölü koç **%83.3 → %0.0** (senaryo 6/8 → 0/8), sessiz koç **%83.3 → %10.0** (senaryo 0/8) | `app/coach.py`, `app/coach_eval.py` | `tests/test_coach_eval.py` **+12 test**: ölü sağlayıcı koşumu geçersiz sayılır (pass_rate 0, uyarı raporda), sessiz koç hiçbir senaryo geçemez ama koşum "geçersiz" DEĞİL (ayrım korunur), **kapsam tabanı** (sağlıklı cevap veren koç puan alır — kapı her şeyi sıfırlamıyor), **drift kilidi** (her kanonik senaryo en az bir OLUMLU kriter taşır — parametrik), cevapsız senaryoda diğer kriterler sıfırlanır. **Mutasyon 4/4 KIRMIZI:** sıfırlama kuralını kaldır → kırmızı; yapısal bayrağı geri al → kırmızı; `cevapladi`yı hep TRUE yap → kırmızı; bir senaryodan olumlu kriteri çıkar → kırmızı | ✅ |
| — | **BUG #277** (LLM-005 devamı) | **KOÇUN YAZILI ÜSLUP SÖZLEŞMESİNİ HİÇBİR ŞEY ÖLÇMÜYORDU.** #275 eval'in bir kriterinin bayat olduğunu, #276 manşet sayının ölü koçu ödüllendirdiğini bulmuştu; üçüncü mercek kriterlerin **KAPSAMINA** tutuldu. **Ölçüm (kanonik senaryo seti, 10 persona):** yapısal olarak KUSURSUZ (aksiyon senaryosunda doğru tool'u çağıran, uydurma sayı kullanmayan) ama V3 prompt'unun üslup maddelerini AÇIKÇA ihlal eden 9 persona kuruldu (dalkavukluk, dolgu, "siz" hitabı, iç jargon, boş teselli, nutuk, sahte niyet, duvar-metin, ezber tavsiye) — **dokuzu da %100 pass_rate / 8-8 senaryo aldı, ihlalsiz referansla BİREBİR aynı.** Harness koçun DOĞRU İŞ yapıp yapmadığını ölçüyor, DÜZGÜN KONUŞUP konuşmadığını hiç ölçmüyordu (**L48**). **Defekt teorik değil:** canlı DB'deki 12 gerçek koç cevabının **5'i** ihlalliydi — biri prompt'un kendi YANLIŞ örneğinin birebir kopyasıydı ("...menüsündeki senaryolara dayanıyor"), 4'ü "siz" hitabı taşıyordu; fix sonrası canlı sağlayıcı koşumunda gerçek koç **8 senaryonun 4'ünde** ihlal verdi ("nakit **kasanızda**" — aynı cümlede sen/siz karışımı, prompt bunu açıkça yasaklar). **İkinci eksen — sözleşmenin kod tarafı olan tek madde (SAHTE NİYET):** `coach._FAKE_NIYET_RE` gerçekçi 12 cümlenin **8'ini kaçırıyordu** ve kaçanların TAMAMI "sen" hitaplı biçimlerdi ("onayını bekliyorum", "onaylarsan kaydediyorum") — oysa AYNI prompt "siz" hitabını yasaklar: **bir kuralın koruması, ikinci bir kuralın ihlaline bağlıydı** (**L49**). Üstelik koruma yalnız retry tetikleyicisiydi ve `offer_propose` dalına bağlıydı → uçtan uca ölçümde (4 mesaj tipi × 2 hitap) sahte-niyet iddiası kullanıcıya **8 hücrenin 7'sinde** ulaşıyordu: kullanıcı hiç oluşmamış bir onay kartını bekliyordu (BUG #271'in niyet karşılığı, kapatılmamış hâli). **Fix:** tek kaynak `app/uslup_kurallari.py` — her madde için katlanmış desenler (L32) + ÖLÇÜLMÜŞ ihlal korpusu + meşru karşı-örnek korpusu; V3 prompt'unun yasak-cümle listesi buradan ÜRETİLİR (elle yazılı ikinci liste kalmadı, L27); ürün güvencesi ifadeye değil DURUMA bağlandı — **onay bekleyen kayıt yoksa** iddia taşıyan satır düşer ve dürüst not eklenir, kayıt VARSA (bu turda doğan ya da önceki turlardan bekleyen) atıf KORUNUR; durum `bekleyen_onay_var` yapısal bayrağıyla chat sözleşmesinin parçası oldu, eval ürünle AYNI ölçütü okur (L46, ADR-051 "önce yapı"). Eval'e üç kriter eklendi: `uslup` (hangi maddenin düştüğü rapora yazılır), `no_fake_niyet`, `oz`. Prompt'un kendi "DOĞRU" örneği de düzeltildi — modele "Onayınızı bekliyorum" diye HİTAP kuralını çiğneten cümle öğretiyordu. **Sonuç:** ihlalli persona %100 → **%77-94** (referans %100'de kaldı), sahte-niyet desen kapsamı **4/12 → 12/12** (meşru korpusta 0/4 yanlış-pozitif), uçtan uca sızıntı **7/8 → 0/8**. **Bilinçli kapsam dışı (sessiz kısıtlama yok):** "MUHAKEME ET / ezber tavsiye yasak", "DOĞRU ÇERÇEVEYLE BAŞLA", "RİSKLİ SEÇENEĞİ İŞARETLE" maddeleri desenle güvenilir ölçülemez — ezber-tavsiye personası hâlâ %100 alıyor; bu judge işidir ve LLM-005'te AÇIK bırakıldı. | `app/uslup_kurallari.py` (yeni), `app/coach.py`, `app/coach_eval.py`, `tests/test_para_birimi_kapisi.py` (muafiyet + tavan 20→25, gerekçeli) | `tests/test_uslup_kapisi.py` **+46 test**: her madde kendi korpusunu yakalar ve meşru cümleyi cezalandırmaz (parametrik), desen kaynakları katlanmış yazılmış (L32 kilidi), ürünün KENDİ metinleri kendi sözleşmesine uyar ("Aksiyon hazırlanAMAdı" ölçümde yakalanmıştı), prompt↔dedektör tek kaynak, durum-temelli güvence (kayıt yokken düşer / varken korunur / çift not yazılmaz), ihlalli persona referanstan ayrışır (parametrik), her kanonik senaryo üslup ölçer (drift kilidi), eval ürünle aynı fonksiyonu çağırır. **Mutasyon 8/8 KIRMIZI:** ek listesini geri al · güvenceyi kaldır · güvenceyi durumdan kopar · bir senaryodan kriteri çıkar · eval'e zayıf kopya koy · deseni diakritikli yaz · prompt örneğini "siz"e döndür · üslup kriterini hep geçir | ✅ |
| — | **BUG #278** (LLM-005 KAPANIŞ) | **LLM-005'İN KALAN ÜÇ AYAĞI: JUDGE + YAN YANA KOŞUM + SKOR SAKLAMA.** #277 koç sözleşmesinin desenle ölçülebilen yarısını kapatmış, ölçülemeyen yarısını (MUHAKEME/ÇERÇEVE/RİSK) açıkça kapsam dışı bırakmıştı. **Önce judge'ın dekoratif olup olmadığı ÖLÇÜLDÜ** (KURAL R3 — LLM-002 tam bu ölçülemezlik yüzünden ertelenmişti): gerçek sağlayıcı zinciriyle, aynı soruya biri EZBER biri MUHAKEMELİ **6 çift cevap** puanlatıldı → **5/6 çift doğru sıralandı, 0 geçersiz**; çekirdek ölçüt MUHAKEME ezber cevapların **6/6'sında** KALDI, muhakemeli cevapların 5/6'sında GEÇTİ. Kaçan çift dürüstçe kaydedildi: "bunu güvenle söyleyemem, elimdeki veri şu" diyen — yani prompt'un TALEP ETTİĞİ — cevabı judge da MUHAKEME'den düşürdü; judge'ın bilinen sınırı. **Yapı:** `app/coach_judge.py` — rubrik TEK KAYNAK ve judge prompt'u ondan üretilir; judge susarsa/bozuk cevap verirse skor **YOK** (0 ya da 100 değil, L45); `uygulanamaz` ayrı sonuçtur ve orana ne pay ne payda girer (selamlaşmada "riskli seçeneği işaretle" ölçütü yoktur — onu "geçti" saymak L47'nin judge karşılığı olurdu); judge sağlayıcısı ölçülen sağlayıcının kendisiyse **ÖZ-DEĞERLENDİRME uyarısı** raporda basılır. `app/eval_store.py` — JSONL kayıt (şema göçü gerektirmez, DB sıfırlansa da yaşar); **metin taşımaz**: koç cevabı, kullanıcı mesajı, judge gerekçesi ve tutar dosyaya ASLA girmez (beta_metrics gizlilik çizgisi, kapı kilitler); `dusus_raporu` kriter bazında düşüşü söyler ve **GEÇERSİZ koşumu "kalite düştü" diye okumaz** (#276 dersi). `scripts/eval_runner.py` — `--saglayicilar a,b` (yan yana), `--judge`, `--judge-saglayici` (yanlılık + kota), `--kaydet`, `--gecmis`. Judge, deterministik koşumun **AYNI** cevaplarını puanlar (`reply_tam`): ikinci geçiş hem maliyeti ikiye katlar hem BAŞKA cevapları puanlardı. **Canlı koşum yapıldı:** deterministik %82.9, judge %59.5 (7 ölçüldü, 1 skor yok), öz-değerlendirme uyarısı tetiklendi; yan-yana koşumda iki sağlayıcı da kota nedeniyle **GEÇERSİZ** etiketi aldı. **Yan bulgu ①:** judge prompt'unun ilk taslağı `--- SON ---` diye kendi ayracını icat etmişti ve `prompt_safety` o işareti tanımadığı için değerlendirilen metin ayracı birebir taklit edebiliyordu (kapı ölçtü) → bölüm işareti savunmanın nötrlediği `##`'e çevrildi (**L50**). **Yan bulgu ②:** para birimi kapısı `"RİSKLİ SEÇENEĞİ İŞARETLE"` içindeki İŞARE-**TL**-E'yi para sabiti sayıyordu; `auth.py`'nin `ACCESS_TTL_MIN` muafiyeti de aynı sınıfın elle telafisiydi → desen harf sınırına bağlandı, muafiyet tümden kalktı, sayılar gerçek değerlerine indi ve tavan 25'e çıkmadan **20'de kaldı** (**L51**). **Dış dünya bulgusu (research-log, KURAL D1):** Gemini ücretsiz katman `gemini-2.5-flash-lite` için **20 istek/gün** (canlı 429 gövdesi) — repo'nun "1000/gün" notu bayattı, düzeltildi; bir `--judge` koşumu 16 istek harcar. | `app/coach_judge.py` (yeni), `app/eval_store.py` (yeni), `scripts/eval_runner.py`, `app/coach_eval.py`, `app/coach.py` (docstring), `tests/test_para_birimi_kapisi.py`, `tests/test_llm_kota_kapisi.py`, `docs/kalite-seruveni/research-log.md` | `tests/test_judge_kapisi.py` **+26 test**: rubrik→prompt tek kaynak, her ölçütün "uygulanamaz" örneği var, gömülü metin bölüm uyduramaz + **savunma-işareti drift kilidi**, judge çöktü/bozuk JSON/eksik ölçüt/geçersiz değer → skor YOK, zarflı JSON ayrışır (LLM-009), "uygulanamaz" orana girmez, hepsi uygulanamazsa oran None, öz-değerlendirme işaretlenir, rapor "SKOR YOK"u gizlemez, kayıt metin taşımaz, kriter kırılımı, bozuk satır atlanır, düşüş raporu kriter bazında, ilk koşumda düşüş yok, geçersiz koşum düşüş sayılmaz, bilinmeyen judge oranı düşüş değil, `reply_tam` saklanır, runner bayrakları, **judge ölçütü deterministik CRITERIA'ya sızmaz**. **Mutasyon 8/8 KIRMIZI:** judge çökünce 100 say · "uygulanamaz"ı geçmiş say · prompt'u elle yaz · kayda serbest metin koy · geçersiz koşumu düşüş say · savunmanın tanımadığı ayraca dön · kısaltılmış cevabı puanla · bilinmeyen oranı 0 yaz | ✅ |
| — | **BUG #279** (B1 / kapalı beta, SEC boyutu) | **DAVET KAPISI VARDI, KAPSAMINI ÖLÇEN HİÇBİR ŞEY YOKTU — VE BİR TÜKETİCİ SESSİZCE DIŞARIDAYDI.** Kapalı beta charter'ının taslağı "allowlist yok" diyordu; **premis R3 ile çürütüldü**: `app/beta_access.py` var, production'da fail-closed (`REGISTRATION_MODE` varsayılanı `invite_only`), hem klasik kayıt (BUG #199) hem OAuth (BUG #226/D05) kapıdan geçiyor ve davranış testleri de var. **Eksik olan davranış değil KAPSAM ölçümüydü (L11/H25):** hiçbir şey "hesap yaratabilen KAÇ yol var, kaçı kapıdan geçiyor?" sorusunu sormuyordu — dördüncü bir kayıt yolu eklendiği gün süit yeşil kalır, kapalı beta **sessizce açılırdı**. **Ölçüm (AST, `app/` tamamı):** `User(...)` yaratan 3 yol var (register / oauth_callback / `POST /api/user`); üçü de korunuyor ama üçüncüsü **farklı** bir kapıyla (`auth_enabled`, BUG #174) ve bu hiçbir yerde yazılı değildi. **Sınıf taraması gerçek bir boşluk buldu:** `POST /api/workspaces/{id}/invite` hedef adresi hiç sorgulamıyordu → owner, kapalı betada **hesap açamayacak** birine workspace daveti gönderiyor; davetli elinde geçerli linkle kayıt duvarına çarpıyor ve **nedenini bilmiyor**, owner da gönderdiğini sanıyor (sızıntı değil, **çift taraflı sessiz ret** — ADR-046 ilkesi). **Fix:** `beta_access`'e iki tek-kaynak fonksiyon — `eposta_daveti_bul()` (bu sorgu `auth.py`'nin OAuth dalında **elle yazılmıştı**, kopya kaldırıldı, L46) ve `hesap_acabilir_mi()` (kayıt dışı tüketiciler için); workspace daveti artık allowlist dışı adreste **400 + net Türkçe gerekçe** döner. | `app/beta_access.py`, `app/routers/auth.py`, `app/routers/workspaces.py` | `tests/test_davet_kapisi_kapsami.py` **16 test** (kapsam tabanı=3 kaynaktan türetilir; her yolun kapısını FİİLEN çağırdığı ayrıca doğrulanır; router katmanında `BetaInvite` kullanımı AST ile yasak). **Mutasyon 6/6 — ve M6 kapının KENDİ kör noktasını buldu:** kopya kilidi yalnız `Name`/`Attribute` düğümlerine bakıyordu, `from app.models import BetaInvite` bir **ImportFrom**'dur ve sessizce geçiyordu → içe aktarma da tarandı. Tam süit yeşil. | ✅ |
| — | **BUG #280** (B3 / kapalı beta, OBS boyutu) | **HATA KAYDEDİLİYORDU AMA KULLANICININ GÖRDÜĞÜ OLAYLA EŞLEŞTİRİLEMİYORDU.** `error_logs` (BUG #195) beklenmedik hataları kendi DB'mizde tutuyor, log dosyası her satırı yazıyordu — ama kullanıcıda yalnız *"Beklenmedik bir hata oluştu."* cümlesi vardı. Kapalı betada davetli "bir şeyler patladı" der ve operatörün elinde **o ANI** bulacak hiçbir tutamak yoktur: log'da binlerce satır, `error_logs`'ta parmak izine göre BİRLEŞTİRİLMİŞ kayıtlar. "Spesifik debug" isteğinin eksik halkası buydu. **Fix:** tek kaynak `app/correlation.py` — sözleşme tek cümle: *bir isteğin kimliği; log satırında, hata yanıtında ve kullanıcının gördüğü ekranda AYNIDIR.* Vekilin kimliği (`X-Request-Id` / Cloudflare `Cf-Ray`) **devralınır** (kenardaki kaydı koparmamak için), yoksa üretilir; gelen değer temizlenir (log enjeksiyonu yüzeyi — ADR-045'in log tarafı); kimlik `secrets` ile üretilir ve **sıralı değildir** (sıralı kimlik trafik hacmini sızdırır); karışan karakterler (0/O, 1/l) elenir çünkü kullanıcı ekrandan okuyup yazacaktır. Kimlik `ContextVar`'da taşınır — parametre olarak taşımak bir gün bir çağrıda unutulur ve zincir sessizce kopar (L42 sınıfı). Middleware **en dışta** durur: gövde-sınırı ve kapasite reddi gibi ERKEN dönen yollar da kimlik taşısın (en çok teşhis gereken istekler onlar). Log filtresi handler'a bağlanır, çağrı yerlerine değil (L24). Frontend: `ApiError.istekId` **başlıktan** okunur (gövde her hata tipinde taşımaz) ve `ErrorBoundary` kodu gösterir — **saf istemci çökmesinde göstermez**, çünkü o çökme sunucuya hiç uğramamıştır ve uydurulmuş kod hiçbir kayda karşılık gelmez (**L33**). | `app/correlation.py` (yeni), `app/main.py`, `app/logging_config.py`, `app/error_tracking.py`, `app/models.py`, `frontend/src/api.js`, `frontend/src/components/ErrorBoundary.jsx`, migration `d6e7f8a9b0c1` | `tests/test_korelasyon_kapisi.py` **27 test** + `frontend/src/korelasyon-kimligi.test.jsx` **4 test**. **Mutasyon 7/7 — M6 kapının kör noktasını İKİ KEZ buldu:** bağlam sızıntısı testi önce `TestClient` üzerinden yazılmıştı (istek ayrı iş parçacığında koşar → görünmez), sonra `asyncio.run` ile (Task bağlamın KOPYASINDA çalışır → yine görünmez); doğrusu koroutini Task'a sarmadan aynı bağlamda elle sürmek oldu. | ✅ |
| — | **BUG #281** (B2 / kapalı beta, UX+OBS) | **GERİ BİLDİRİM TOPLANIYORDU AMA TEŞHİS EDİLEMİYORDU.** Charter taslağı "geri bildirim sistemi diskte yok, sıfırdan yazılacak" diyordu; **premis R3 ile çürütüldü** — FEAT-033 var: `Feedback` modeli + migration + `POST/GET /api/feedback` + `FeedbackWidget` (App.jsx'te kalıcı) + operatör aracı `scripts/beta_triage.py` (BUG #209) + 6 test. Eksik olan SİSTEM değil **TEŞHİS EDİLEBİLİRLİKTİ**: kayıt "kim, ne zaman, hangi sekme, ne yazdı" diyordu ama **"hangi kod koşuyordu ve o an ne patladı"** demiyordu — teşhis edilemeyen geri bildirim gürültüdür. **Fix:** sabit teşhis alan kümesi (`app_version` **SUNUCUDAN** türetilir — istemcinin beyanı bayat sekmede sessizce yanlış olurdu; `istek_id` BUG #280'in korelasyon kimliği, istemciden gelir ama **aynı temizleyiciden** geçer, L46; `viewport_w`; `tarayici` — **ham UA saklanmaz**, yalnız aile adı, çünkü sürüm+platform+cihaz güçlü bir parmak izidir ve teşhis için gerekmez; `pwa`). Dördüncü tür **`kafa_karistirdi`** eklendi: hata da istek de değil, **kullanılabilirlik** sinyali ve kapalı betanın en değerli çıktısı (mevcut üç değer korundu — canlı tabloda 0 satır olduğu için ekleme bedelsizdi ama geçmişe dönük eşleme borcu da üretilmedi). `ErrorBoundary`'ye **"Bunu bildir"**: hatayı gördüğü ANDA bildirim, kod forma taşınır (sonra widget'ı bul demek pratikte bildirmemektir; kod da o an ekranda, sonra kaybolur). `beta_triage.py` teşhis satırını yazar. | `app/models.py`, `app/routers/feedback.py`, `scripts/beta_triage.py`, `frontend/src/api.js`, `frontend/src/components/FeedbackWidget.jsx`, `frontend/src/components/ErrorBoundary.jsx`, migration `e7f8a9b0c1d2` | `tests/test_geri_bildirim_teshis_kapisi.py` **22 test** (alan kümesi SABİT — yeni alan gizlilik kapısından geçer ve kapı kırmızıya döner; sürümün gerçek kaynaktan geldiği; ham UA'nın saklanmadığı; tarayıcı çözümünün **özelden genele** sırası — ters sırada herkes "Safari" görünür ve ölçüm sessizce yanlış olur; **5xx → kod → geri bildirim** uçtan uca zinciri) + `frontend/src/hatadan-bildirime.test.jsx` **4**. E2E kapısı genişletildi: `tema-mobil.spec.js` artık **her panel × her temada** geri bildirim düğmesinin GÖRÜNÜR olduğunu ölçer (statik "bir kez render ediliyor" tespiti yetmez, L29) + panel listesinin `App.jsx` TABS ile aynı büyüklükte olduğunu assert eder (L11/H25). **Mutasyon 7/7.** | ✅ |
| — | **BUG #282** (B5 / kapalı beta, SEC-KVKK) | **RIZA METNİ, KODUN TOPLADIĞI TEŞHİS VERİSİNİ BEYAN ETMİYORDU.** BUG #281 geri bildirim kaydına beş teşhis alanı ekledi (uygulama sürümü, korelasyon kodu, ekran genişliği, tarayıcı ailesi, PWA bayrağı). KVKK v3'ün §2'si dört kategori sayıyordu (kimlik/iletişim · kimlik doğrulama · finansal · kullanım) ve bu alanların hiçbiri onlara **tam** girmiyordu. **Kapsamı eksik beyan edilen rıza sakattır** — ve bu, bu projenin en pahalı hata sınıfının hukuki yüzüdür: kod bir şey toplamaya başlar, metin eski kalır, ayrışma **sessiz** olur. **Zamanlama bilinçli:** sürüm ŞİMDİ yükseltildi çünkü canlıda tek kullanıcı var (yeniden onay maliyeti ~0); davetliler geldikten sonra yükseltmek 3-10 kişiyi **beta ortasında** yeniden onaya zorlardı. **Fix:** `kvkk-consent-v4.md` — teşhis kategorisi + **§2b bağlayıcı sınırlar** (otomatik ekran görüntüsü YOK · işlem/tutar gövdeye otomatik kopyalanmaz · **ham User-Agent saklanmaz** · alan kümesi sabittir); `KVKK_CONSENT_VERSION="v4"` (mevcut mekanizma `_KVKK_DOSYA` ve `/api/legal/kvkk`'yı kendiliğinden takip ediyor, `yeniden_onay_gerekli` bayrağı kullanıcıya çıkar). Ayrıca **B5 davetli paketi** yazıldı: `docs/user-guide/kapali-beta-karsilama.md` (ne yapar / **ne YAPMAZ** / hangi aşamada / verin nerede / uygulamanın kapalı olabileceği dürüstçe) + `kapali-beta-kurulum.md` (iOS Safari & Android Chrome & masaüstü PWA adımları, davet kodu ↔ OAuth ayrımı, silme yolu) + `docs/deployment/cloudflare-tunnel-runbook.md` (tam komut + beklenen çıktı + hata karşılığı + 9 maddelik yayın kapısı + **yazılı bilinen sınırlar**). | `docs/legal/kvkk-consent-v4.md`, `app/routers/auth.py`, `docs/user-guide/*`, `docs/deployment/cloudflare-tunnel-runbook.md` | `tests/test_legal_docs.py` **+2 test**: (a) kayda giren her teşhis alanının metinde beyan edildiği — kontrol **`Feedback` modelinin kendi sütunlarından türetilir**, yeni alan eklendiği an kırmızıya döner ve yeni rıza sürümünü zorunlu kılar; (b) toplanmayanın da yazıldığı (ekran görüntüsü / User-Agent sınırları). Mevcut 45 KVKK/legal testi v4 ile yeşil. | ✅ |
| — | **BUG #283** (B4 / kapalı beta, deploy) | **TÜNEL RUNBOOK'U SONSUZ YÖNLENDİRME DÖNGÜSÜNE GÖTÜRÜYORDU — VE BUNU BELGE DEĞİL KOD SÖYLEDİ.** `cloudflare-tunnel-runbook.md`'nin ilk hâli `service: http://localhost:80` diyordu; belge tutarlı görünüyordu. `deploy/nginx.conf.template` okununca defekt çıktı: :80 **koşulsuz** `return 301 https://$host$request_uri` yapar. Bu, sertifikanın BİZDE olduğu VPS kurulumu için doğrudur — ama tünel yollarında (Cloudflare Tunnel / Tailscale Funnel) **TLS dışarıda sonlanır** ve nginx'e düz HTTP gelir: zaten HTTPS olan istek tekrar HTTPS'e yönlendirilir → tarayıcı döngüye girer, **uygulama hiç açılmaz.** İkinci ve daha sessiz yarısı: `proxy_set_header X-Forwarded-Proto $scheme` tünelde `http` yazar → uygulama isteği HTTP sanar, **HSTS'i atlar** ve `secure` çerez mantığı yanlış tarafa düşer. **Fix:** `deploy/nginx.tunnel.conf.template` — yönlendirmez, TLS sonlandırmaz, `X-Forwarded-Proto: https` **sabitler** (dış hop gerçekten HTTPS'tir), güvenlik başlıklarını VPS şablonuyla **birebir aynı** taşır. Rate-limit bölgesinin tünel arkasında tek kovaya düştüğü de **yazılı** (gerçek IP için PROXY protokolü gerekir) — sessiz bırakılmadı. Ayrıca **`tailscale-funnel-runbook.md`** yazıldı: alan adı GEREKTİRMEYEN 0 TL yol (kalıcı `*.ts.net`, otomatik geçerli sertifika, port yönlendirme yok, CGNAT sorunu yok ve **TLS kendi makinemizde sonlanıyor** — Cloudflare Tunnel'da kenar sonlandırır, yani bu yol gizlilik açısından daha iyi). | `deploy/nginx.tunnel.conf.template` (yeni), `docs/deployment/cloudflare-tunnel-runbook.md` (§0.0 + döngü kontrolü eklendi), `docs/deployment/tailscale-funnel-runbook.md` (yeni) | `tests/security/test_guvenlik_basliklari.py` **+3 test**: tünel şablonunda `return 301` YOK · `listen 443 ssl` YOK · **iki nginx şablonu AYNI başlık kümesini taşır** (birinde sertleştirme yapılıp diğeri unutulursa güvenlik seviyesi moda göre değişir ve kimse fark etmez). Mevcut nginx↔uygulama eşitlik kapısı yeni şablonu **kendiliğinden** kapsamına aldı (`deploy/nginx*` glob'u). | ✅ |
| — | **BUG #284** (B4) | **nginx'SİZ YAYINDA ARAYÜZÜ KİMSE SERVİS ETMİYORDU.** Ölçüm: makinede **Docker/WSL yok** → kapalı betanın ilk adımı Docker'sız koşuyor ve tünel **tek** bir yerel porta vekillik ediyor. nginx yokken o portta hem `/api/*` hem derlenmiş arayüz bulunmalı; yoksa davetli **boş sayfa** görür. **Fix:** `app/spa.py` (`SERVE_SPA=1`) — **varsayılan KAPALI** (Docker/nginx yolu hiç etkilenmez), mount **en sonda** (`/` catch-all'ı önce kayıtlı `/api/*`'ı gölgeleyemez; yukarı taşınırsa TÜM API 404 olur), açıkken **fail-fast** (build yoksa açılışta patlar ve çözümü söyler — sessizce 404 servis etmek operatöre "kurdum" dedirtir, davetliye boş sayfa gösterir). Kök yol SPA modunda uygulamayı döndürür; sağlık zaten `/api/health` + `/api/ready`'de (BUG #247 ayrımı). | `app/spa.py`, `app/main.py` | `tests/test_spa_servisi_kapisi.py` **17 test** (asıl işi: mount'un `/api`'yi yutmadığını ölçmek). | ✅ |
| — | **BUG #285** (B4) | **YAYINA ÇIKARKEN İKİ SESSİZ TEHLİKE.** ① **`ENVIRONMENT` tanımsızsa kapalı beta AÇIKTIR:** `registration_mode()` production değilse varsayılan **`"open"`** döner → adresi bilen herkes kayıt olabilir. BUG #199'un tam olarak engellemek için yazıldığı durum, **tek bir env eksiği** yüzünden geri geliyordu — ve bu, Tailscale Funnel'ın cihaz adını **kalıcı public CT günlüğüne** yazması nedeniyle teorik değil: adres keşfedilebilir hâle geliyor. ② **`FRONTEND_URL` = `localhost:5173`** idi → şifre sıfırlama e-postalarındaki bağlantılar ve OAuth yönlendirmesi **davetlinin ulaşamayacağı** adrese giderdi; hata vermez, kullanıcı tıklar ve hiçbir şey olmaz. **Fix:** `scripts/beta_yerel_kur.py` — `ENVIRONMENT=production` **ve** açıkça `REGISTRATION_MODE=invite_only` (iki katman: birinin varsayılanına güvenilmez), `FRONTEND_URL`/`OAUTH_REDIRECT_BASE`/`SUPPORT_EMAIL`/`SERVE_SPA`/`BUILD_COMMIT`. Script **sır üretmez/yazmaz/basmaz**, `.env`'i yedekler, **kuru koşum varsayılandır**, ve OAuth konsollarına eklenecek callback URL'lerini yazdırır (eklenmezse sosyal giriş sessizce kırılır). | `scripts/beta_yerel_kur.py` | Canlı doğrulama: `/api/ready` 200 · `kayit_modu=invite_only` · `build` gerçek SHA · HSTS+X-Frame+nosniff · `X-Request-Id` (BUG #280 canlıda). | ✅ |
| — | **BUG #286** | **SÜİTİN SONUCU, REPODA OLMAYAN BİR DOSYAYA BAĞLIYDI.** `.env`'e kapalı beta için `REGISTRATION_MODE=invite_only` yazıldığı an süit kırmızıya döndü (`test_register_basarili_token_doner`). Sebep: `load_dotenv()` ortamda olmayan her anahtarı **doldurur** → aynı commit iki makinede **farklı sonuç** verebiliyordu; bu, projenin en temel iddiasını (deterministik süit) sessizce deliyordu. Koruma `conftest.py`'de **zaten vardı** ("Testler .env'e BAĞLI OLMAMALI") ama **kapsamı tahminle kurulmuştu**: yalnız `AUTH_ENABLED` + `ENVIRONMENT`. Koruma var, kapsamı ölçülmemiş — bu projede en az beş kez tekrar eden sınıf (L11/H25). **Fix:** `DAVRANIS_DEGISTIREN_ENV` listesi + kapsamı **KAYNAKTAN doğrulayan** kapı: `app/settings.py` + `app/beta_access.py` + `app/spa.py` içindeki her `os.getenv("X")` AST ile taranır; nötrlenmemiş ve muaf-değilse kırmızı. Muafiyetlerin her biri **neden test sonucunu değiştirmediğiyle** yazılı. | `tests/conftest.py` | `tests/test_env_bagimsizligi_kapisi.py` (kaynak-türetimli kapsam + her anahtarın fiilen temiz olduğu + defektin kendisi: `.env` invite_only iken `registration_mode()=="open"`). | ✅ |
| — | **BUG #287** (B4, canlı) | **CANLI YAYINDA BEMBEYAZ EKRAN — GÜVENLİK BAŞLIĞI ARAYÜZÜ ÖLDÜRÜYORDU.** Kapalı beta Tailscale Funnel ile yayına alındı; kullanıcı telefondan açtı ve **boş beyaz sayfa** gördü. Ölçüm: `index.html` 200, `assets/*.js` 200 (1.013.041 bayt, doğru `content-type`), tüm varlıklar sağlam — ama `#root` **boştu**, React hiç mount olmamıştı. Kök neden: `app/security_headers.py` her yanıta `Content-Security-Policy: default-src 'none'` yazıyordu. Bu başlık **doğruydu** — uygulama saf JSON API sunduğu sürece (docstring'i bile "statik arayüzü nginx sunar" diyordu). BUG #284 ile uygulama arayüzü **kendisi** servis etmeye başlayınca aynı kural kendi arayüzünü öldürdü: tarayıcı JS'i engelledi. **Bunu 17 testlik SPA kapısı YAKALAYAMADI** — `TestClient` CSP **uygulamaz**; CSP'yi yalnız gerçek tarayıcı zorlar. Başlığın "gönderildiğini" test etmek, sayfanın **açıldığını** ölçmez (**L29**'un bu turdaki yüzü; defekt kullanıcı bildirimiyle bulundu). **İkinci bulgu — Docker yolunda da bozuktu:** `script-src 'self'` `index.html`'deki inline tema script'ini de engelliyordu; nginx şablonunda aynı direktif var, yani **ilk gerçek deploy'da** tema sessizce yanlış başlayacaktı. **Fix:** CSP artık **yüzeye göre** (`csp_degeri()`): SPA sunuluyorsa SPA politikası, saf API ise dar politika. Tema başlatıcı `frontend/public/theme-init.js`'e taşındı — CSP `'unsafe-inline'` ile **gevşetilmedi**, kod kurallara uygun yere taşındı (**L51**). | `app/security_headers.py`, `frontend/index.html`, `frontend/public/theme-init.js` | `tests/security/test_guvenlik_basliklari.py` **+4**: SPA modunda CSP arayüzü öldürmez · API modunda dar kalır · **uygulama CSP'si nginx şablonuyla BİREBİR aynı** (iki dağıtım yolu iki farklı güvenlik seviyesi üretemez) · `index.html` inline script taşımaz. Canlı doğrulama: gerçek tarayıcıda giriş ekranı render edildi + telefonda PWA kuruldu. | ✅ |
| — | **BUG #288** (B4, canlı kullanım) | **SERVICE WORKER, API İSTEKLERİNİ ÖLDÜRÜYORDU — VE BAKİYEYİ ÖNBELLEĞE ALIYORDU.** Kullanıcı kapalı betada giriş yaptı, *"Backend ile bağlantı yok / Cockpit yüklenemedi"* gördü; konsolda `FetchEvent.respondWith received an error: no-response`. **Backend sağlamdı** (`/api/ready`, `/api/meta`, `/api/health` üçü de 200; Funnel açık; uvicorn dinliyor) — yani hata mesajı **yanlış yeri** işaret ediyordu. Suçlu PWA'nın service worker'ıydı: `/api/*` için `NetworkFirst` + **`networkTimeoutSeconds: 5`**. Mekanizma: 5 sn'de yanıt gelmezse Workbox **önbelleğe düşer**; o istek önbellekte YOKSA `no-response` fırlatır ve istek **tamamen ölür** — yani zaman aşımı bir yedek değil, **arıza üreticisiydi**. 5 sn zaten yanlış bir tavandı: koç ucu **iki LLM çağrısı** sürer (10-40 sn — `app/capacity.py` yavaş-yol tavanlarıyla çelişiyordu) ve tünel yolu ek gecikme ekler. **Tavanı büyütmek çözüm DEĞİLDİ:** bu bir finans uygulaması ve API yanıtları **bakiye** taşıyor — önbellekten servis edilen bakiye kullanıcıya **taze görünür**, ki bu projenin BUG #239'da kapattığı hatanın ta kendisidir; ayrıca yanıtlar tarayıcı önbelleğinde kalır ve aynı cihazı kullanan başkası okuyabilir (BUG #180). **Karar:** app shell precache **korunur** (uygulama çevrimdışı açılır ve **dürüst** bir "bağlantı yok" hatası gösterir), **veri asla önbellekten gelmez** (`runtimeCaching: []`); ayrıca `/api/*` navigasyonlarına `index.html` dönülmez (`navigateFallbackDenylist`) — aksi halde bir API yolu tarayıcıda açıldığında JSON yerine uygulama HTML'i döner ve teşhis yanıltıcı olur. | `frontend/vite.config.js` | `frontend/src/pwa-onbellek.test.js` **6 test** (yorum satırları taramadan atılır — BUG #273 dersi: gerekçe metni kapıyı tetiklemesin). Canlı doğrulama: yayınlanan `sw.js` içinde `api-cache`/`NetworkFirst` **yok**. | ✅ |
| — | **BUG #290** (B4, operasyon) | **YENİDEN BAŞLATMA "YARIM ÇALIŞAN" BİR SİSTEM BIRAKIYORDU — KAPALI OLMAKTAN DAHA KÖTÜ.** Kullanıcı sordu: *"bilgisayarı kapatsam ne olur?"* Ölçüm: **Tailscale servis** (`StartType: Automatic`) → yeniden başlatınca döner ✅; **uvicorn** → zamanlanmış görev **YOK**, başlangıç klasörü **BOŞ** → dönmez ❌. Sonucu tamamen kapalı olmaktan **daha kötü**: adres çözülür, HTTPS çalışır, tünel açıktır — ama `127.0.0.1:8000`'de kimse yoktur; davetli hata alır, operatör "açık" sanır. **Fix:** üç Windows görevi — `FinancialOS-Baslat` (oturum açılışında, **idempotent**: çalışıyorsa dokunmaz, yoksa iki uvicorn portu çakıştırırdı; ayrıca "başlattım" demez, `/api/health` 200 dönene kadar **ölçer**), `FinancialOS-Saglik` (10 dk; **üç ayrı şeyi** ölçer ve karıştırmaz: uygulama → yeniden başlatılır · tünel → yeniden kurulur · **dış yol → yalnız raporlanır**, çünkü geçici ağ dalgalanmasında servisi sarsmak çalışan sistemi bozar, L6), `FinancialOS-Yedek` (03:15 — fiyat cron'u 02:45 ve gece batch 03:00'ten **sonra**, günün işlenmiş hâlini içersin). **Parola SAKLANMADI:** "kullanıcı oturum açmasa da çalıştır" seçeneği Windows'ta sır saklamayı gerektirir; bilinçli olarak kullanılmadı ve sonucu (**oturum açılınca döner, kilit ekranında dönmez**) yazıldı. **İki ölçüm hatası bulundu ve düzeltildi:** ① `.ps1` dosyaları BOM'suz UTF-8 yazılmıştı — Windows PowerShell 5.1 bunları ANSI sanıyor, Türkçe karakterler tırnak/parantez hizasını bozuyor ve dosya **ayrıştırılamıyordu**; ② dış yol kontrolü normal DNS kullanıyordu, ama **Tailscale bu makinede DNS'i ele geçiriyor** (`-Server 1.1.1.1` bile tailnet IP'si döndürüyor) → kontrol her seferinde **yanlış negatif** veriyordu; DoH'a çevrildi. Genel ders: **bu makineden yapılan düz istek funnel'ı ATLAR — "bende çalışıyor" bu kurulumda kanıt değildir** (bir kez tam olarak bu yüzden yanıldım ve "yol bozuk" dedim; aracım ALPN göndermiyormuş). | `deploy/windows/baslat.ps1`, `saglik.ps1`, `gorevleri_kur.ps1`, `docs/deployment/kalici-cozum-plani.md` | **Kurulum değil ÇALIŞMA ölçüldü:** üç görev de zamanlayıcıdan tetiklendi → `LastTaskResult: 0`; yedek gerçekten üretildi (708 KB, damgalı); `baslat` idempotentliği doğrulandı (dinleyen süreç sayısı 1'de kaldı); sağlık kontrolü çıkış kodu **0** (uygulama + tünel + dış yol). | ✅ |
| — | **BUG #291** (kapalı beta, operasyon/güvenlik) | **SIZAN OTURUMU İPTAL EDECEK ARAÇ YOKTU.** Davetli, tarayıcı geliştirici araçlarından "Copy as cURL" ile kendi isteğini kopyalayıp sohbette paylaştı; içinde **kendi geçerli access token'ı** vardı. **Teşhis (açık yazılıyor, yanlış alarm olmasın): bu bir uygulama sızıntısı DEĞİLDİR** — kullanıcı kendi tarayıcısındaki kendi oturumunun token'ını kendi kopyaladı, her web uygulamasında olağandır, uygulama kimseye başkasının verisini göstermedi. **Ama token bir sohbet geçmişine düştüğü an yakılmıştır** ve o noktada operatörün elinde hiçbir yol yoktu: `token_version` mekanizması **vardı** (BUG #172/#225) ama onu kullanacak bir araç **yoktu** — yani koruma vardı, erişimi yoktu. **Fix:** `scripts/oturum_iptal.py` — `token_version` artırılır; kullanıcının **üretilmiş TÜM token'ları** (access + refresh + şifre sıfırlama) tek hamlede geçersiz olur. **Neden "token'ı kara listeye al" değil:** kara liste elinizde token varsa çalışır; sızıntıda kaç kopya çıktığı **bilinmez** ve JWT durumsuzdur — sayaç yaklaşımı bilinmeyen sayıda kopyayı aynı anda öldürür (asimetri doğru tarafta). Kuru koşum varsayılan, e-posta çıktıda **maskeli** (bu çıktı ekran görüntüsüyle paylaşılabilir — BUG #180 ruhu), ve bedeli yazılı: kullanıcı yeniden giriş yapmak zorunda kalır. | `scripts/oturum_iptal.py` | Canlı uygulandı: user 3 `token_version` 0 → 1. Kontrolün **her kimlikli istekte** koştuğu doğrulandı (`app/dependencies.py:83` + refresh `auth.py:285` + şifre sıfırlama `auth.py:563`). | ✅ |


---

## 11 Ağustos 2026 — beta gözlem turu (BUG #289 / #292 / #293 + öğretici sistem)

Tetikleyici: bir beta kullanıcısının "verilerimi kontrol et" talebi. Talep bir destek
sorusuydu; kontrol sırasında üç canlı defekt ölçüldü.

| # | ID | Değişiklik | Dosya | Doğrulama | Durum |
|---|----|-----------|-------|-----------|-------|
| — | **BUG #294** | **SÜRÜM DAMGASI YALAN SÖYLÜYORDU — "hangi kod koşuyor?" sorusuna yanlış cevap.** Canlı `GET /api/health` `build: 6d3bf26abd62` derken çalışan kod `fc10e0b`di. Kök neden: `build_commit()` YALNIZ `BUILD_COMMIT` env değişkenini okuyordu ve o değişken `.env`'de **elle** tutuluyordu; bu makinedeki kapalı beta "git pull + yeniden başlat" ile güncellendiği için damga deploy'dan deploy'a donuyordu. Damganın var olma sebebi tam olarak bu soruydu (BUG #200/P9) — yani özellik duruyor, işlevi ölmüştü. Bir kullanıcı hata bildirdiğinde yanlış commit'e bakılırdı. Öncelik tersine çevrildi: **önce çalışma kopyasının git'i, sonra env** (konteynerde `.git` yoktur, orada env doğru kaynaktır ve fallback olarak kalır). Kirli çalışma kopyası `+` ile işaretlenir — commit edilmemiş değişiklikle koşmak, o commit'i koşmakla aynı şey değildir. Sonuç süreç ömrü boyunca bir kez hesaplanır (sağlık ucu sıcak yol). **L57: bir alanın DOLU olması DOĞRU olduğunu göstermez — elle güncellenen bir kimlik alanı, güncellenmediği ilk günden itibaren yanlış cevap üretir ve bunu hiçbir şey haber vermez. Kimliği, kimliğin sahibinden türet.** | `app/version.py`, `tests/test_surum_damgasi_kapisi.py` (yeni), `.env` | **8 test, mutasyon 6/6** (env yine öncelikli / kirli işareti kaldırıldı / kirli işareti hep koyulur / önbellek kaldırıldı / kaynak yoksa boş döner / git hatası yutulmaz). Kapının kendi kör noktası bir turda kapandı: `+` testi `_git_commit`'i mock'luyordu, yani işareti KOYAN kodu hiç çalıştırmıyordu — mock bir alt katmana (`subprocess.run`) indirildi. **Canlı doğrulama:** yeniden başlatma sonrası hem yerel hem **Tailscale Funnel** üzerinden `build: fc10e0b8182c+` = gerçek `git rev-parse HEAD`. | ✅ |
| — | **BUG #292** | **KAYIT GÜNÜNÜN NET DEĞERİ KALICI OLARAK 0 KALIYORDU — beta kullanıcılarının HEPSİNDE.** `_ensure_today_snapshot` "o gün kayıt varsa dokunma" diyordu (create-once). Yeni kullanıcı paneli ilk açtığında henüz hiçbir hesabı yoktur → o günün snapshot'ı **0** yazılır; aynı gün hesabını ve işlemlerini girdiğinde kayıt **güncellenmez**; ertesi gün `catch_up_snapshots` yalnız EKSİK günleri doldurduğu için o gün sonsuza kadar 0 kalır. **Canlı ölçüm:** cockpit 7.313 / 20.354 / 10.350 TL derken grafik üçünde de 0. İkinci etkisi daha sinsi: `coach_insights` trendi ve FEAT-017 borç ilerlemesi **EN ESKİ snapshot'ı** baz alır → sahte 0'dan bugüne koç "net değerin 10.350 TL arttı" diyebilirdi. Sözleşme artık **upsert**: snapshot o günün SON BİLİNEN durumudur; geçmiş günlere dokunulmaz; değer değişmediyse DB'ye yazılmaz (cockpit her panel açılışında çağrılır). **L53: "idempotent" iki ayrı şeydir — bir kez yaz (create-once) ve aynı sonuca yakınsa (upsert). Bir günü temsil eden kayıt o gün BİTMEDEN yazılıyorsa create-once yanlış cevaptır.** | `app/routers/cockpit.py`, `tests/test_net_deger_snapshot_kapisi.py` (yeni) | **5 yeni test, mutasyon 6/6** (upsert geri alma / hep yeni satır / hep-yaz / hiç-güncelleme / gün filtresinin kalkması / kırılımın bayat kalması). Kapının kendi kör noktası iki turda kapandı: `id`/`created_at` karşılaştırması "hep yaz" mutasyonunu kaçırıyordu; SQL yazması saymak da yetmedi (SQLAlchemy aynı değeri yazmaz) — ölçüm **boş `commit()`'i de** sayar. **Canlı veri onarıldı** (`scripts/backfill_net_worth`, 5 kullanıcı; yedek `data/backups/2026-08-11-200128.db`). | ✅ |
| — | **BUG #289** | **TEST SÜİTİ CANLI VERİTABANINA YAZIYORDU — ve OKUYORDU.** Üç ayak ölçüldü: ① **Yazma:** uçtan uca ölçüm (canlı DB'nin kopyası üzerinde tüm süit) `api_call_log: 252 → 254`; canlı `scheduler_runs`'ın 50 satırının tamamı `weekly_smoke_test` (içlerinde `RuntimeError: smoke boom`). Tek tek koşulan hiçbir dosya sızdırmıyordu — sızıntı test ETKİLEŞİMİNDEN/arka plan iş parçacığından geliyordu, yani "hangi testi düzelteyim" yaklaşımı bunu bulamazdı. ② **Okuma:** `test_prices_endpoint.py`'nin 7 testi kendi kullanıcısını kurmuyor, kimliksiz fallback'in döndürdüğü "ilk kullanıcı"ya güveniyordu — o kullanıcı geliştiricinin CANLI veritabanındaydı; süit izole edilince hepsi 404 verdi. **L55: yazma kirletir, OKUMA YALAN SÖYLER — yeşilliği o makinede duran veriye bağlı bir test hiçbir şey kanıtlamıyordur.** ③ **e2e:** `npm run e2e` :8000'i varsayıyordu ve o portta **kapalı beta sunucusu koşuyor** — Playwright kendi kullanıcısını gerçek kullanıcıların defterine kaydediyordu. Koruma, testin ne yaptığına değil **sürecin neye BAĞLANABİLDİĞİNE** dayandırıldı (conftest, app import'undan önce). | `tests/conftest.py`, `tests/test_suit_izolasyon_kapisi.py` (yeni), `tests/test_prices_endpoint.py`, `scripts/e2e_izole.py` (yeni), `scripts/suite_db_izolasyon_kontrolu.py`, `frontend/vite.config.js`, `frontend/e2e/*.spec.js` | **Kanıt:** tam süit koşumu öncesi/sonrası canlı DB parmak izi (dosya + WAL + SHM; boyut + mtime) **bit-bit aynı**. Kapı 4 test; koruma kaldırılınca 2'si kırmızı. Kapının kendi kör noktası: WAL modunda yazma `-wal` dosyasına gider, yalnız ana dosyaya bakan kontrol koruma kalksa bile yeşil kalıyordu. Ölçüm aracının escape hatch'i (`FINANCIALOS_SUITE_DB_OVERRIDE`) da kapıya bağlandı — aksi hâlde araç kendi korumasını ölçüp her zaman "temiz" derdi. | ✅ |
| — | **BUG #293** | **"KAPSAMSIZ KAPI ÖLÜ KAPIDIR" DİYEN TESTİN KENDİSİ ÖLÜYDÜ.** `e2e/tema-mobil.spec.js`'in kapsam tabanı testi (panel listesi App.jsx TABS ile aynı büyüklükte mi) yazıldığı günden beri **hiç geçmemiş**: kaynakta `` sınır kaçışı yerine **ham 0x08 (backspace) baytı** duruyordu → hiçbir eşleşme, `idler.length` daima 0. Editörde `/^Hid:` diye görünür ve göze normal bir kaçış gibi gelir. Neden fark edilmedi: e2e ayrı bir koşum ve canlı sunucu/DB gerektirdiği için rutin koşulmuyordu (BUG #289 ile aynı kök). **L56: bir kapının yeşil olduğunu görmediysen kapı yoktur — kırmızı bile olsa haber vermeyen test, olmayan testten farksızdır.** | `frontend/e2e/tema-mobil.spec.js` | İzole e2e: **6 → 7 test yeşil**; kapsam testi ilk kez gerçekten çalışıp 13 sekmeyi okuyor ve PANELLER listesiyle eşleştiriyor. | ✅ |
| — | **FEAT-034 + UX-001** | **ÖĞRETİCİ SİSTEM + PANEL SADELEŞTİRME (kullanıcı talebi).** Betanın ilk davranış verisi öğretici ihtiyacını gösterdi: davetliler bir-iki kayıt girip duruyor, koçu hiç kullanmıyor. Üç katman, **içerik tek kaynakta** (`lib/ogretici.js`) — bileşenler yalnız çizer (ADR-001 ruhu): ① **panel içi ipucu şeridi** (13 panelin her biri için ne-işe-yarar + nasıl-kullanılır + GERÇEK örnek + sık hata; daraltılmış gelir, kapatılınca hatırlanır, yardım köşesinden geri gelir), ② **kurulum sihirbazı** (7 adım, ZORUNLU DEĞİL, ilk girişte bir kez açılır; "yaptın mı?" ölçütü uydurulmaz — `GET /api/onboarding/rehber`'den okunur, BUG #262'nin dersi), ③ **yardım köşesi** (her panelde sağ altta kalıcı düğme: bu ekran ne işe yarar + sihirbazı yeniden başlat + kısayollar + sorun bildir). **Sadeleştirme EKSİLTMEDEN:** `KatlanirBolum` — Cockpit'in ikincil bölümleri katlı gelir ama **özet başlıkta kalır** ("4 kalem · 12.400 TL"); dikkat gerektiren durum (bayat fiyat) katlıyken de rozetle görünür ve bölüm kendiliğinden açılır. | `frontend/src/lib/ogretici.js`, `components/Ipucu.jsx`, `components/OgreticiSihirbaz.jsx`, `components/YardimKosesi.jsx`, `components/KatlanirBolum.jsx` (hepsi yeni), `App.jsx`, `panels/Cockpit.jsx` | **25 yeni vitest** → frontend **214 yeşil**. Kapsam kapısı KAYNAKTAN doğrular: App.jsx TABS ↔ rehber anahtarları (iki yön) ve sihirbaz doğrulama anahtarları ↔ `onboarding.py`'deki `tamamlar` sözlüğü. **Mutasyon 3/3** (panel rehberi silinir / anahtar backend'de yok / örnek boşaltılır). e2e tema-mobil kapısı yeni bileşende **gerçek bir 44px ihlali yakaladı** (İpucu kapat düğmesi 30×30 → düzeltildi) — ADR-011. | ✅ |


---

## 11 Ağustos 2026 gece — CI DİRİLTME TURU (BUG #295-#298)

**Tetikleyici bir soruydu: "sıradaki iş ne olmalı?"** Ölçüm cevabı verdi: GitHub Actions'ın
**son 30 koşumunun 30'u da kırmızıydı** (en az 9 Ağustos'tan beri). Yani o güne kadar
yazılan bütün kapılar — aynı gün eklenenler dahil — uzaktan hiçbir şeyi korumuyordu.
Bu, aynı günün dört bugunun (#289/#292/#293/#294) sınıfının en büyük örneğidir:
**mekanizma var, işlevi ölü.** BUG #293'ün (ölü e2e testi) neden fark edilmediğini de bu
açıklar — kırmızı bir CI'da bir kırmızı daha görünmez.

| # | ID | Değişiklik | Dosya | Doğrulama | Durum |
|---|----|-----------|-------|-----------|-------|
| — | **BUG #301** | **vite 5 → 8: son yüksek açık kapandı, hiçbir şey kırılmadı.** BUG #298'de bilinçli ertelenmişti (beta canlıyken üç major atlamak düzelttiğinden fazlasını kırabilirdi); kullanıcılar uyurken, geri dönüşü pahalı iş için doğru pencerede yapıldı. `vite ^5.4.11 → ^8.0.2`, `@vitejs/plugin-react ^4.3.3 → ^6.0.5`; `vite-plugin-pwa` **değişmedi** — peer aralığı zaten `^8.0.0` içeriyordu (kontrol edildi, tahmin edilmedi). İlk deneme peer çakışmasıyla düştü (`@rolldown/plugin-babel → @babel/core@8`); kirli ağaç üzerinde değil **sıfırdan** kurulunca çözüldü. **Yan düzeltme — sürüm tek kaynağa indi:** `.nvmrc` (20.19) + `package.json engines` (`^20.19.0 \|\| >=22.12.0`, vite 8'in kendi şartı); CI'da elle yazılan `node-version: '20'` yerine `node-version-file` kullanılıyor — o alan bir gün 20.9 getirseydi build sessizce kırılırdı (BUG #299'un aynı sınıfı). | `frontend/package.json`, `frontend/package-lock.json`, `.nvmrc`, `.github/workflows/ci.yml` | Hepsi vite 8 ile: build yeşil (PWA 14 entry) · **vitest 214** · **izole e2e 7** · `npm ci` temiz · `npm audit --audit-level=high` → **found 0 vulnerabilities** (üretim VE geliştirme). **CI üç job'da da yeşil** (üçüncü ardışık). **Canlı doğrulama:** funnel üzerinden `build: 3a03b1f65dde` = git HEAD, arayüz 200, veriler yerinde (5 kullanıcı / 9 hesap / 13 işlem). PWA `registerType: 'autoUpdate'` olduğu için uyanan kullanıcılar yeni sürümü kendiliğinden alır. | ✅ |
| — | **BUG #300** | **POSTGRES URL ÜRETİMİ ŞİFREYİ MASKELİYORDU — dual-dialect kapıları hiç koşamıyordu.** CI'ın son kırmızı işi: 4 failed + 4 error, hepsi `password authentication failed for user "postgres"`. Adres (`localhost` → `127.0.0.1`) ve port (5432 → 55432) düzeltmeleri hatayı **değiştirmedi** — çünkü sorun bağlantı hedefi değil, ŞİFRENİN KENDİSİYDİ. `fresh_pg_database` yeni test veritabanının URL'ini `str(make_url(base).set(database=ad))` ile üretiyordu; SQLAlchemy'nin `URL.__str__()` şifreyi **maskeler** (`postgres:***@...`). Aynı tuzak RLS testlerinde iki kez daha vardı (uygulama rolünün şifresi). **Neden yerelde görünmedi:** yerel gate `pgserver` ile açılır ve `initdb` onu **trust** kimlik doğrulamasıyla kurar — şifre hiç sorulmaz (CI logundaki `initdb: enabling "trust" authentication` satırı ipucuydu). Maskelenmiş şifre orada zararsızdır; şifre isteyen her gerçek Postgres'te (CI servisi, prod, compose) sessizce çöker. **L60: bir sırrı taşıyan nesnenin `str()` biçimi o sırrı KORUMAK için maskelenir — `str()` bir gösterim biçimidir, serileştirme biçimi değil.** | `tests/pg_gate.py`, `tests/test_rls_postgres.py`, `tests/test_pg_url_sifre_kapisi.py` (yeni) | Kapı **postgres gerektirmez** (defektin yerelde görünmemesinin sebebi zaten postgres'in kendisiydi): saf URL üretimini ölçer, projede başka hiçbir yerde `str(make_url(...))` ile bağlantı dizesi üretilmediğini kilitler. Orijinal defekt geri konunca kırmızı (kanıtlandı). **CI SONUCU: üç job da yeşil** — 30+ koşumluk kırmızı seri kırıldı. | ✅ |
| — | **BUG #299** | **LOCK DOSYASI CI'IN npm SÜRÜMÜYLE TUTARSIZDI.** e2e job'ı `npm ci: Missing: esbuild@0.28.2 from lock file` ile düşüyordu. Kök neden sürüm farkı: yerelde npm 11 (node 24), CI'da npm 10 (node 20). npm 11 ile üretilen lock npm 10 tarafından reddediliyor; dahası npm 11'in ürettiği lock **yerelde bile** `npm ci` ile kurulamıyordu (`Valid cpu: arm64, Actual cpu: x64` — platform işaretleme hatası). Lock, CI'ın kullandığı sürümle sıfırdan üretildi. Kalıcı hizalama: `.nvmrc` + `package.json engines` (node >=20 <25, npm >=10 <11). | `frontend/package-lock.json`, `frontend/package.json`, `.nvmrc` | `npm@10 ci` temiz kurdu · vitest 214 · build yeşil · `npm audit --omit=dev`: 0 açık. | ✅ |
| — | **GÖRÜNÜRLÜK** | **Turun asıl dersi bir defekt değil, bir KÖR NOKTAYDI: CI 30+ koşum kırmızı kaldı ve kimse görmedi.** Yerel pre-commit her commit'te süiti koşuyordu; ama CI farklı OS, farklı Node/npm, **`.env` YOKLUĞU** ve gerçek bir PostgreSQL ile koşar — bu turda bulunan altı defektin **dördü yalnızca orada görünüyordu**. Kırmızı normalleşince yeni bir kırmızı da görünmez olur (BUG #293'ün ölü e2e testi tam bu yüzden saklandı). `scripts/ci_durum.py` tek komutla son koşumu (ve isteğe bağlı son N koşumun Y/K şeridini) gösterir; kırmızıysa hangi job ve hangi adım. pre-commit hook'a `--sessiz` modda bağlandı: yeşilse hiç konuşmaz, **commit'i asla engellemez** (ağ yoksa çalışma durmamalı — amacı uyarmak, kapı olmak değil). | `scripts/ci_durum.py` (yeni), `.githooks/pre-commit` | Araç çalıştırıldı: `son 8 koşum (yeni-eski): Y K K K K K K K` — serinin kırıldığı görünür hâle geldi. | ✅ |
| — | **BUG #295** | **DUAL-DIALECT KAPILARI CI'DA FİİLEN ÖLÜYDÜ.** `PG_TEST_URL` adresi `localhost`tu; GitHub runner'da `localhost` önce **::1 (IPv6)** çözülür. Servis konteynerinin port eşlemesi IPv4'tedir; ::1'de ise runner imajının KENDİ PostgreSQL'i dinliyor ve şifresi farklı → `FATAL: password authentication failed for user "postgres"`. Yani RLS/Numeric/NULL-ordering testleri **yanlış veritabanına** bağlanıp 5 hata + 4 setup-error veriyordu. BUG #238 "postgres servisi eklendi, kapılar artık gerçekten koşuyor" demişti — servis eklenmişti ama bağlantı hiç kurulamamıştı (L26'nın bu turdaki yüzü: eklemek ≠ çalışmak). | `.github/workflows/ci.yml` | Postgres kapıları yerelde `scripts/pg_gate_run.py` ile **18 passed**; CI'da adres tekilleştirildi (127.0.0.1). | ✅ |
| — | **BUG #296** | **ANAHTARSIZ KURULUMDA COCKPIT ROZETİ 500 VERİYORDU.** `GET /api/coach/usage` cevabı tamamen DB'den (`api_call_log`) üretir; sağlayıcı yalnızca ETİKET içindir. Buna rağmen uç, cevap üretmeden önce gerçek bir LLM sağlayıcısı kurmaya çalışıyor ve anahtar yoksa `ValueError: GEMINI_API_KEY bulunamadi` fırlatıyordu. Yani bir SAYIYI göstermek için, o sayıyla ilgisi olmayan bir dış servis bağlantısı ön koşuldu. Etkilenen: yeni klon, self-host, offline kurulum ve CI. Geliştirme makinesinde `.env` dolu olduğu için defekt **yerelde görünmez**. **L58: bir uç, döndürdüğü veriden DAHA FAZLASINI ön koşul yapmamalı.** | `app/routers/coach.py`, `tests/test_anahtarsiz_kurulum_kapisi.py` (yeni) | 3 test; fix geri alınınca kırmızı (kanıtlandı). Kapı ayrıca iç hata metninin sızmadığını (BUG #175) ve anahtar yokluğunun YALNIZ koçu etkilediğini ölçer. | ✅ |
| — | **BUG #297** | **TEST FIXTURE'LARI VAR OLMAYAN ENV ADLARINI KURUYORDU — kurdukları dünya sahteydi.** `test_kapasite_kapisi` SMTP için `SMTP_PASSWORD` set ediyordu; kod `SMTP_PASS` okur. Test yine de yeşildi çünkü **geliştiricinin `.env`'indeki gerçek şifre** boşluğu dolduruyordu; CI'da `.env` yok → `smtp_configured()` False → `send_email` kapasite yoluna hiç girmeden çıktı → "bekleme süresi 0.00 sn" ile kırmızı. Aynı yazım hatası üç testte daha vardı ve orada TERSTEN zarar veriyordu: `delenv` ile "SMTP kapalı" senaryosu kurmaya çalışan testler yanlış adı sildikleri için SMTP'yi kapatamıyor, geliştirici makinesinde **SMTP AÇIK** koşuyorlardı. İkinci vaka: `tests/auth/test_auth.py` `AUTH_RATE_MAX=5` set ediyordu — kod o adı hiç okumuyor (gerçek ad `RATE_LIMIT_LOGIN_MAX`); test geçiyordu çünkü **varsayılan da tesadüfen 5**'ti: doğru sonuç, yanlış sebeple. **L59: yanlış yazılmış bir env adı sessizdir (KeyError yok, uyarı yok) ve `.env`'i dolu olan makinede gizlenir.** | `tests/test_kapasite_kapisi.py`, `tests/test_email_verification.py`, `tests/security/test_auth_hardening_p2.py`, `tests/security/test_refresh_rotation_password_policy_p2.py`, `tests/auth/test_auth.py`, `tests/test_env_adi_kapisi.py` (yeni) | Kapı, testlerin dokunduğu HER env adının kodda gerçekten var olduğunu doğrular; `RATE_LIMIT_*` ailesi gibi f-string ile üretilen adlar **kodun kendi bucket listesinden** türetilir (elle beyaz liste değil) → yanlış pozitif yok ama `RATE_LIMIT_YANLISAD_MAX` yakalanır. Orijinal defekt geri konunca kapı **kırmızı** (kanıtlandı). CI'ı taklit eden anahtarsız/SMTP'siz koşum: **3110 passed**. | ✅ |
| — | **BUG #298** | **TEK EŞİK İKİ FARKLI SORUYU KARIŞTIRIYORDU.** `npm audit --audit-level=high` üretim ve geliştirme bağımlılıklarını aynı kefeye koyuyordu. Üretimdeki bir açık kullanıcıya ulaşan koddadır — yayını durdurmalıdır; `vite`/`esbuild` dev-server açığı bundle'a hiç girmez. İkisi birleşince bir dev aracının **major sürüm gerektiren** açığı bütün CI'ı kırmızıya çekiyor, o kırmızı normalleşince gerçek bir üretim açığı da fark edilmez hâle geliyordu. Ayrıştırıldı: üretim tarafı kırmızı yapar, geliştirme tarafı raporlanır. Ayrıca `npm audit fix` ile 5 yüksek açığın 4'ü kapatıldı (nanoid, postcss); kalan `vite 5 → 8` **ayrı iş** olarak açık bırakıldı (beta canlıdayken major dev-araç yükseltmesi düzelttiğinden fazlasını kırar). | `.github/workflows/ci.yml`, `frontend/package-lock.json` | `npm audit --omit=dev --audit-level=high` → **found 0 vulnerabilities**. Ayrıca e2e job'ının `npm ci` adımı da düzeldi: lock dosyası `package.json` ile senkron değildi ("Missing: esbuild@0.28.2 from lock file"). | ✅ |


---

## 12 Ağustos 2026 gece — OPERASYON TURU (BUG #302 + defter temizliği)

Soru: "yapılması gereken bir şey var mı, yoksa kapatalım mı?" Ölçüm bir tane buldu.

| # | ID | Değişiklik | Dosya | Doğrulama | Durum |
|---|----|-----------|-------|-----------|-------|
| — | **BUG #302** | **KAÇIRILAN GECE İŞLERİ TELAFİ EDİLMİYORDU.** `misfire_grace_time=3600` bir işi yalnız **1 saat** gecikmeye kadar kurtarır; fazlası sessizce atlanır ve o gün bir daha koşmaz. 7/24 açık bir sunucuda makul bir varsayım — ama kapalı beta **kişisel bir Windows makinesinde** koşuyor ve o makine her gece açık kalmaz. 02:45–04:00 penceresi uykuda geçtiğinde: yatırım fiyatları güncellenmiyor (kullanıcı bayat fiyata dayanan net değer görür — BUG #239 sınıfı), gece batch'i atlanıyor, iz temizliği koşmuyor (90 günlük saklama sözü kendiliğinden tutulmuyor — KVKK). **Veri zaten vardı, EYLEM yoktu:** `beklenen_periyot_saat` tanımlıydı ve `/api/ops/scheduler` işi "gecikti" diye işaretliyordu — işaretlemek koşturmaz. **L61: bir gecikmeyi ÖLÇEN sistem, onu TELAFİ eden sistem değildir.** Telafi açılışta koşar ama açılışı BEKLETMEZ (`create_task`), işler SIRAYLA gider (BUG #263 kapasite), ölçüt son BAŞARILI koşumdur ve patlayan bir iş zinciri durdurmaz. | `app/scheduler.py`, `app/main.py`, `tests/test_telafi_kapisi.py` (yeni) | **6 test, mutasyon 4/4** (gecikme kontrolü kalkarsa / hiç koşmazsa / başarısız koşum güncel sayılırsa / patlayan iş zinciri durdurursa). **CANLI KANIT:** yeniden başlatmada `5/5 is telafi edildi` — `fetch_investment_prices` (0/0 hesap), `nightly_batch` (5 kullanıcı), `k2_batch` (5 kullanıcı), `nightly_trace_cleanup` (0 trace), `weekly_smoke_test` (**4 api, 0 başarısız** — dış servisler sağlıklı). | ✅ |
| — | **DEFTER TEMİZLİĞİ** | **BUG #289 sızıntıyı durdurdu ama GEÇMİŞ kirlilik defterde kaldı.** `scheduler_runs`'taki 50 satırın 50'si de `weekly_smoke_test`ti; 17'si `RuntimeError: smoke boom` gibi yalnız testte üretilen mesajlar taşıyordu (haftalık koşan bir iş iki günde 50 kez koşmaz). **Kozmetik değil:** operatör cron sağlığına bakınca 50 sahte satır tek işi "çok koşmuş" gösterirken, gerçekten hiç koşmamış gece işleri fark edilmiyordu — kirli defter ölçümü yanlış cevaplıyordu. Silmek bir düzeltme değil, **ölçümü geri kazanmaktır**. Araç varsayılan olarak KURU koşar, yalnız test imzalı satırların işini/gününü hedefler ve kullanıcı verisine (hesap/işlem/koç) hiç dokunmaz. | `scripts/defter_temizle.py` (yeni) | Yedek alındı (`2026-08-11-235523.db`), 50 satır silindi; ardından telafi 5 gerçek koşum yazdı → defterde artık **yalnız gerçek operasyon kaydı** var. | ✅ |
| — | **YANLIŞ ALARM (dürüst kayıt)** | Canlı sürecin `ExecutablePath`'i `C:\Program Files\Python311\python.exe` göründüğü için "canlı, test edilmemiş bağımlılıklarla koşuyor" diye raporlandı (sistem `fastapi 0.115` / venv `0.141`). **Yanlıştı:** Windows'ta venv `python.exe` sistem binary'sinin KOPYASIDIR; süreç listesi orijinal yolu gösterir. Yüklü modüller ölçülünce gerçek görüldü — `venv\Lib\site-packages\...`, yani `sys.prefix` venv. Kayda geçiriliyor çünkü bu turun kendi kuralı bunu gerektiriyor: **kanıt olmadan "sorun var" demek de "yok" demek kadar yanlıştır** ve düzeltme sessizce yapılmaz. | — | `Process.Modules` ölçümü: yüklü `.pyd` dosyalarının tamamı venv'den geliyor. | ✅ |

---

## 12 Ağustos 2026 gündüz — DAVETLİ TURU (BUG #303 + canlı DNS bulgusu)

Tetik iki kullanıcı cümlesiydi: "bilgisayarda ara ara komut paneli açılıp kapanıyor" ve
"Mert siteyi Chrome'da da Brave'de de açamadı". İkisi de görünüşte küçük; ikisi de gerçek
defekt çıktı ve **ikincisi beta kullanıcılarının bir kısmını fiilen dışarıda bırakıyordu.**

| # | ID | Değişiklik | Dosya | Doğrulama | Durum |
|---|----|-----------|-------|-----------|-------|
| — | **BUG #303** | **SAĞLIK KONTROLÜ HER 10 DAKİKADA BİR EKRANDA PENCERE ÇAKIYORDU.** Görev Zamanlayıcı, kullanıcının OTURUMUNDA koşan göreve konsol penceresi yaratır; `-WindowStyle Hidden` pencereyi ancak **açıldıktan sonra** gizler — yani siyah kutu yine görünür. Kullanıcı bunu günde ~100 kez görüyordu. **Kolay ama yanlış çözüm sıklığı azaltmaktı:** rahatsızlık ölçümün BEDELİ değil YAN ETKİSİYDİ ve ölçümü seyrekleştirmek düşen tüneli daha uzun süre düşük bırakırdı (kanıt: `saglik.log` 12 Ağu 08:52 `funnel kapali — yeniden kuruluyor`). Yan etki kaldırıldı, **10 dakikalık sıklık korundu**: üç görev de `gizli_calistir.vbs` üzerinden koşar, konsol penceresi hiç yaratılmaz. Sarmalayıcı çıkış kodunu KORUR (`bWaitOnReturn=True`) — beklemeden dönseydi görev zamanlayıcıdaki "sonuc" sütunu her koşumda "başarılı" derdi; **ölçen sistemin yalan söylemesi hiç ölçmemekten kötüdür.** | `deploy/windows/gizli_calistir.vbs` (yeni), `deploy/windows/gorevleri_kur.ps1` | Sarmalayıcı kapısı: `cmd /c exit 7` → **çıkış kodu 7** (ilk yazımda 1 dönüyordu — her argümanı koşulsuz tırnaklamak `cmd.exe "/c"`yi bozuyordu; tırnak artık yalnız boşluklu argümana konur). Görev yeniden kuruldu, elle tetiklendi: `LastTaskResult 0`. Günlük yedek görevi de aynı yoldan (o da pencere açıyordu). | ✅ |
| — | **BUG #303 (b)** | **DIŞ YOL ÖLÇÜMÜ YANLIŞ NEGATİF ÜRETİYOR VE GERÇEK KESİNTİYİ YANLIŞ ADLA RAPORLUYORDU.** Üç kusur: (1) ölçülen uç `/api/ready` idi — o uç **veritabanına** dokunur, oysa buradaki soru "dış yol açık mı"; uygulamanın hazır olup olmadığı 1. adımda zaten yerel ölçülüyor (iki ayrı soru tek bayrakla — bu defterde tanıdık sınıf). (2) DoH isteği başarısız olursa `catch` sessizce yutuyor ve sonuç "DIŞ YOL erişilemiyor" oluyordu: **çözümlenemeyen DNS ile kapalı tünel aynı şey değildir**, operatör yanlış yere bakar. (3) Tek deneme — relay dalgalanması (ölçüldü: 0.5-3.1 sn) doğrudan HATA satırına dönüyordu. Ayrıca DoH çağrısı `Invoke-RestMethod` ile yapılıyordu ve bu makinede aynı sorguya **boş gövde** döndürüyordu (curl aynı anda doğru cevap veriyordu) — yani ölçüm aracının kendisi sessizce başarısızdı. Artık: `curl.exe` ile **iki çözümleyiciye** (Cloudflare + Google) sorulur, ucuz `/api/health` ölçülür, başarısızlıkta 5 sn sonra bir kez daha denenir ve üç durum AYRI raporlanır — kısmi DNS kesintisi / hiç çözülmüyor / ingress erişilemiyor. | `deploy/windows/saglik.ps1` | Mutasyon niyetli kapı koşumu: var olmayan adla → `DIS ADRES HIC COZULMUYOR ... davetliler SITEYI ACAMAZ`, çıkış 1. Gerçek adla → çıkış 0. **Ve yeni kapı arızayı CANLI yakaladı:** 14:26:32 → `DNS KISMI KESINTI — cozmeyen: cloudflare (cozen: google)`. Eski kapı aynı olayı `DIS YOL erisilemiyor` diye yazıyordu (13:11 / 13:51 / 14:01 satırları). | ✅ |
| — | **CANLI BULGU (kod defekti DEĞİL, kısıt)** | **`ts.net` adı Cloudflare çözümleyicisinde 12 sorgunun 11'inde NXDOMAIN döndü; Google ve AdGuard 12/12 çözdü.** Chrome ve Brave'in "Güvenli DNS" özelliği yaygın olarak Cloudflare'e gider ve işletim sisteminin DNS ayarını atlar → **davetli siteyi açamaz, operatör hiçbir şey göremez** (tailnet adı içeriden çözer). Önbellek bayatlığı değil (Cloudflare'in açık purge aracı çağrıldı, sonuç oynamaya devam etti), DNSSEC değil (`cd=1` ile de NXDOMAIN). Bizim tarafımız sağlam: ingress IP'leri pinlenince **10/10 200**. Ayrıntı + kaynak: `research-log.md` (12 Ağu 2026). **Bu, `kalici-cozum-plani.md`'deki kendi-alan-adı hattının ilk KULLANICIDA GÖRÜLMÜŞ tetikleyicisidir.** Geçici yol davetliye "Güvenli DNS'i kapat" dedirtmek; ücretsiz ve anında ama her yeni davetliye söylenmesi gerektiği için **çözüm değil, betanın bedeli** olarak kayda geçti. | `docs/kalite-seruveni/research-log.md`, `docs/deployment/tailscale-funnel-runbook.md` | 12 sorgu × 2 çözümleyici; purge denemesi; `cd=1` denemesi; `--resolve` ile ingress 10/10 200. | ✅ |

---

## 24 Ağustos 2026 — SEEN-BACKEND KARŞILAŞTIRMA TURU (BUG #304)

Tetikleyici bir dış kaynaktı: `seen-backend` (abisi Rezan'ın projesi, 107 dosya) ile bu depo
karşılaştırıldı. **Karşılaştırmanın ilk somut çıktısı seen'den KOPYALANMADI** — seen'in
`src/lib/config.ts`'i "yapılandırmanın tek kaynağı" sanılıyordu, ölçülünce **7 satır** olduğu
ve seen'in de env'i dağınık okuduğu görüldü (27 çağrı / 12 dosya / 15 ad; burada 77 / 22 / 63).
Yani taşınacak desen yoktu. Ama karşılaştırma **doğru soruyu** sordurdu: "bu depoda env adının
iki tarafı da bağlı mı?" Cevap hayırdı ve iki yönde de canlı defekt çıktı.

| # | ID | Değişiklik | Dosya | Doğrulama | Durum |
|---|----|-----------|-------|-----------|-------|
| — | **BUG #304** | **ENV ADI KAPISI TEK YÖNLÜYDÜ — belge ile kod ayrı ayrı doğru olup birlikte yalan söylüyordu.** BUG #297'de kurulan `test_env_adi_kapisi.py` yalnız **test → kod** eksenine bakıyordu; operatörün gördüğü belge hiç bağlı değildi. Ölçüm iki yönde de gerçek defekt buldu. **(a) ÖLÜ AD:** `.env.example` `AUTH_RATE_MAX=10` ve `AUTH_RATE_WINDOW=60` ilan ediyordu; kod bu iki adı **hiç okumuyor** — gerçekleri `RATE_LIMIT_LOGIN_MAX/_WINDOW` ve aynı dosyanın 30 satır aşağısında zaten duruyorlar. Zarar "dağınıklık" değil: `AUTH_RATE_MAX=100` yazan operatör **rate-limit'i gevşettiğini sanır**, kod varsayılanda kalır, hiçbir hata çıkmaz — belge YAPILMAMIŞ bir yapılandırmayı YAPILMIŞ gösterir. Bu ikili BUG #297'nin metninde ve `PROJE.md:183`te adıyla geçiyordu; yani sorun **biliniyordu**, kapı o yönü kapatmıyordu. **(b) BELGESİZ AD:** `app/` ortamdan **58** ad okuyor, iki örnek dosya **53** ad ilan ediyordu ve bunların **24'ü** hiçbir örnek dosyada yoktu. Aralarında **`SERVE_SPA`** vardı: açıkken `frontend/dist` yoksa `app/spa.py` bilerek fail-fast eder ve **uygulama hiç açılmaz** — operatörün elindeki tek şema, uygulamayı açtırmayan anahtardan söz etmiyordu. **L63: bir env adının iki tarafı vardır — kodun okuduğu ve belgenin vaat ettiği; ikisi ayrı ayrı doğru olabilir ve sistem yine yalan söyler, kapı ikisini de bağlamalıdır.** | `tests/test_env_adi_kapisi.py`, `.env.example` | **4 test** (2 eski + 2 yeni), tam süit **3122 passed / 18 skipped**. **Mutasyon 2/2:** `AUTH_RATE_MAX` örnek dosyaya geri konunca ölü-ad kapısı kırmızı (`assert not {'AUTH_RATE_MAX': ['.env.example']}`); `SERVE_SPA` belgeden silinince belgesiz-ad kapısı kırmızı (`{'SERVE_SPA': ['app/spa.py']}`). Ölçülen kapanış: belgesiz ad **24 → 0**, ölü ad **2 → 0**, ilan edilen ad 53 → 75. Kapı yanlış pozitif üretmesin diye `docker-compose*`, `Dockerfile*`, `Caddyfile`, `docker-entrypoint.sh`, CI ve `deploy/` de taranır (`DOMAIN`, `POSTGRES_PASSWORD`, `WEB_CONCURRENCY` Python'da okunmaz ama gerçektir); kapsam bilinçli olarak `app/` — `scripts/` altındakiler operatörün `.env`'ine girmeyen geliştirme bayraklarıdır. | ✅ |
